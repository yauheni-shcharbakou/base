[build]
watchPatterns = ["admin/apps/auth/**", "admin/packages/**", "packages/**"]
builder = "DOCKERFILE"
dockerfilePath = "admin/apps/auth/Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
