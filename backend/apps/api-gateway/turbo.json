{
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": [
        "src/**/*.ts",
        "Dockerfile",
        "eslint.config.mjs",
        "nest-cli.json",
        "package.json",
        "tsconfig*.json"
      ],
      "outputs": ["dist/**"]
    }
  }
}
