{
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": ["src/**/*.ts", "package.json", "ts-config*.json"],
      "outputs": ["dist/**"]
    },
    "compile": {
      "inputs": ["src/**/*.ts", "compiler/**", "package.json", "ts-config*.json"],
      "outputs": ["src/generated/**"]
    }
  }
}
