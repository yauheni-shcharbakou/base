{
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": ["src/**/*.ts", "package.json", "ts-config*.json"],
      "outputs": ["dist/**"],
      "dependsOn": ["^compile", "^build", "format"]
    },
    "reset:modules": {
      "inputs": ["package.json"]
    }
  }
}
