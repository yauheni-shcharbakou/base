{
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": ["src/**/*.ts", "package.json", "ts-config*.json"],
      "outputs": ["dist/**"]
    },
    "reset:modules": {
      "inputs": ["node_modules/**"]
    }
  }
}
