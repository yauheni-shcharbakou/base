// @ts-nocheck
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               v7.34.0
// source: storage/common/common.messages.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from '@bufbuild/protobuf/wire';

export interface DownloadData {
  url: string;
  fileName: string;
}

export interface DownloadMap {
  items: Map<string, DownloadData>;
}

export interface DownloadMap_ItemsEntry {
  key: string;
  value: DownloadData;
}

export interface UploadOne {
  id?: string;
  chunk?: Uint8Array<ArrayBufferLike>;
}

export interface StorageMeta {
  name: string;
  isPublic: boolean;
  parent: string;
}

export interface StorageManyMeta {
  isPublic: boolean;
  parent: string;
}

export interface GetUrlMap {
  id?: string;
  ids: string[];
  ip?: string;
  userId: string;
}

export interface GetUrlMapWeb {
  id?: string;
  ids: string[];
  ip?: string;
}

function createBaseDownloadData(): DownloadData {
  return { url: '', fileName: '' };
}

export const DownloadData: MessageFns<DownloadData> = {
  encode(message: DownloadData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.url !== '') {
      writer.uint32(10).string(message.url);
    }
    if (message.fileName !== '') {
      writer.uint32(18).string(message.fileName);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DownloadData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDownloadData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.url = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.fileName = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DownloadData {
    return {
      url: isSet(object.url) ? globalThis.String(object.url) : '',
      fileName: isSet(object.fileName) ? globalThis.String(object.fileName) : '',
    };
  },

  toJSON(message: DownloadData): unknown {
    const obj: any = {};
    if (message.url !== '') {
      obj.url = message.url;
    }
    if (message.fileName !== '') {
      obj.fileName = message.fileName;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DownloadData>, I>>(base?: I): DownloadData {
    return DownloadData.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DownloadData>, I>>(object: I): DownloadData {
    const message = createBaseDownloadData();
    message.url = object.url ?? '';
    message.fileName = object.fileName ?? '';
    return message;
  },
};

function createBaseDownloadMap(): DownloadMap {
  return { items: new Map() };
}

export const DownloadMap: MessageFns<DownloadMap> = {
  encode(message: DownloadMap, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    message.items.forEach((value, key) => {
      DownloadMap_ItemsEntry.encode({ key: key as any, value }, writer.uint32(10).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DownloadMap {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDownloadMap();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          const entry1 = DownloadMap_ItemsEntry.decode(reader, reader.uint32());
          if (entry1.value !== undefined) {
            message.items.set(entry1.key, entry1.value);
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DownloadMap {
    return {
      items: isObject(object.items)
        ? (globalThis.Object.entries(object.items) as [string, any][]).reduce(
            (acc: Map<string, DownloadData>, [key, value]: [string, any]) => {
              acc.set(key, DownloadData.fromJSON(value));
              return acc;
            },
            new Map(),
          )
        : new Map(),
    };
  },

  toJSON(message: DownloadMap): unknown {
    const obj: any = {};
    if (message.items?.size) {
      obj.items = {};
      message.items.forEach((v, k) => {
        obj.items[k] = DownloadData.toJSON(v);
      });
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DownloadMap>, I>>(base?: I): DownloadMap {
    return DownloadMap.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DownloadMap>, I>>(object: I): DownloadMap {
    const message = createBaseDownloadMap();
    message.items = (() => {
      const m = new Map();
      ((object.items as Map<string, DownloadData>) ?? new Map()).forEach((value, key) => {
        if (value !== undefined) {
          m.set(key, DownloadData.fromPartial(value));
        }
      });
      return m;
    })();
    return message;
  },
};

function createBaseDownloadMap_ItemsEntry(): DownloadMap_ItemsEntry {
  return { key: '', value: undefined };
}

export const DownloadMap_ItemsEntry: MessageFns<DownloadMap_ItemsEntry> = {
  encode(message: DownloadMap_ItemsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== '') {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== undefined) {
      DownloadData.encode(message.value, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DownloadMap_ItemsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDownloadMap_ItemsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = DownloadData.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DownloadMap_ItemsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : '',
      value: isSet(object.value) ? DownloadData.fromJSON(object.value) : undefined,
    };
  },

  toJSON(message: DownloadMap_ItemsEntry): unknown {
    const obj: any = {};
    if (message.key !== '') {
      obj.key = message.key;
    }
    if (message.value !== undefined) {
      obj.value = DownloadData.toJSON(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DownloadMap_ItemsEntry>, I>>(
    base?: I,
  ): DownloadMap_ItemsEntry {
    return DownloadMap_ItemsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DownloadMap_ItemsEntry>, I>>(
    object: I,
  ): DownloadMap_ItemsEntry {
    const message = createBaseDownloadMap_ItemsEntry();
    message.key = object.key ?? '';
    message.value =
      object.value !== undefined && object.value !== null
        ? DownloadData.fromPartial(object.value)
        : undefined;
    return message;
  },
};

function createBaseUploadOne(): UploadOne {
  return { id: undefined, chunk: undefined };
}

export const UploadOne: MessageFns<UploadOne> = {
  encode(message: UploadOne, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      writer.uint32(10).string(message.id);
    }
    if (message.chunk !== undefined) {
      writer.uint32(18).bytes(message.chunk);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UploadOne {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUploadOne();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.chunk = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UploadOne {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : undefined,
      chunk: isSet(object.chunk) ? bytesFromBase64(object.chunk) : undefined,
    };
  },

  toJSON(message: UploadOne): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = message.id;
    }
    if (message.chunk !== undefined) {
      obj.chunk = base64FromBytes(message.chunk);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UploadOne>, I>>(base?: I): UploadOne {
    return UploadOne.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UploadOne>, I>>(object: I): UploadOne {
    const message = createBaseUploadOne();
    message.id = object.id ?? undefined;
    message.chunk = object.chunk ?? undefined;
    return message;
  },
};

function createBaseStorageMeta(): StorageMeta {
  return { name: '', isPublic: false, parent: '' };
}

export const StorageMeta: MessageFns<StorageMeta> = {
  encode(message: StorageMeta, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== '') {
      writer.uint32(10).string(message.name);
    }
    if (message.isPublic !== false) {
      writer.uint32(16).bool(message.isPublic);
    }
    if (message.parent !== '') {
      writer.uint32(26).string(message.parent);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StorageMeta {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStorageMeta();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.isPublic = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.parent = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StorageMeta {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : '',
      isPublic: isSet(object.isPublic) ? globalThis.Boolean(object.isPublic) : false,
      parent: isSet(object.parent) ? globalThis.String(object.parent) : '',
    };
  },

  toJSON(message: StorageMeta): unknown {
    const obj: any = {};
    if (message.name !== '') {
      obj.name = message.name;
    }
    if (message.isPublic !== false) {
      obj.isPublic = message.isPublic;
    }
    if (message.parent !== '') {
      obj.parent = message.parent;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StorageMeta>, I>>(base?: I): StorageMeta {
    return StorageMeta.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StorageMeta>, I>>(object: I): StorageMeta {
    const message = createBaseStorageMeta();
    message.name = object.name ?? '';
    message.isPublic = object.isPublic ?? false;
    message.parent = object.parent ?? '';
    return message;
  },
};

function createBaseStorageManyMeta(): StorageManyMeta {
  return { isPublic: false, parent: '' };
}

export const StorageManyMeta: MessageFns<StorageManyMeta> = {
  encode(message: StorageManyMeta, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.isPublic !== false) {
      writer.uint32(8).bool(message.isPublic);
    }
    if (message.parent !== '') {
      writer.uint32(18).string(message.parent);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StorageManyMeta {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStorageManyMeta();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.isPublic = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.parent = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StorageManyMeta {
    return {
      isPublic: isSet(object.isPublic) ? globalThis.Boolean(object.isPublic) : false,
      parent: isSet(object.parent) ? globalThis.String(object.parent) : '',
    };
  },

  toJSON(message: StorageManyMeta): unknown {
    const obj: any = {};
    if (message.isPublic !== false) {
      obj.isPublic = message.isPublic;
    }
    if (message.parent !== '') {
      obj.parent = message.parent;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StorageManyMeta>, I>>(base?: I): StorageManyMeta {
    return StorageManyMeta.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StorageManyMeta>, I>>(object: I): StorageManyMeta {
    const message = createBaseStorageManyMeta();
    message.isPublic = object.isPublic ?? false;
    message.parent = object.parent ?? '';
    return message;
  },
};

function createBaseGetUrlMap(): GetUrlMap {
  return { id: undefined, ids: [], ip: undefined, userId: '' };
}

export const GetUrlMap: MessageFns<GetUrlMap> = {
  encode(message: GetUrlMap, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      writer.uint32(10).string(message.id);
    }
    for (const v of message.ids) {
      writer.uint32(18).string(v!);
    }
    if (message.ip !== undefined) {
      writer.uint32(26).string(message.ip);
    }
    if (message.userId !== '') {
      writer.uint32(34).string(message.userId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetUrlMap {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetUrlMap();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.ids.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.ip = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetUrlMap {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : undefined,
      ids: globalThis.Array.isArray(object?.ids)
        ? object.ids.map((e: any) => globalThis.String(e))
        : [],
      ip: isSet(object.ip) ? globalThis.String(object.ip) : undefined,
      userId: isSet(object.userId) ? globalThis.String(object.userId) : '',
    };
  },

  toJSON(message: GetUrlMap): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = message.id;
    }
    if (message.ids?.length) {
      obj.ids = message.ids;
    }
    if (message.ip !== undefined) {
      obj.ip = message.ip;
    }
    if (message.userId !== '') {
      obj.userId = message.userId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetUrlMap>, I>>(base?: I): GetUrlMap {
    return GetUrlMap.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetUrlMap>, I>>(object: I): GetUrlMap {
    const message = createBaseGetUrlMap();
    message.id = object.id ?? undefined;
    message.ids = object.ids?.map((e) => e) || [];
    message.ip = object.ip ?? undefined;
    message.userId = object.userId ?? '';
    return message;
  },
};

function createBaseGetUrlMapWeb(): GetUrlMapWeb {
  return { id: undefined, ids: [], ip: undefined };
}

export const GetUrlMapWeb: MessageFns<GetUrlMapWeb> = {
  encode(message: GetUrlMapWeb, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      writer.uint32(10).string(message.id);
    }
    for (const v of message.ids) {
      writer.uint32(18).string(v!);
    }
    if (message.ip !== undefined) {
      writer.uint32(26).string(message.ip);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetUrlMapWeb {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetUrlMapWeb();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.ids.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.ip = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetUrlMapWeb {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : undefined,
      ids: globalThis.Array.isArray(object?.ids)
        ? object.ids.map((e: any) => globalThis.String(e))
        : [],
      ip: isSet(object.ip) ? globalThis.String(object.ip) : undefined,
    };
  },

  toJSON(message: GetUrlMapWeb): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = message.id;
    }
    if (message.ids?.length) {
      obj.ids = message.ids;
    }
    if (message.ip !== undefined) {
      obj.ip = message.ip;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetUrlMapWeb>, I>>(base?: I): GetUrlMapWeb {
    return GetUrlMapWeb.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetUrlMapWeb>, I>>(object: I): GetUrlMapWeb {
    const message = createBaseGetUrlMapWeb();
    message.id = object.id ?? undefined;
    message.ids = object.ids?.map((e) => e) || [];
    message.ip = object.ip ?? undefined;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, 'base64'));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString('base64');
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(''));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isObject(value: any): boolean {
  return typeof value === 'object' && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
