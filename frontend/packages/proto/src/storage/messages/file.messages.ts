// @ts-nocheck
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               v7.34.0
// source: storage/messages/file.messages.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from '@bufbuild/protobuf/wire';
import { Timestamp } from '../../google/protobuf/timestamp';
import {
  File,
  FileUploadStatus,
  fileUploadStatusFromJSON,
  fileUploadStatusToJSON,
  fileUploadStatusToNumber,
} from '../models/file';
import { StorageObjectManyMetadata, StorageObjectMetadata } from './storage-object.messages';

export interface FileQuery {
  id?: string;
  ids: string[];
  mimeType?: string;
  mimeTypes: string[];
  userId?: string;
  userIds: string[];
  extension?: string;
  uploadStatus?: FileUploadStatus;
  uploadStatuses: FileUploadStatus[];
  createdAfter?: Date;
}

export interface FileRequest {
  query: FileQuery;
}

export interface FileGetListResponse {
  items: File[];
  total: number;
}

export interface FileCreate {
  originalName: string;
  size: number;
  mimeType: string;
}

export interface FileCreateRequest {
  file: FileCreate;
  storage?: StorageObjectMetadata;
}

export interface FileCreateManyItem {
  file: FileCreate;
  uploadId: string;
}

export interface FileCreateManyRequest {
  storage?: StorageObjectManyMetadata;
  items: FileCreateManyItem[];
}

export interface FileCreateManyResponse {
  items: File[];
}

export interface FileUploadResponse {
  entity?: File;
  canSendChunks?: boolean;
  ack?: boolean;
}

function createBaseFileQuery(): FileQuery {
  return {
    id: undefined,
    ids: [],
    mimeType: undefined,
    mimeTypes: [],
    userId: undefined,
    userIds: [],
    extension: undefined,
    uploadStatus: undefined,
    uploadStatuses: [],
    createdAfter: undefined,
  };
}

export const FileQuery: MessageFns<FileQuery> = {
  encode(message: FileQuery, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      writer.uint32(10).string(message.id);
    }
    for (const v of message.ids) {
      writer.uint32(18).string(v!);
    }
    if (message.mimeType !== undefined) {
      writer.uint32(26).string(message.mimeType);
    }
    for (const v of message.mimeTypes) {
      writer.uint32(34).string(v!);
    }
    if (message.userId !== undefined) {
      writer.uint32(42).string(message.userId);
    }
    for (const v of message.userIds) {
      writer.uint32(50).string(v!);
    }
    if (message.extension !== undefined) {
      writer.uint32(58).string(message.extension);
    }
    if (message.uploadStatus !== undefined) {
      writer.uint32(64).int32(fileUploadStatusToNumber(message.uploadStatus));
    }
    writer.uint32(74).fork();
    for (const v of message.uploadStatuses) {
      writer.int32(fileUploadStatusToNumber(v));
    }
    writer.join();
    if (message.createdAfter !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAfter), writer.uint32(82).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FileQuery {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFileQuery();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.ids.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.mimeType = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.mimeTypes.push(reader.string());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.userIds.push(reader.string());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.extension = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.uploadStatus = fileUploadStatusFromJSON(reader.int32());
          continue;
        }
        case 9: {
          if (tag === 72) {
            message.uploadStatuses.push(fileUploadStatusFromJSON(reader.int32()));

            continue;
          }

          if (tag === 74) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.uploadStatuses.push(fileUploadStatusFromJSON(reader.int32()));
            }

            continue;
          }

          break;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.createdAfter = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FileQuery {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : undefined,
      ids: globalThis.Array.isArray(object?.ids)
        ? object.ids.map((e: any) => globalThis.String(e))
        : [],
      mimeType: isSet(object.mimeType) ? globalThis.String(object.mimeType) : undefined,
      mimeTypes: globalThis.Array.isArray(object?.mimeTypes)
        ? object.mimeTypes.map((e: any) => globalThis.String(e))
        : [],
      userId: isSet(object.userId) ? globalThis.String(object.userId) : undefined,
      userIds: globalThis.Array.isArray(object?.userIds)
        ? object.userIds.map((e: any) => globalThis.String(e))
        : [],
      extension: isSet(object.extension) ? globalThis.String(object.extension) : undefined,
      uploadStatus: isSet(object.uploadStatus)
        ? fileUploadStatusFromJSON(object.uploadStatus)
        : undefined,
      uploadStatuses: globalThis.Array.isArray(object?.uploadStatuses)
        ? object.uploadStatuses.map((e: any) => fileUploadStatusFromJSON(e))
        : [],
      createdAfter: isSet(object.createdAfter) ? fromJsonTimestamp(object.createdAfter) : undefined,
    };
  },

  toJSON(message: FileQuery): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = message.id;
    }
    if (message.ids?.length) {
      obj.ids = message.ids;
    }
    if (message.mimeType !== undefined) {
      obj.mimeType = message.mimeType;
    }
    if (message.mimeTypes?.length) {
      obj.mimeTypes = message.mimeTypes;
    }
    if (message.userId !== undefined) {
      obj.userId = message.userId;
    }
    if (message.userIds?.length) {
      obj.userIds = message.userIds;
    }
    if (message.extension !== undefined) {
      obj.extension = message.extension;
    }
    if (message.uploadStatus !== undefined) {
      obj.uploadStatus = fileUploadStatusToJSON(message.uploadStatus);
    }
    if (message.uploadStatuses?.length) {
      obj.uploadStatuses = message.uploadStatuses.map((e) => fileUploadStatusToJSON(e));
    }
    if (message.createdAfter !== undefined) {
      obj.createdAfter = message.createdAfter.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FileQuery>, I>>(base?: I): FileQuery {
    return FileQuery.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FileQuery>, I>>(object: I): FileQuery {
    const message = createBaseFileQuery();
    message.id = object.id ?? undefined;
    message.ids = object.ids?.map((e) => e) || [];
    message.mimeType = object.mimeType ?? undefined;
    message.mimeTypes = object.mimeTypes?.map((e) => e) || [];
    message.userId = object.userId ?? undefined;
    message.userIds = object.userIds?.map((e) => e) || [];
    message.extension = object.extension ?? undefined;
    message.uploadStatus = object.uploadStatus ?? undefined;
    message.uploadStatuses = object.uploadStatuses?.map((e) => e) || [];
    message.createdAfter = object.createdAfter ?? undefined;
    return message;
  },
};

function createBaseFileRequest(): FileRequest {
  return { query: undefined };
}

export const FileRequest: MessageFns<FileRequest> = {
  encode(message: FileRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.query !== undefined) {
      FileQuery.encode(message.query, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FileRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFileRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.query = FileQuery.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FileRequest {
    return { query: isSet(object.query) ? FileQuery.fromJSON(object.query) : undefined };
  },

  toJSON(message: FileRequest): unknown {
    const obj: any = {};
    if (message.query !== undefined) {
      obj.query = FileQuery.toJSON(message.query);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FileRequest>, I>>(base?: I): FileRequest {
    return FileRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FileRequest>, I>>(object: I): FileRequest {
    const message = createBaseFileRequest();
    message.query =
      object.query !== undefined && object.query !== null
        ? FileQuery.fromPartial(object.query)
        : undefined;
    return message;
  },
};

function createBaseFileGetListResponse(): FileGetListResponse {
  return { items: [], total: 0 };
}

export const FileGetListResponse: MessageFns<FileGetListResponse> = {
  encode(message: FileGetListResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.items) {
      File.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.total !== 0) {
      writer.uint32(16).int32(message.total);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FileGetListResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFileGetListResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.items.push(File.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.total = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FileGetListResponse {
    return {
      items: globalThis.Array.isArray(object?.items)
        ? object.items.map((e: any) => File.fromJSON(e))
        : [],
      total: isSet(object.total) ? globalThis.Number(object.total) : 0,
    };
  },

  toJSON(message: FileGetListResponse): unknown {
    const obj: any = {};
    if (message.items?.length) {
      obj.items = message.items.map((e) => File.toJSON(e));
    }
    if (message.total !== 0) {
      obj.total = Math.round(message.total);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FileGetListResponse>, I>>(base?: I): FileGetListResponse {
    return FileGetListResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FileGetListResponse>, I>>(
    object: I,
  ): FileGetListResponse {
    const message = createBaseFileGetListResponse();
    message.items = object.items?.map((e) => File.fromPartial(e)) || [];
    message.total = object.total ?? 0;
    return message;
  },
};

function createBaseFileCreate(): FileCreate {
  return { originalName: '', size: 0, mimeType: '' };
}

export const FileCreate: MessageFns<FileCreate> = {
  encode(message: FileCreate, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.originalName !== '') {
      writer.uint32(10).string(message.originalName);
    }
    if (message.size !== 0) {
      writer.uint32(16).int32(message.size);
    }
    if (message.mimeType !== '') {
      writer.uint32(26).string(message.mimeType);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FileCreate {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFileCreate();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.originalName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.size = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.mimeType = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FileCreate {
    return {
      originalName: isSet(object.originalName) ? globalThis.String(object.originalName) : '',
      size: isSet(object.size) ? globalThis.Number(object.size) : 0,
      mimeType: isSet(object.mimeType) ? globalThis.String(object.mimeType) : '',
    };
  },

  toJSON(message: FileCreate): unknown {
    const obj: any = {};
    if (message.originalName !== '') {
      obj.originalName = message.originalName;
    }
    if (message.size !== 0) {
      obj.size = Math.round(message.size);
    }
    if (message.mimeType !== '') {
      obj.mimeType = message.mimeType;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FileCreate>, I>>(base?: I): FileCreate {
    return FileCreate.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FileCreate>, I>>(object: I): FileCreate {
    const message = createBaseFileCreate();
    message.originalName = object.originalName ?? '';
    message.size = object.size ?? 0;
    message.mimeType = object.mimeType ?? '';
    return message;
  },
};

function createBaseFileCreateRequest(): FileCreateRequest {
  return { file: undefined, storage: undefined };
}

export const FileCreateRequest: MessageFns<FileCreateRequest> = {
  encode(message: FileCreateRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.file !== undefined) {
      FileCreate.encode(message.file, writer.uint32(10).fork()).join();
    }
    if (message.storage !== undefined) {
      StorageObjectMetadata.encode(message.storage, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FileCreateRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFileCreateRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.file = FileCreate.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.storage = StorageObjectMetadata.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FileCreateRequest {
    return {
      file: isSet(object.file) ? FileCreate.fromJSON(object.file) : undefined,
      storage: isSet(object.storage) ? StorageObjectMetadata.fromJSON(object.storage) : undefined,
    };
  },

  toJSON(message: FileCreateRequest): unknown {
    const obj: any = {};
    if (message.file !== undefined) {
      obj.file = FileCreate.toJSON(message.file);
    }
    if (message.storage !== undefined) {
      obj.storage = StorageObjectMetadata.toJSON(message.storage);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FileCreateRequest>, I>>(base?: I): FileCreateRequest {
    return FileCreateRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FileCreateRequest>, I>>(object: I): FileCreateRequest {
    const message = createBaseFileCreateRequest();
    message.file =
      object.file !== undefined && object.file !== null
        ? FileCreate.fromPartial(object.file)
        : undefined;
    message.storage =
      object.storage !== undefined && object.storage !== null
        ? StorageObjectMetadata.fromPartial(object.storage)
        : undefined;
    return message;
  },
};

function createBaseFileCreateManyItem(): FileCreateManyItem {
  return { file: undefined, uploadId: '' };
}

export const FileCreateManyItem: MessageFns<FileCreateManyItem> = {
  encode(message: FileCreateManyItem, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.file !== undefined) {
      FileCreate.encode(message.file, writer.uint32(10).fork()).join();
    }
    if (message.uploadId !== '') {
      writer.uint32(18).string(message.uploadId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FileCreateManyItem {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFileCreateManyItem();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.file = FileCreate.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.uploadId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FileCreateManyItem {
    return {
      file: isSet(object.file) ? FileCreate.fromJSON(object.file) : undefined,
      uploadId: isSet(object.uploadId) ? globalThis.String(object.uploadId) : '',
    };
  },

  toJSON(message: FileCreateManyItem): unknown {
    const obj: any = {};
    if (message.file !== undefined) {
      obj.file = FileCreate.toJSON(message.file);
    }
    if (message.uploadId !== '') {
      obj.uploadId = message.uploadId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FileCreateManyItem>, I>>(base?: I): FileCreateManyItem {
    return FileCreateManyItem.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FileCreateManyItem>, I>>(object: I): FileCreateManyItem {
    const message = createBaseFileCreateManyItem();
    message.file =
      object.file !== undefined && object.file !== null
        ? FileCreate.fromPartial(object.file)
        : undefined;
    message.uploadId = object.uploadId ?? '';
    return message;
  },
};

function createBaseFileCreateManyRequest(): FileCreateManyRequest {
  return { storage: undefined, items: [] };
}

export const FileCreateManyRequest: MessageFns<FileCreateManyRequest> = {
  encode(message: FileCreateManyRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.storage !== undefined) {
      StorageObjectManyMetadata.encode(message.storage, writer.uint32(10).fork()).join();
    }
    for (const v of message.items) {
      FileCreateManyItem.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FileCreateManyRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFileCreateManyRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.storage = StorageObjectManyMetadata.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.items.push(FileCreateManyItem.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FileCreateManyRequest {
    return {
      storage: isSet(object.storage)
        ? StorageObjectManyMetadata.fromJSON(object.storage)
        : undefined,
      items: globalThis.Array.isArray(object?.items)
        ? object.items.map((e: any) => FileCreateManyItem.fromJSON(e))
        : [],
    };
  },

  toJSON(message: FileCreateManyRequest): unknown {
    const obj: any = {};
    if (message.storage !== undefined) {
      obj.storage = StorageObjectManyMetadata.toJSON(message.storage);
    }
    if (message.items?.length) {
      obj.items = message.items.map((e) => FileCreateManyItem.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FileCreateManyRequest>, I>>(base?: I): FileCreateManyRequest {
    return FileCreateManyRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FileCreateManyRequest>, I>>(
    object: I,
  ): FileCreateManyRequest {
    const message = createBaseFileCreateManyRequest();
    message.storage =
      object.storage !== undefined && object.storage !== null
        ? StorageObjectManyMetadata.fromPartial(object.storage)
        : undefined;
    message.items = object.items?.map((e) => FileCreateManyItem.fromPartial(e)) || [];
    return message;
  },
};

function createBaseFileCreateManyResponse(): FileCreateManyResponse {
  return { items: [] };
}

export const FileCreateManyResponse: MessageFns<FileCreateManyResponse> = {
  encode(message: FileCreateManyResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.items) {
      File.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FileCreateManyResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFileCreateManyResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.items.push(File.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FileCreateManyResponse {
    return {
      items: globalThis.Array.isArray(object?.items)
        ? object.items.map((e: any) => File.fromJSON(e))
        : [],
    };
  },

  toJSON(message: FileCreateManyResponse): unknown {
    const obj: any = {};
    if (message.items?.length) {
      obj.items = message.items.map((e) => File.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FileCreateManyResponse>, I>>(
    base?: I,
  ): FileCreateManyResponse {
    return FileCreateManyResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FileCreateManyResponse>, I>>(
    object: I,
  ): FileCreateManyResponse {
    const message = createBaseFileCreateManyResponse();
    message.items = object.items?.map((e) => File.fromPartial(e)) || [];
    return message;
  },
};

function createBaseFileUploadResponse(): FileUploadResponse {
  return { entity: undefined, canSendChunks: undefined, ack: undefined };
}

export const FileUploadResponse: MessageFns<FileUploadResponse> = {
  encode(message: FileUploadResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.entity !== undefined) {
      File.encode(message.entity, writer.uint32(10).fork()).join();
    }
    if (message.canSendChunks !== undefined) {
      writer.uint32(16).bool(message.canSendChunks);
    }
    if (message.ack !== undefined) {
      writer.uint32(24).bool(message.ack);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FileUploadResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFileUploadResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.entity = File.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.canSendChunks = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.ack = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FileUploadResponse {
    return {
      entity: isSet(object.entity) ? File.fromJSON(object.entity) : undefined,
      canSendChunks: isSet(object.canSendChunks)
        ? globalThis.Boolean(object.canSendChunks)
        : undefined,
      ack: isSet(object.ack) ? globalThis.Boolean(object.ack) : undefined,
    };
  },

  toJSON(message: FileUploadResponse): unknown {
    const obj: any = {};
    if (message.entity !== undefined) {
      obj.entity = File.toJSON(message.entity);
    }
    if (message.canSendChunks !== undefined) {
      obj.canSendChunks = message.canSendChunks;
    }
    if (message.ack !== undefined) {
      obj.ack = message.ack;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FileUploadResponse>, I>>(base?: I): FileUploadResponse {
    return FileUploadResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FileUploadResponse>, I>>(object: I): FileUploadResponse {
    const message = createBaseFileUploadResponse();
    message.entity =
      object.entity !== undefined && object.entity !== null
        ? File.fromPartial(object.entity)
        : undefined;
    message.canSendChunks = object.canSendChunks ?? undefined;
    message.ack = object.ack ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === 'string') {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
