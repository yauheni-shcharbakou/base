// @ts-nocheck
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               v7.34.0
// source: storage/messages/video.messages.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../../google/protobuf/timestamp";
import { File } from "../models/file";
import { Video } from "../models/video";
import { FileCreate } from "./file.messages";
import { StorageObjectManyMetadata, StorageObjectMetadata } from "./storage-object.messages";

export interface VideoPopulated {
  id: string;
  createdAt: Date;
  updatedAt?: Date;
  title: string;
  duration: number;
  views: number;
  description?: string;
  providerId: string;
  userId: string;
  fileId: string;
  file: File;
  uploadId: string;
}

export interface VideoQuery {
  id?: string;
  ids: string[];
  file?: string;
  providerId?: string;
  providerIds: string[];
  userId?: string;
}

export interface VideoGetListResponse {
  items: VideoPopulated[];
  total: number;
}

export interface VideoCreate {
  title: string;
  description?: string;
}

export interface VideoCreateRequest {
  video: VideoCreate;
  file: FileCreate;
  storage?: StorageObjectMetadata;
}

export interface VideoCreateManyItem {
  video: VideoCreate;
  file: FileCreate;
  uploadId: string;
}

export interface VideoCreateManyRequest {
  storage?: StorageObjectManyMetadata;
  items: VideoCreateManyItem[];
}

export interface VideoCreateManyResponse {
  items: Video[];
}

export interface VideoUploadResponse {
  entity?: Video;
  canSendChunks?: boolean;
  ack?: boolean;
}

export interface VideoUpdateSet {
  title?: string;
  description?: string;
}

export interface VideoUpdate {
  set?: VideoUpdateSet;
}

export interface VideoUpdateRequest {
  query: VideoQuery;
  update: VideoUpdate;
}

export interface VideoUpdateByIdRequest {
  id: string;
  update: VideoUpdate;
}

function createBaseVideoPopulated(): VideoPopulated {
  return {
    id: "",
    createdAt: undefined,
    updatedAt: undefined,
    title: "",
    duration: 0,
    views: 0,
    description: undefined,
    providerId: "",
    userId: "",
    fileId: "",
    file: undefined,
    uploadId: "",
  };
}

export const VideoPopulated: MessageFns<VideoPopulated> = {
  encode(message: VideoPopulated, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(18).fork()).join();
    }
    if (message.updatedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.updatedAt), writer.uint32(26).fork()).join();
    }
    if (message.title !== "") {
      writer.uint32(34).string(message.title);
    }
    if (message.duration !== 0) {
      writer.uint32(40).int32(message.duration);
    }
    if (message.views !== 0) {
      writer.uint32(48).int32(message.views);
    }
    if (message.description !== undefined) {
      writer.uint32(58).string(message.description);
    }
    if (message.providerId !== "") {
      writer.uint32(66).string(message.providerId);
    }
    if (message.userId !== "") {
      writer.uint32(74).string(message.userId);
    }
    if (message.fileId !== "") {
      writer.uint32(82).string(message.fileId);
    }
    if (message.file !== undefined) {
      File.encode(message.file, writer.uint32(90).fork()).join();
    }
    if (message.uploadId !== "") {
      writer.uint32(98).string(message.uploadId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VideoPopulated {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVideoPopulated();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.updatedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.duration = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.views = reader.int32();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.providerId = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.fileId = reader.string();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.file = File.decode(reader, reader.uint32());
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.uploadId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VideoPopulated {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      createdAt: isSet(object.createdAt) ? fromJsonTimestamp(object.createdAt) : undefined,
      updatedAt: isSet(object.updatedAt) ? fromJsonTimestamp(object.updatedAt) : undefined,
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      duration: isSet(object.duration) ? globalThis.Number(object.duration) : 0,
      views: isSet(object.views) ? globalThis.Number(object.views) : 0,
      description: isSet(object.description) ? globalThis.String(object.description) : undefined,
      providerId: isSet(object.providerId) ? globalThis.String(object.providerId) : "",
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "",
      fileId: isSet(object.fileId) ? globalThis.String(object.fileId) : "",
      file: isSet(object.file) ? File.fromJSON(object.file) : undefined,
      uploadId: isSet(object.uploadId) ? globalThis.String(object.uploadId) : "",
    };
  },

  toJSON(message: VideoPopulated): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.updatedAt !== undefined) {
      obj.updatedAt = message.updatedAt.toISOString();
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.duration !== 0) {
      obj.duration = Math.round(message.duration);
    }
    if (message.views !== 0) {
      obj.views = Math.round(message.views);
    }
    if (message.description !== undefined) {
      obj.description = message.description;
    }
    if (message.providerId !== "") {
      obj.providerId = message.providerId;
    }
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.fileId !== "") {
      obj.fileId = message.fileId;
    }
    if (message.file !== undefined) {
      obj.file = File.toJSON(message.file);
    }
    if (message.uploadId !== "") {
      obj.uploadId = message.uploadId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VideoPopulated>, I>>(base?: I): VideoPopulated {
    return VideoPopulated.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VideoPopulated>, I>>(object: I): VideoPopulated {
    const message = createBaseVideoPopulated();
    message.id = object.id ?? "";
    message.createdAt = object.createdAt ?? undefined;
    message.updatedAt = object.updatedAt ?? undefined;
    message.title = object.title ?? "";
    message.duration = object.duration ?? 0;
    message.views = object.views ?? 0;
    message.description = object.description ?? undefined;
    message.providerId = object.providerId ?? "";
    message.userId = object.userId ?? "";
    message.fileId = object.fileId ?? "";
    message.file = (object.file !== undefined && object.file !== null) ? File.fromPartial(object.file) : undefined;
    message.uploadId = object.uploadId ?? "";
    return message;
  },
};

function createBaseVideoQuery(): VideoQuery {
  return { id: undefined, ids: [], file: undefined, providerId: undefined, providerIds: [], userId: undefined };
}

export const VideoQuery: MessageFns<VideoQuery> = {
  encode(message: VideoQuery, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      writer.uint32(10).string(message.id);
    }
    for (const v of message.ids) {
      writer.uint32(18).string(v!);
    }
    if (message.file !== undefined) {
      writer.uint32(26).string(message.file);
    }
    if (message.providerId !== undefined) {
      writer.uint32(34).string(message.providerId);
    }
    for (const v of message.providerIds) {
      writer.uint32(42).string(v!);
    }
    if (message.userId !== undefined) {
      writer.uint32(50).string(message.userId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VideoQuery {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVideoQuery();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.ids.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.file = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.providerId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.providerIds.push(reader.string());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VideoQuery {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : undefined,
      ids: globalThis.Array.isArray(object?.ids) ? object.ids.map((e: any) => globalThis.String(e)) : [],
      file: isSet(object.file) ? globalThis.String(object.file) : undefined,
      providerId: isSet(object.providerId) ? globalThis.String(object.providerId) : undefined,
      providerIds: globalThis.Array.isArray(object?.providerIds)
        ? object.providerIds.map((e: any) => globalThis.String(e))
        : [],
      userId: isSet(object.userId) ? globalThis.String(object.userId) : undefined,
    };
  },

  toJSON(message: VideoQuery): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = message.id;
    }
    if (message.ids?.length) {
      obj.ids = message.ids;
    }
    if (message.file !== undefined) {
      obj.file = message.file;
    }
    if (message.providerId !== undefined) {
      obj.providerId = message.providerId;
    }
    if (message.providerIds?.length) {
      obj.providerIds = message.providerIds;
    }
    if (message.userId !== undefined) {
      obj.userId = message.userId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VideoQuery>, I>>(base?: I): VideoQuery {
    return VideoQuery.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VideoQuery>, I>>(object: I): VideoQuery {
    const message = createBaseVideoQuery();
    message.id = object.id ?? undefined;
    message.ids = object.ids?.map((e) => e) || [];
    message.file = object.file ?? undefined;
    message.providerId = object.providerId ?? undefined;
    message.providerIds = object.providerIds?.map((e) => e) || [];
    message.userId = object.userId ?? undefined;
    return message;
  },
};

function createBaseVideoGetListResponse(): VideoGetListResponse {
  return { items: [], total: 0 };
}

export const VideoGetListResponse: MessageFns<VideoGetListResponse> = {
  encode(message: VideoGetListResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.items) {
      VideoPopulated.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.total !== 0) {
      writer.uint32(16).int32(message.total);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VideoGetListResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVideoGetListResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.items.push(VideoPopulated.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.total = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VideoGetListResponse {
    return {
      items: globalThis.Array.isArray(object?.items) ? object.items.map((e: any) => VideoPopulated.fromJSON(e)) : [],
      total: isSet(object.total) ? globalThis.Number(object.total) : 0,
    };
  },

  toJSON(message: VideoGetListResponse): unknown {
    const obj: any = {};
    if (message.items?.length) {
      obj.items = message.items.map((e) => VideoPopulated.toJSON(e));
    }
    if (message.total !== 0) {
      obj.total = Math.round(message.total);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VideoGetListResponse>, I>>(base?: I): VideoGetListResponse {
    return VideoGetListResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VideoGetListResponse>, I>>(object: I): VideoGetListResponse {
    const message = createBaseVideoGetListResponse();
    message.items = object.items?.map((e) => VideoPopulated.fromPartial(e)) || [];
    message.total = object.total ?? 0;
    return message;
  },
};

function createBaseVideoCreate(): VideoCreate {
  return { title: "", description: undefined };
}

export const VideoCreate: MessageFns<VideoCreate> = {
  encode(message: VideoCreate, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.title !== "") {
      writer.uint32(10).string(message.title);
    }
    if (message.description !== undefined) {
      writer.uint32(18).string(message.description);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VideoCreate {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVideoCreate();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.description = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VideoCreate {
    return {
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : undefined,
    };
  },

  toJSON(message: VideoCreate): unknown {
    const obj: any = {};
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.description !== undefined) {
      obj.description = message.description;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VideoCreate>, I>>(base?: I): VideoCreate {
    return VideoCreate.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VideoCreate>, I>>(object: I): VideoCreate {
    const message = createBaseVideoCreate();
    message.title = object.title ?? "";
    message.description = object.description ?? undefined;
    return message;
  },
};

function createBaseVideoCreateRequest(): VideoCreateRequest {
  return { video: undefined, file: undefined, storage: undefined };
}

export const VideoCreateRequest: MessageFns<VideoCreateRequest> = {
  encode(message: VideoCreateRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.video !== undefined) {
      VideoCreate.encode(message.video, writer.uint32(10).fork()).join();
    }
    if (message.file !== undefined) {
      FileCreate.encode(message.file, writer.uint32(18).fork()).join();
    }
    if (message.storage !== undefined) {
      StorageObjectMetadata.encode(message.storage, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VideoCreateRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVideoCreateRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.video = VideoCreate.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.file = FileCreate.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.storage = StorageObjectMetadata.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VideoCreateRequest {
    return {
      video: isSet(object.video) ? VideoCreate.fromJSON(object.video) : undefined,
      file: isSet(object.file) ? FileCreate.fromJSON(object.file) : undefined,
      storage: isSet(object.storage) ? StorageObjectMetadata.fromJSON(object.storage) : undefined,
    };
  },

  toJSON(message: VideoCreateRequest): unknown {
    const obj: any = {};
    if (message.video !== undefined) {
      obj.video = VideoCreate.toJSON(message.video);
    }
    if (message.file !== undefined) {
      obj.file = FileCreate.toJSON(message.file);
    }
    if (message.storage !== undefined) {
      obj.storage = StorageObjectMetadata.toJSON(message.storage);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VideoCreateRequest>, I>>(base?: I): VideoCreateRequest {
    return VideoCreateRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VideoCreateRequest>, I>>(object: I): VideoCreateRequest {
    const message = createBaseVideoCreateRequest();
    message.video = (object.video !== undefined && object.video !== null)
      ? VideoCreate.fromPartial(object.video)
      : undefined;
    message.file = (object.file !== undefined && object.file !== null)
      ? FileCreate.fromPartial(object.file)
      : undefined;
    message.storage = (object.storage !== undefined && object.storage !== null)
      ? StorageObjectMetadata.fromPartial(object.storage)
      : undefined;
    return message;
  },
};

function createBaseVideoCreateManyItem(): VideoCreateManyItem {
  return { video: undefined, file: undefined, uploadId: "" };
}

export const VideoCreateManyItem: MessageFns<VideoCreateManyItem> = {
  encode(message: VideoCreateManyItem, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.video !== undefined) {
      VideoCreate.encode(message.video, writer.uint32(10).fork()).join();
    }
    if (message.file !== undefined) {
      FileCreate.encode(message.file, writer.uint32(18).fork()).join();
    }
    if (message.uploadId !== "") {
      writer.uint32(26).string(message.uploadId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VideoCreateManyItem {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVideoCreateManyItem();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.video = VideoCreate.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.file = FileCreate.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.uploadId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VideoCreateManyItem {
    return {
      video: isSet(object.video) ? VideoCreate.fromJSON(object.video) : undefined,
      file: isSet(object.file) ? FileCreate.fromJSON(object.file) : undefined,
      uploadId: isSet(object.uploadId) ? globalThis.String(object.uploadId) : "",
    };
  },

  toJSON(message: VideoCreateManyItem): unknown {
    const obj: any = {};
    if (message.video !== undefined) {
      obj.video = VideoCreate.toJSON(message.video);
    }
    if (message.file !== undefined) {
      obj.file = FileCreate.toJSON(message.file);
    }
    if (message.uploadId !== "") {
      obj.uploadId = message.uploadId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VideoCreateManyItem>, I>>(base?: I): VideoCreateManyItem {
    return VideoCreateManyItem.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VideoCreateManyItem>, I>>(object: I): VideoCreateManyItem {
    const message = createBaseVideoCreateManyItem();
    message.video = (object.video !== undefined && object.video !== null)
      ? VideoCreate.fromPartial(object.video)
      : undefined;
    message.file = (object.file !== undefined && object.file !== null)
      ? FileCreate.fromPartial(object.file)
      : undefined;
    message.uploadId = object.uploadId ?? "";
    return message;
  },
};

function createBaseVideoCreateManyRequest(): VideoCreateManyRequest {
  return { storage: undefined, items: [] };
}

export const VideoCreateManyRequest: MessageFns<VideoCreateManyRequest> = {
  encode(message: VideoCreateManyRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.storage !== undefined) {
      StorageObjectManyMetadata.encode(message.storage, writer.uint32(10).fork()).join();
    }
    for (const v of message.items) {
      VideoCreateManyItem.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VideoCreateManyRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVideoCreateManyRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.storage = StorageObjectManyMetadata.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.items.push(VideoCreateManyItem.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VideoCreateManyRequest {
    return {
      storage: isSet(object.storage) ? StorageObjectManyMetadata.fromJSON(object.storage) : undefined,
      items: globalThis.Array.isArray(object?.items)
        ? object.items.map((e: any) => VideoCreateManyItem.fromJSON(e))
        : [],
    };
  },

  toJSON(message: VideoCreateManyRequest): unknown {
    const obj: any = {};
    if (message.storage !== undefined) {
      obj.storage = StorageObjectManyMetadata.toJSON(message.storage);
    }
    if (message.items?.length) {
      obj.items = message.items.map((e) => VideoCreateManyItem.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VideoCreateManyRequest>, I>>(base?: I): VideoCreateManyRequest {
    return VideoCreateManyRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VideoCreateManyRequest>, I>>(object: I): VideoCreateManyRequest {
    const message = createBaseVideoCreateManyRequest();
    message.storage = (object.storage !== undefined && object.storage !== null)
      ? StorageObjectManyMetadata.fromPartial(object.storage)
      : undefined;
    message.items = object.items?.map((e) => VideoCreateManyItem.fromPartial(e)) || [];
    return message;
  },
};

function createBaseVideoCreateManyResponse(): VideoCreateManyResponse {
  return { items: [] };
}

export const VideoCreateManyResponse: MessageFns<VideoCreateManyResponse> = {
  encode(message: VideoCreateManyResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.items) {
      Video.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VideoCreateManyResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVideoCreateManyResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.items.push(Video.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VideoCreateManyResponse {
    return { items: globalThis.Array.isArray(object?.items) ? object.items.map((e: any) => Video.fromJSON(e)) : [] };
  },

  toJSON(message: VideoCreateManyResponse): unknown {
    const obj: any = {};
    if (message.items?.length) {
      obj.items = message.items.map((e) => Video.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VideoCreateManyResponse>, I>>(base?: I): VideoCreateManyResponse {
    return VideoCreateManyResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VideoCreateManyResponse>, I>>(object: I): VideoCreateManyResponse {
    const message = createBaseVideoCreateManyResponse();
    message.items = object.items?.map((e) => Video.fromPartial(e)) || [];
    return message;
  },
};

function createBaseVideoUploadResponse(): VideoUploadResponse {
  return { entity: undefined, canSendChunks: undefined, ack: undefined };
}

export const VideoUploadResponse: MessageFns<VideoUploadResponse> = {
  encode(message: VideoUploadResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.entity !== undefined) {
      Video.encode(message.entity, writer.uint32(10).fork()).join();
    }
    if (message.canSendChunks !== undefined) {
      writer.uint32(16).bool(message.canSendChunks);
    }
    if (message.ack !== undefined) {
      writer.uint32(24).bool(message.ack);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VideoUploadResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVideoUploadResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.entity = Video.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.canSendChunks = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.ack = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VideoUploadResponse {
    return {
      entity: isSet(object.entity) ? Video.fromJSON(object.entity) : undefined,
      canSendChunks: isSet(object.canSendChunks) ? globalThis.Boolean(object.canSendChunks) : undefined,
      ack: isSet(object.ack) ? globalThis.Boolean(object.ack) : undefined,
    };
  },

  toJSON(message: VideoUploadResponse): unknown {
    const obj: any = {};
    if (message.entity !== undefined) {
      obj.entity = Video.toJSON(message.entity);
    }
    if (message.canSendChunks !== undefined) {
      obj.canSendChunks = message.canSendChunks;
    }
    if (message.ack !== undefined) {
      obj.ack = message.ack;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VideoUploadResponse>, I>>(base?: I): VideoUploadResponse {
    return VideoUploadResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VideoUploadResponse>, I>>(object: I): VideoUploadResponse {
    const message = createBaseVideoUploadResponse();
    message.entity = (object.entity !== undefined && object.entity !== null)
      ? Video.fromPartial(object.entity)
      : undefined;
    message.canSendChunks = object.canSendChunks ?? undefined;
    message.ack = object.ack ?? undefined;
    return message;
  },
};

function createBaseVideoUpdateSet(): VideoUpdateSet {
  return { title: undefined, description: undefined };
}

export const VideoUpdateSet: MessageFns<VideoUpdateSet> = {
  encode(message: VideoUpdateSet, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.title !== undefined) {
      writer.uint32(10).string(message.title);
    }
    if (message.description !== undefined) {
      writer.uint32(18).string(message.description);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VideoUpdateSet {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVideoUpdateSet();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.description = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VideoUpdateSet {
    return {
      title: isSet(object.title) ? globalThis.String(object.title) : undefined,
      description: isSet(object.description) ? globalThis.String(object.description) : undefined,
    };
  },

  toJSON(message: VideoUpdateSet): unknown {
    const obj: any = {};
    if (message.title !== undefined) {
      obj.title = message.title;
    }
    if (message.description !== undefined) {
      obj.description = message.description;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VideoUpdateSet>, I>>(base?: I): VideoUpdateSet {
    return VideoUpdateSet.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VideoUpdateSet>, I>>(object: I): VideoUpdateSet {
    const message = createBaseVideoUpdateSet();
    message.title = object.title ?? undefined;
    message.description = object.description ?? undefined;
    return message;
  },
};

function createBaseVideoUpdate(): VideoUpdate {
  return { set: undefined };
}

export const VideoUpdate: MessageFns<VideoUpdate> = {
  encode(message: VideoUpdate, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.set !== undefined) {
      VideoUpdateSet.encode(message.set, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VideoUpdate {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVideoUpdate();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.set = VideoUpdateSet.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VideoUpdate {
    return { set: isSet(object.set) ? VideoUpdateSet.fromJSON(object.set) : undefined };
  },

  toJSON(message: VideoUpdate): unknown {
    const obj: any = {};
    if (message.set !== undefined) {
      obj.set = VideoUpdateSet.toJSON(message.set);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VideoUpdate>, I>>(base?: I): VideoUpdate {
    return VideoUpdate.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VideoUpdate>, I>>(object: I): VideoUpdate {
    const message = createBaseVideoUpdate();
    message.set = (object.set !== undefined && object.set !== null)
      ? VideoUpdateSet.fromPartial(object.set)
      : undefined;
    return message;
  },
};

function createBaseVideoUpdateRequest(): VideoUpdateRequest {
  return { query: undefined, update: undefined };
}

export const VideoUpdateRequest: MessageFns<VideoUpdateRequest> = {
  encode(message: VideoUpdateRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.query !== undefined) {
      VideoQuery.encode(message.query, writer.uint32(10).fork()).join();
    }
    if (message.update !== undefined) {
      VideoUpdate.encode(message.update, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VideoUpdateRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVideoUpdateRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.query = VideoQuery.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.update = VideoUpdate.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VideoUpdateRequest {
    return {
      query: isSet(object.query) ? VideoQuery.fromJSON(object.query) : undefined,
      update: isSet(object.update) ? VideoUpdate.fromJSON(object.update) : undefined,
    };
  },

  toJSON(message: VideoUpdateRequest): unknown {
    const obj: any = {};
    if (message.query !== undefined) {
      obj.query = VideoQuery.toJSON(message.query);
    }
    if (message.update !== undefined) {
      obj.update = VideoUpdate.toJSON(message.update);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VideoUpdateRequest>, I>>(base?: I): VideoUpdateRequest {
    return VideoUpdateRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VideoUpdateRequest>, I>>(object: I): VideoUpdateRequest {
    const message = createBaseVideoUpdateRequest();
    message.query = (object.query !== undefined && object.query !== null)
      ? VideoQuery.fromPartial(object.query)
      : undefined;
    message.update = (object.update !== undefined && object.update !== null)
      ? VideoUpdate.fromPartial(object.update)
      : undefined;
    return message;
  },
};

function createBaseVideoUpdateByIdRequest(): VideoUpdateByIdRequest {
  return { id: "", update: undefined };
}

export const VideoUpdateByIdRequest: MessageFns<VideoUpdateByIdRequest> = {
  encode(message: VideoUpdateByIdRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.update !== undefined) {
      VideoUpdate.encode(message.update, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VideoUpdateByIdRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVideoUpdateByIdRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.update = VideoUpdate.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VideoUpdateByIdRequest {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      update: isSet(object.update) ? VideoUpdate.fromJSON(object.update) : undefined,
    };
  },

  toJSON(message: VideoUpdateByIdRequest): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.update !== undefined) {
      obj.update = VideoUpdate.toJSON(message.update);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VideoUpdateByIdRequest>, I>>(base?: I): VideoUpdateByIdRequest {
    return VideoUpdateByIdRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VideoUpdateByIdRequest>, I>>(object: I): VideoUpdateByIdRequest {
    const message = createBaseVideoUpdateByIdRequest();
    message.id = object.id ?? "";
    message.update = (object.update !== undefined && object.update !== null)
      ? VideoUpdate.fromPartial(object.update)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
