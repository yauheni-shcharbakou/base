// @ts-nocheck
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               v7.34.0
// source: storage/messages/image.messages.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from '@bufbuild/protobuf/wire';
import { Timestamp } from '../../google/protobuf/timestamp';
import { File } from '../models/file';
import { Image } from '../models/image';
import { FileCreate } from './file.messages';
import { StorageObjectManyMetadata, StorageObjectMetadata } from './storage-object.messages';

export interface ImagePopulated {
  id: string;
  createdAt: Date;
  updatedAt?: Date;
  width: number;
  height: number;
  alt: string;
  userId: string;
  fileId: string;
  file: File;
  uploadId: string;
}

export interface ImageQuery {
  id?: string;
  ids: string[];
  file?: string;
  userId?: string;
}

export interface ImageGetListResponse {
  items: ImagePopulated[];
  total: number;
}

export interface ImageCreate {
  width: number;
  height: number;
  alt: string;
}

export interface ImageCreateRequest {
  image: ImageCreate;
  file: FileCreate;
  storage?: StorageObjectMetadata;
}

export interface ImageCreateManyItem {
  image: ImageCreate;
  file: FileCreate;
  uploadId: string;
}

export interface ImageCreateManyRequest {
  storage?: StorageObjectManyMetadata;
  items: ImageCreateManyItem[];
}

export interface ImageCreateManyResponse {
  items: Image[];
}

export interface ImageUpdateSet {
  alt?: string;
}

export interface ImageUpdate {
  set?: ImageUpdateSet;
}

export interface ImageUpdateRequest {
  query: ImageQuery;
  update: ImageUpdate;
}

export interface ImageUpdateByIdRequest {
  id: string;
  update: ImageUpdate;
}

function createBaseImagePopulated(): ImagePopulated {
  return {
    id: '',
    createdAt: undefined,
    updatedAt: undefined,
    width: 0,
    height: 0,
    alt: '',
    userId: '',
    fileId: '',
    file: undefined,
    uploadId: '',
  };
}

export const ImagePopulated: MessageFns<ImagePopulated> = {
  encode(message: ImagePopulated, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== '') {
      writer.uint32(10).string(message.id);
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(18).fork()).join();
    }
    if (message.updatedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.updatedAt), writer.uint32(26).fork()).join();
    }
    if (message.width !== 0) {
      writer.uint32(32).int32(message.width);
    }
    if (message.height !== 0) {
      writer.uint32(40).int32(message.height);
    }
    if (message.alt !== '') {
      writer.uint32(50).string(message.alt);
    }
    if (message.userId !== '') {
      writer.uint32(58).string(message.userId);
    }
    if (message.fileId !== '') {
      writer.uint32(66).string(message.fileId);
    }
    if (message.file !== undefined) {
      File.encode(message.file, writer.uint32(74).fork()).join();
    }
    if (message.uploadId !== '') {
      writer.uint32(82).string(message.uploadId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImagePopulated {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImagePopulated();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.updatedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.width = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.height = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.alt = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.fileId = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.file = File.decode(reader, reader.uint32());
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.uploadId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ImagePopulated {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : '',
      createdAt: isSet(object.createdAt) ? fromJsonTimestamp(object.createdAt) : undefined,
      updatedAt: isSet(object.updatedAt) ? fromJsonTimestamp(object.updatedAt) : undefined,
      width: isSet(object.width) ? globalThis.Number(object.width) : 0,
      height: isSet(object.height) ? globalThis.Number(object.height) : 0,
      alt: isSet(object.alt) ? globalThis.String(object.alt) : '',
      userId: isSet(object.userId) ? globalThis.String(object.userId) : '',
      fileId: isSet(object.fileId) ? globalThis.String(object.fileId) : '',
      file: isSet(object.file) ? File.fromJSON(object.file) : undefined,
      uploadId: isSet(object.uploadId) ? globalThis.String(object.uploadId) : '',
    };
  },

  toJSON(message: ImagePopulated): unknown {
    const obj: any = {};
    if (message.id !== '') {
      obj.id = message.id;
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.updatedAt !== undefined) {
      obj.updatedAt = message.updatedAt.toISOString();
    }
    if (message.width !== 0) {
      obj.width = Math.round(message.width);
    }
    if (message.height !== 0) {
      obj.height = Math.round(message.height);
    }
    if (message.alt !== '') {
      obj.alt = message.alt;
    }
    if (message.userId !== '') {
      obj.userId = message.userId;
    }
    if (message.fileId !== '') {
      obj.fileId = message.fileId;
    }
    if (message.file !== undefined) {
      obj.file = File.toJSON(message.file);
    }
    if (message.uploadId !== '') {
      obj.uploadId = message.uploadId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ImagePopulated>, I>>(base?: I): ImagePopulated {
    return ImagePopulated.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ImagePopulated>, I>>(object: I): ImagePopulated {
    const message = createBaseImagePopulated();
    message.id = object.id ?? '';
    message.createdAt = object.createdAt ?? undefined;
    message.updatedAt = object.updatedAt ?? undefined;
    message.width = object.width ?? 0;
    message.height = object.height ?? 0;
    message.alt = object.alt ?? '';
    message.userId = object.userId ?? '';
    message.fileId = object.fileId ?? '';
    message.file =
      object.file !== undefined && object.file !== null ? File.fromPartial(object.file) : undefined;
    message.uploadId = object.uploadId ?? '';
    return message;
  },
};

function createBaseImageQuery(): ImageQuery {
  return { id: undefined, ids: [], file: undefined, userId: undefined };
}

export const ImageQuery: MessageFns<ImageQuery> = {
  encode(message: ImageQuery, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      writer.uint32(10).string(message.id);
    }
    for (const v of message.ids) {
      writer.uint32(18).string(v!);
    }
    if (message.file !== undefined) {
      writer.uint32(26).string(message.file);
    }
    if (message.userId !== undefined) {
      writer.uint32(34).string(message.userId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImageQuery {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImageQuery();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.ids.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.file = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ImageQuery {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : undefined,
      ids: globalThis.Array.isArray(object?.ids)
        ? object.ids.map((e: any) => globalThis.String(e))
        : [],
      file: isSet(object.file) ? globalThis.String(object.file) : undefined,
      userId: isSet(object.userId) ? globalThis.String(object.userId) : undefined,
    };
  },

  toJSON(message: ImageQuery): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = message.id;
    }
    if (message.ids?.length) {
      obj.ids = message.ids;
    }
    if (message.file !== undefined) {
      obj.file = message.file;
    }
    if (message.userId !== undefined) {
      obj.userId = message.userId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ImageQuery>, I>>(base?: I): ImageQuery {
    return ImageQuery.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ImageQuery>, I>>(object: I): ImageQuery {
    const message = createBaseImageQuery();
    message.id = object.id ?? undefined;
    message.ids = object.ids?.map((e) => e) || [];
    message.file = object.file ?? undefined;
    message.userId = object.userId ?? undefined;
    return message;
  },
};

function createBaseImageGetListResponse(): ImageGetListResponse {
  return { items: [], total: 0 };
}

export const ImageGetListResponse: MessageFns<ImageGetListResponse> = {
  encode(message: ImageGetListResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.items) {
      ImagePopulated.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.total !== 0) {
      writer.uint32(16).int32(message.total);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImageGetListResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImageGetListResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.items.push(ImagePopulated.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.total = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ImageGetListResponse {
    return {
      items: globalThis.Array.isArray(object?.items)
        ? object.items.map((e: any) => ImagePopulated.fromJSON(e))
        : [],
      total: isSet(object.total) ? globalThis.Number(object.total) : 0,
    };
  },

  toJSON(message: ImageGetListResponse): unknown {
    const obj: any = {};
    if (message.items?.length) {
      obj.items = message.items.map((e) => ImagePopulated.toJSON(e));
    }
    if (message.total !== 0) {
      obj.total = Math.round(message.total);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ImageGetListResponse>, I>>(base?: I): ImageGetListResponse {
    return ImageGetListResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ImageGetListResponse>, I>>(
    object: I,
  ): ImageGetListResponse {
    const message = createBaseImageGetListResponse();
    message.items = object.items?.map((e) => ImagePopulated.fromPartial(e)) || [];
    message.total = object.total ?? 0;
    return message;
  },
};

function createBaseImageCreate(): ImageCreate {
  return { width: 0, height: 0, alt: '' };
}

export const ImageCreate: MessageFns<ImageCreate> = {
  encode(message: ImageCreate, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.width !== 0) {
      writer.uint32(8).int32(message.width);
    }
    if (message.height !== 0) {
      writer.uint32(16).int32(message.height);
    }
    if (message.alt !== '') {
      writer.uint32(26).string(message.alt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImageCreate {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImageCreate();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.width = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.height = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.alt = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ImageCreate {
    return {
      width: isSet(object.width) ? globalThis.Number(object.width) : 0,
      height: isSet(object.height) ? globalThis.Number(object.height) : 0,
      alt: isSet(object.alt) ? globalThis.String(object.alt) : '',
    };
  },

  toJSON(message: ImageCreate): unknown {
    const obj: any = {};
    if (message.width !== 0) {
      obj.width = Math.round(message.width);
    }
    if (message.height !== 0) {
      obj.height = Math.round(message.height);
    }
    if (message.alt !== '') {
      obj.alt = message.alt;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ImageCreate>, I>>(base?: I): ImageCreate {
    return ImageCreate.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ImageCreate>, I>>(object: I): ImageCreate {
    const message = createBaseImageCreate();
    message.width = object.width ?? 0;
    message.height = object.height ?? 0;
    message.alt = object.alt ?? '';
    return message;
  },
};

function createBaseImageCreateRequest(): ImageCreateRequest {
  return { image: undefined, file: undefined, storage: undefined };
}

export const ImageCreateRequest: MessageFns<ImageCreateRequest> = {
  encode(message: ImageCreateRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.image !== undefined) {
      ImageCreate.encode(message.image, writer.uint32(10).fork()).join();
    }
    if (message.file !== undefined) {
      FileCreate.encode(message.file, writer.uint32(18).fork()).join();
    }
    if (message.storage !== undefined) {
      StorageObjectMetadata.encode(message.storage, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImageCreateRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImageCreateRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.image = ImageCreate.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.file = FileCreate.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.storage = StorageObjectMetadata.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ImageCreateRequest {
    return {
      image: isSet(object.image) ? ImageCreate.fromJSON(object.image) : undefined,
      file: isSet(object.file) ? FileCreate.fromJSON(object.file) : undefined,
      storage: isSet(object.storage) ? StorageObjectMetadata.fromJSON(object.storage) : undefined,
    };
  },

  toJSON(message: ImageCreateRequest): unknown {
    const obj: any = {};
    if (message.image !== undefined) {
      obj.image = ImageCreate.toJSON(message.image);
    }
    if (message.file !== undefined) {
      obj.file = FileCreate.toJSON(message.file);
    }
    if (message.storage !== undefined) {
      obj.storage = StorageObjectMetadata.toJSON(message.storage);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ImageCreateRequest>, I>>(base?: I): ImageCreateRequest {
    return ImageCreateRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ImageCreateRequest>, I>>(object: I): ImageCreateRequest {
    const message = createBaseImageCreateRequest();
    message.image =
      object.image !== undefined && object.image !== null
        ? ImageCreate.fromPartial(object.image)
        : undefined;
    message.file =
      object.file !== undefined && object.file !== null
        ? FileCreate.fromPartial(object.file)
        : undefined;
    message.storage =
      object.storage !== undefined && object.storage !== null
        ? StorageObjectMetadata.fromPartial(object.storage)
        : undefined;
    return message;
  },
};

function createBaseImageCreateManyItem(): ImageCreateManyItem {
  return { image: undefined, file: undefined, uploadId: '' };
}

export const ImageCreateManyItem: MessageFns<ImageCreateManyItem> = {
  encode(message: ImageCreateManyItem, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.image !== undefined) {
      ImageCreate.encode(message.image, writer.uint32(10).fork()).join();
    }
    if (message.file !== undefined) {
      FileCreate.encode(message.file, writer.uint32(18).fork()).join();
    }
    if (message.uploadId !== '') {
      writer.uint32(26).string(message.uploadId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImageCreateManyItem {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImageCreateManyItem();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.image = ImageCreate.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.file = FileCreate.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.uploadId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ImageCreateManyItem {
    return {
      image: isSet(object.image) ? ImageCreate.fromJSON(object.image) : undefined,
      file: isSet(object.file) ? FileCreate.fromJSON(object.file) : undefined,
      uploadId: isSet(object.uploadId) ? globalThis.String(object.uploadId) : '',
    };
  },

  toJSON(message: ImageCreateManyItem): unknown {
    const obj: any = {};
    if (message.image !== undefined) {
      obj.image = ImageCreate.toJSON(message.image);
    }
    if (message.file !== undefined) {
      obj.file = FileCreate.toJSON(message.file);
    }
    if (message.uploadId !== '') {
      obj.uploadId = message.uploadId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ImageCreateManyItem>, I>>(base?: I): ImageCreateManyItem {
    return ImageCreateManyItem.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ImageCreateManyItem>, I>>(
    object: I,
  ): ImageCreateManyItem {
    const message = createBaseImageCreateManyItem();
    message.image =
      object.image !== undefined && object.image !== null
        ? ImageCreate.fromPartial(object.image)
        : undefined;
    message.file =
      object.file !== undefined && object.file !== null
        ? FileCreate.fromPartial(object.file)
        : undefined;
    message.uploadId = object.uploadId ?? '';
    return message;
  },
};

function createBaseImageCreateManyRequest(): ImageCreateManyRequest {
  return { storage: undefined, items: [] };
}

export const ImageCreateManyRequest: MessageFns<ImageCreateManyRequest> = {
  encode(message: ImageCreateManyRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.storage !== undefined) {
      StorageObjectManyMetadata.encode(message.storage, writer.uint32(10).fork()).join();
    }
    for (const v of message.items) {
      ImageCreateManyItem.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImageCreateManyRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImageCreateManyRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.storage = StorageObjectManyMetadata.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.items.push(ImageCreateManyItem.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ImageCreateManyRequest {
    return {
      storage: isSet(object.storage)
        ? StorageObjectManyMetadata.fromJSON(object.storage)
        : undefined,
      items: globalThis.Array.isArray(object?.items)
        ? object.items.map((e: any) => ImageCreateManyItem.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ImageCreateManyRequest): unknown {
    const obj: any = {};
    if (message.storage !== undefined) {
      obj.storage = StorageObjectManyMetadata.toJSON(message.storage);
    }
    if (message.items?.length) {
      obj.items = message.items.map((e) => ImageCreateManyItem.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ImageCreateManyRequest>, I>>(
    base?: I,
  ): ImageCreateManyRequest {
    return ImageCreateManyRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ImageCreateManyRequest>, I>>(
    object: I,
  ): ImageCreateManyRequest {
    const message = createBaseImageCreateManyRequest();
    message.storage =
      object.storage !== undefined && object.storage !== null
        ? StorageObjectManyMetadata.fromPartial(object.storage)
        : undefined;
    message.items = object.items?.map((e) => ImageCreateManyItem.fromPartial(e)) || [];
    return message;
  },
};

function createBaseImageCreateManyResponse(): ImageCreateManyResponse {
  return { items: [] };
}

export const ImageCreateManyResponse: MessageFns<ImageCreateManyResponse> = {
  encode(
    message: ImageCreateManyResponse,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    for (const v of message.items) {
      Image.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImageCreateManyResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImageCreateManyResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.items.push(Image.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ImageCreateManyResponse {
    return {
      items: globalThis.Array.isArray(object?.items)
        ? object.items.map((e: any) => Image.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ImageCreateManyResponse): unknown {
    const obj: any = {};
    if (message.items?.length) {
      obj.items = message.items.map((e) => Image.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ImageCreateManyResponse>, I>>(
    base?: I,
  ): ImageCreateManyResponse {
    return ImageCreateManyResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ImageCreateManyResponse>, I>>(
    object: I,
  ): ImageCreateManyResponse {
    const message = createBaseImageCreateManyResponse();
    message.items = object.items?.map((e) => Image.fromPartial(e)) || [];
    return message;
  },
};

function createBaseImageUpdateSet(): ImageUpdateSet {
  return { alt: undefined };
}

export const ImageUpdateSet: MessageFns<ImageUpdateSet> = {
  encode(message: ImageUpdateSet, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.alt !== undefined) {
      writer.uint32(10).string(message.alt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImageUpdateSet {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImageUpdateSet();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.alt = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ImageUpdateSet {
    return { alt: isSet(object.alt) ? globalThis.String(object.alt) : undefined };
  },

  toJSON(message: ImageUpdateSet): unknown {
    const obj: any = {};
    if (message.alt !== undefined) {
      obj.alt = message.alt;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ImageUpdateSet>, I>>(base?: I): ImageUpdateSet {
    return ImageUpdateSet.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ImageUpdateSet>, I>>(object: I): ImageUpdateSet {
    const message = createBaseImageUpdateSet();
    message.alt = object.alt ?? undefined;
    return message;
  },
};

function createBaseImageUpdate(): ImageUpdate {
  return { set: undefined };
}

export const ImageUpdate: MessageFns<ImageUpdate> = {
  encode(message: ImageUpdate, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.set !== undefined) {
      ImageUpdateSet.encode(message.set, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImageUpdate {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImageUpdate();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.set = ImageUpdateSet.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ImageUpdate {
    return { set: isSet(object.set) ? ImageUpdateSet.fromJSON(object.set) : undefined };
  },

  toJSON(message: ImageUpdate): unknown {
    const obj: any = {};
    if (message.set !== undefined) {
      obj.set = ImageUpdateSet.toJSON(message.set);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ImageUpdate>, I>>(base?: I): ImageUpdate {
    return ImageUpdate.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ImageUpdate>, I>>(object: I): ImageUpdate {
    const message = createBaseImageUpdate();
    message.set =
      object.set !== undefined && object.set !== null
        ? ImageUpdateSet.fromPartial(object.set)
        : undefined;
    return message;
  },
};

function createBaseImageUpdateRequest(): ImageUpdateRequest {
  return { query: undefined, update: undefined };
}

export const ImageUpdateRequest: MessageFns<ImageUpdateRequest> = {
  encode(message: ImageUpdateRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.query !== undefined) {
      ImageQuery.encode(message.query, writer.uint32(10).fork()).join();
    }
    if (message.update !== undefined) {
      ImageUpdate.encode(message.update, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImageUpdateRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImageUpdateRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.query = ImageQuery.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.update = ImageUpdate.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ImageUpdateRequest {
    return {
      query: isSet(object.query) ? ImageQuery.fromJSON(object.query) : undefined,
      update: isSet(object.update) ? ImageUpdate.fromJSON(object.update) : undefined,
    };
  },

  toJSON(message: ImageUpdateRequest): unknown {
    const obj: any = {};
    if (message.query !== undefined) {
      obj.query = ImageQuery.toJSON(message.query);
    }
    if (message.update !== undefined) {
      obj.update = ImageUpdate.toJSON(message.update);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ImageUpdateRequest>, I>>(base?: I): ImageUpdateRequest {
    return ImageUpdateRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ImageUpdateRequest>, I>>(object: I): ImageUpdateRequest {
    const message = createBaseImageUpdateRequest();
    message.query =
      object.query !== undefined && object.query !== null
        ? ImageQuery.fromPartial(object.query)
        : undefined;
    message.update =
      object.update !== undefined && object.update !== null
        ? ImageUpdate.fromPartial(object.update)
        : undefined;
    return message;
  },
};

function createBaseImageUpdateByIdRequest(): ImageUpdateByIdRequest {
  return { id: '', update: undefined };
}

export const ImageUpdateByIdRequest: MessageFns<ImageUpdateByIdRequest> = {
  encode(message: ImageUpdateByIdRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== '') {
      writer.uint32(10).string(message.id);
    }
    if (message.update !== undefined) {
      ImageUpdate.encode(message.update, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImageUpdateByIdRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImageUpdateByIdRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.update = ImageUpdate.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ImageUpdateByIdRequest {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : '',
      update: isSet(object.update) ? ImageUpdate.fromJSON(object.update) : undefined,
    };
  },

  toJSON(message: ImageUpdateByIdRequest): unknown {
    const obj: any = {};
    if (message.id !== '') {
      obj.id = message.id;
    }
    if (message.update !== undefined) {
      obj.update = ImageUpdate.toJSON(message.update);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ImageUpdateByIdRequest>, I>>(
    base?: I,
  ): ImageUpdateByIdRequest {
    return ImageUpdateByIdRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ImageUpdateByIdRequest>, I>>(
    object: I,
  ): ImageUpdateByIdRequest {
    const message = createBaseImageUpdateByIdRequest();
    message.id = object.id ?? '';
    message.update =
      object.update !== undefined && object.update !== null
        ? ImageUpdate.fromPartial(object.update)
        : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === 'string') {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
