// @ts-nocheck
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               v7.34.0
// source: storage/image/image.messages.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from '@bufbuild/protobuf/wire';
import { StorageManyMeta, StorageMeta } from '../common/common.messages';
import { FileCreate } from '../file/file.messages';
import { Image } from './image';
import { ImagePopulated } from './image.populates';

export interface ImageQuery {
  id?: string;
  ids: string[];
  file?: string;
  userId?: string;
}

export interface ImageList {
  items: ImagePopulated[];
  total: number;
}

export interface ImageArray {
  images: Image[];
}

export interface ImageCreate {
  width: number;
  height: number;
  alt: string;
}

export interface ImageCreateOne {
  image: ImageCreate;
  file: FileCreate;
  storage?: StorageMeta;
  userId: string;
}

export interface ImageCreateOneWeb {
  image: ImageCreate;
  file: FileCreate;
  storage?: StorageMeta;
}

export interface ImageCreateManyItem {
  image: ImageCreate;
  file: FileCreate;
  uploadId: string;
}

export interface ImageCreateMany {
  storage?: StorageManyMeta;
  items: ImageCreateManyItem[];
  userId: string;
}

export interface ImageCreateManyWeb {
  storage?: StorageManyMeta;
  items: ImageCreateManyItem[];
}

export interface ImageUpdateSet {
  alt?: string;
}

export interface ImageUpdate {
  set?: ImageUpdateSet;
}

export interface ImageUpdateOne {
  query: ImageQuery;
  update: ImageUpdate;
}

export interface ImageUpdateById {
  id: string;
  update: ImageUpdate;
}

function createBaseImageQuery(): ImageQuery {
  return { id: undefined, ids: [], file: undefined, userId: undefined };
}

export const ImageQuery: MessageFns<ImageQuery> = {
  encode(message: ImageQuery, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      writer.uint32(10).string(message.id);
    }
    for (const v of message.ids) {
      writer.uint32(18).string(v!);
    }
    if (message.file !== undefined) {
      writer.uint32(26).string(message.file);
    }
    if (message.userId !== undefined) {
      writer.uint32(34).string(message.userId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImageQuery {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImageQuery();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.ids.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.file = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ImageQuery {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : undefined,
      ids: globalThis.Array.isArray(object?.ids)
        ? object.ids.map((e: any) => globalThis.String(e))
        : [],
      file: isSet(object.file) ? globalThis.String(object.file) : undefined,
      userId: isSet(object.userId) ? globalThis.String(object.userId) : undefined,
    };
  },

  toJSON(message: ImageQuery): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = message.id;
    }
    if (message.ids?.length) {
      obj.ids = message.ids;
    }
    if (message.file !== undefined) {
      obj.file = message.file;
    }
    if (message.userId !== undefined) {
      obj.userId = message.userId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ImageQuery>, I>>(base?: I): ImageQuery {
    return ImageQuery.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ImageQuery>, I>>(object: I): ImageQuery {
    const message = createBaseImageQuery();
    message.id = object.id ?? undefined;
    message.ids = object.ids?.map((e) => e) || [];
    message.file = object.file ?? undefined;
    message.userId = object.userId ?? undefined;
    return message;
  },
};

function createBaseImageList(): ImageList {
  return { items: [], total: 0 };
}

export const ImageList: MessageFns<ImageList> = {
  encode(message: ImageList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.items) {
      ImagePopulated.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.total !== 0) {
      writer.uint32(16).int32(message.total);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImageList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImageList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.items.push(ImagePopulated.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.total = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ImageList {
    return {
      items: globalThis.Array.isArray(object?.items)
        ? object.items.map((e: any) => ImagePopulated.fromJSON(e))
        : [],
      total: isSet(object.total) ? globalThis.Number(object.total) : 0,
    };
  },

  toJSON(message: ImageList): unknown {
    const obj: any = {};
    if (message.items?.length) {
      obj.items = message.items.map((e) => ImagePopulated.toJSON(e));
    }
    if (message.total !== 0) {
      obj.total = Math.round(message.total);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ImageList>, I>>(base?: I): ImageList {
    return ImageList.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ImageList>, I>>(object: I): ImageList {
    const message = createBaseImageList();
    message.items = object.items?.map((e) => ImagePopulated.fromPartial(e)) || [];
    message.total = object.total ?? 0;
    return message;
  },
};

function createBaseImageArray(): ImageArray {
  return { images: [] };
}

export const ImageArray: MessageFns<ImageArray> = {
  encode(message: ImageArray, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.images) {
      Image.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImageArray {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImageArray();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.images.push(Image.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ImageArray {
    return {
      images: globalThis.Array.isArray(object?.images)
        ? object.images.map((e: any) => Image.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ImageArray): unknown {
    const obj: any = {};
    if (message.images?.length) {
      obj.images = message.images.map((e) => Image.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ImageArray>, I>>(base?: I): ImageArray {
    return ImageArray.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ImageArray>, I>>(object: I): ImageArray {
    const message = createBaseImageArray();
    message.images = object.images?.map((e) => Image.fromPartial(e)) || [];
    return message;
  },
};

function createBaseImageCreate(): ImageCreate {
  return { width: 0, height: 0, alt: '' };
}

export const ImageCreate: MessageFns<ImageCreate> = {
  encode(message: ImageCreate, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.width !== 0) {
      writer.uint32(8).int32(message.width);
    }
    if (message.height !== 0) {
      writer.uint32(16).int32(message.height);
    }
    if (message.alt !== '') {
      writer.uint32(26).string(message.alt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImageCreate {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImageCreate();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.width = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.height = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.alt = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ImageCreate {
    return {
      width: isSet(object.width) ? globalThis.Number(object.width) : 0,
      height: isSet(object.height) ? globalThis.Number(object.height) : 0,
      alt: isSet(object.alt) ? globalThis.String(object.alt) : '',
    };
  },

  toJSON(message: ImageCreate): unknown {
    const obj: any = {};
    if (message.width !== 0) {
      obj.width = Math.round(message.width);
    }
    if (message.height !== 0) {
      obj.height = Math.round(message.height);
    }
    if (message.alt !== '') {
      obj.alt = message.alt;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ImageCreate>, I>>(base?: I): ImageCreate {
    return ImageCreate.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ImageCreate>, I>>(object: I): ImageCreate {
    const message = createBaseImageCreate();
    message.width = object.width ?? 0;
    message.height = object.height ?? 0;
    message.alt = object.alt ?? '';
    return message;
  },
};

function createBaseImageCreateOne(): ImageCreateOne {
  return { image: undefined, file: undefined, storage: undefined, userId: '' };
}

export const ImageCreateOne: MessageFns<ImageCreateOne> = {
  encode(message: ImageCreateOne, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.image !== undefined) {
      ImageCreate.encode(message.image, writer.uint32(10).fork()).join();
    }
    if (message.file !== undefined) {
      FileCreate.encode(message.file, writer.uint32(18).fork()).join();
    }
    if (message.storage !== undefined) {
      StorageMeta.encode(message.storage, writer.uint32(26).fork()).join();
    }
    if (message.userId !== '') {
      writer.uint32(34).string(message.userId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImageCreateOne {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImageCreateOne();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.image = ImageCreate.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.file = FileCreate.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.storage = StorageMeta.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ImageCreateOne {
    return {
      image: isSet(object.image) ? ImageCreate.fromJSON(object.image) : undefined,
      file: isSet(object.file) ? FileCreate.fromJSON(object.file) : undefined,
      storage: isSet(object.storage) ? StorageMeta.fromJSON(object.storage) : undefined,
      userId: isSet(object.userId) ? globalThis.String(object.userId) : '',
    };
  },

  toJSON(message: ImageCreateOne): unknown {
    const obj: any = {};
    if (message.image !== undefined) {
      obj.image = ImageCreate.toJSON(message.image);
    }
    if (message.file !== undefined) {
      obj.file = FileCreate.toJSON(message.file);
    }
    if (message.storage !== undefined) {
      obj.storage = StorageMeta.toJSON(message.storage);
    }
    if (message.userId !== '') {
      obj.userId = message.userId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ImageCreateOne>, I>>(base?: I): ImageCreateOne {
    return ImageCreateOne.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ImageCreateOne>, I>>(object: I): ImageCreateOne {
    const message = createBaseImageCreateOne();
    message.image =
      object.image !== undefined && object.image !== null
        ? ImageCreate.fromPartial(object.image)
        : undefined;
    message.file =
      object.file !== undefined && object.file !== null
        ? FileCreate.fromPartial(object.file)
        : undefined;
    message.storage =
      object.storage !== undefined && object.storage !== null
        ? StorageMeta.fromPartial(object.storage)
        : undefined;
    message.userId = object.userId ?? '';
    return message;
  },
};

function createBaseImageCreateOneWeb(): ImageCreateOneWeb {
  return { image: undefined, file: undefined, storage: undefined };
}

export const ImageCreateOneWeb: MessageFns<ImageCreateOneWeb> = {
  encode(message: ImageCreateOneWeb, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.image !== undefined) {
      ImageCreate.encode(message.image, writer.uint32(10).fork()).join();
    }
    if (message.file !== undefined) {
      FileCreate.encode(message.file, writer.uint32(18).fork()).join();
    }
    if (message.storage !== undefined) {
      StorageMeta.encode(message.storage, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImageCreateOneWeb {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImageCreateOneWeb();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.image = ImageCreate.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.file = FileCreate.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.storage = StorageMeta.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ImageCreateOneWeb {
    return {
      image: isSet(object.image) ? ImageCreate.fromJSON(object.image) : undefined,
      file: isSet(object.file) ? FileCreate.fromJSON(object.file) : undefined,
      storage: isSet(object.storage) ? StorageMeta.fromJSON(object.storage) : undefined,
    };
  },

  toJSON(message: ImageCreateOneWeb): unknown {
    const obj: any = {};
    if (message.image !== undefined) {
      obj.image = ImageCreate.toJSON(message.image);
    }
    if (message.file !== undefined) {
      obj.file = FileCreate.toJSON(message.file);
    }
    if (message.storage !== undefined) {
      obj.storage = StorageMeta.toJSON(message.storage);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ImageCreateOneWeb>, I>>(base?: I): ImageCreateOneWeb {
    return ImageCreateOneWeb.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ImageCreateOneWeb>, I>>(object: I): ImageCreateOneWeb {
    const message = createBaseImageCreateOneWeb();
    message.image =
      object.image !== undefined && object.image !== null
        ? ImageCreate.fromPartial(object.image)
        : undefined;
    message.file =
      object.file !== undefined && object.file !== null
        ? FileCreate.fromPartial(object.file)
        : undefined;
    message.storage =
      object.storage !== undefined && object.storage !== null
        ? StorageMeta.fromPartial(object.storage)
        : undefined;
    return message;
  },
};

function createBaseImageCreateManyItem(): ImageCreateManyItem {
  return { image: undefined, file: undefined, uploadId: '' };
}

export const ImageCreateManyItem: MessageFns<ImageCreateManyItem> = {
  encode(message: ImageCreateManyItem, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.image !== undefined) {
      ImageCreate.encode(message.image, writer.uint32(10).fork()).join();
    }
    if (message.file !== undefined) {
      FileCreate.encode(message.file, writer.uint32(18).fork()).join();
    }
    if (message.uploadId !== '') {
      writer.uint32(26).string(message.uploadId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImageCreateManyItem {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImageCreateManyItem();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.image = ImageCreate.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.file = FileCreate.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.uploadId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ImageCreateManyItem {
    return {
      image: isSet(object.image) ? ImageCreate.fromJSON(object.image) : undefined,
      file: isSet(object.file) ? FileCreate.fromJSON(object.file) : undefined,
      uploadId: isSet(object.uploadId) ? globalThis.String(object.uploadId) : '',
    };
  },

  toJSON(message: ImageCreateManyItem): unknown {
    const obj: any = {};
    if (message.image !== undefined) {
      obj.image = ImageCreate.toJSON(message.image);
    }
    if (message.file !== undefined) {
      obj.file = FileCreate.toJSON(message.file);
    }
    if (message.uploadId !== '') {
      obj.uploadId = message.uploadId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ImageCreateManyItem>, I>>(base?: I): ImageCreateManyItem {
    return ImageCreateManyItem.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ImageCreateManyItem>, I>>(
    object: I,
  ): ImageCreateManyItem {
    const message = createBaseImageCreateManyItem();
    message.image =
      object.image !== undefined && object.image !== null
        ? ImageCreate.fromPartial(object.image)
        : undefined;
    message.file =
      object.file !== undefined && object.file !== null
        ? FileCreate.fromPartial(object.file)
        : undefined;
    message.uploadId = object.uploadId ?? '';
    return message;
  },
};

function createBaseImageCreateMany(): ImageCreateMany {
  return { storage: undefined, items: [], userId: '' };
}

export const ImageCreateMany: MessageFns<ImageCreateMany> = {
  encode(message: ImageCreateMany, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.storage !== undefined) {
      StorageManyMeta.encode(message.storage, writer.uint32(10).fork()).join();
    }
    for (const v of message.items) {
      ImageCreateManyItem.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.userId !== '') {
      writer.uint32(26).string(message.userId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImageCreateMany {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImageCreateMany();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.storage = StorageManyMeta.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.items.push(ImageCreateManyItem.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ImageCreateMany {
    return {
      storage: isSet(object.storage) ? StorageManyMeta.fromJSON(object.storage) : undefined,
      items: globalThis.Array.isArray(object?.items)
        ? object.items.map((e: any) => ImageCreateManyItem.fromJSON(e))
        : [],
      userId: isSet(object.userId) ? globalThis.String(object.userId) : '',
    };
  },

  toJSON(message: ImageCreateMany): unknown {
    const obj: any = {};
    if (message.storage !== undefined) {
      obj.storage = StorageManyMeta.toJSON(message.storage);
    }
    if (message.items?.length) {
      obj.items = message.items.map((e) => ImageCreateManyItem.toJSON(e));
    }
    if (message.userId !== '') {
      obj.userId = message.userId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ImageCreateMany>, I>>(base?: I): ImageCreateMany {
    return ImageCreateMany.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ImageCreateMany>, I>>(object: I): ImageCreateMany {
    const message = createBaseImageCreateMany();
    message.storage =
      object.storage !== undefined && object.storage !== null
        ? StorageManyMeta.fromPartial(object.storage)
        : undefined;
    message.items = object.items?.map((e) => ImageCreateManyItem.fromPartial(e)) || [];
    message.userId = object.userId ?? '';
    return message;
  },
};

function createBaseImageCreateManyWeb(): ImageCreateManyWeb {
  return { storage: undefined, items: [] };
}

export const ImageCreateManyWeb: MessageFns<ImageCreateManyWeb> = {
  encode(message: ImageCreateManyWeb, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.storage !== undefined) {
      StorageManyMeta.encode(message.storage, writer.uint32(10).fork()).join();
    }
    for (const v of message.items) {
      ImageCreateManyItem.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImageCreateManyWeb {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImageCreateManyWeb();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.storage = StorageManyMeta.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.items.push(ImageCreateManyItem.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ImageCreateManyWeb {
    return {
      storage: isSet(object.storage) ? StorageManyMeta.fromJSON(object.storage) : undefined,
      items: globalThis.Array.isArray(object?.items)
        ? object.items.map((e: any) => ImageCreateManyItem.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ImageCreateManyWeb): unknown {
    const obj: any = {};
    if (message.storage !== undefined) {
      obj.storage = StorageManyMeta.toJSON(message.storage);
    }
    if (message.items?.length) {
      obj.items = message.items.map((e) => ImageCreateManyItem.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ImageCreateManyWeb>, I>>(base?: I): ImageCreateManyWeb {
    return ImageCreateManyWeb.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ImageCreateManyWeb>, I>>(object: I): ImageCreateManyWeb {
    const message = createBaseImageCreateManyWeb();
    message.storage =
      object.storage !== undefined && object.storage !== null
        ? StorageManyMeta.fromPartial(object.storage)
        : undefined;
    message.items = object.items?.map((e) => ImageCreateManyItem.fromPartial(e)) || [];
    return message;
  },
};

function createBaseImageUpdateSet(): ImageUpdateSet {
  return { alt: undefined };
}

export const ImageUpdateSet: MessageFns<ImageUpdateSet> = {
  encode(message: ImageUpdateSet, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.alt !== undefined) {
      writer.uint32(10).string(message.alt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImageUpdateSet {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImageUpdateSet();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.alt = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ImageUpdateSet {
    return { alt: isSet(object.alt) ? globalThis.String(object.alt) : undefined };
  },

  toJSON(message: ImageUpdateSet): unknown {
    const obj: any = {};
    if (message.alt !== undefined) {
      obj.alt = message.alt;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ImageUpdateSet>, I>>(base?: I): ImageUpdateSet {
    return ImageUpdateSet.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ImageUpdateSet>, I>>(object: I): ImageUpdateSet {
    const message = createBaseImageUpdateSet();
    message.alt = object.alt ?? undefined;
    return message;
  },
};

function createBaseImageUpdate(): ImageUpdate {
  return { set: undefined };
}

export const ImageUpdate: MessageFns<ImageUpdate> = {
  encode(message: ImageUpdate, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.set !== undefined) {
      ImageUpdateSet.encode(message.set, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImageUpdate {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImageUpdate();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.set = ImageUpdateSet.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ImageUpdate {
    return { set: isSet(object.set) ? ImageUpdateSet.fromJSON(object.set) : undefined };
  },

  toJSON(message: ImageUpdate): unknown {
    const obj: any = {};
    if (message.set !== undefined) {
      obj.set = ImageUpdateSet.toJSON(message.set);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ImageUpdate>, I>>(base?: I): ImageUpdate {
    return ImageUpdate.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ImageUpdate>, I>>(object: I): ImageUpdate {
    const message = createBaseImageUpdate();
    message.set =
      object.set !== undefined && object.set !== null
        ? ImageUpdateSet.fromPartial(object.set)
        : undefined;
    return message;
  },
};

function createBaseImageUpdateOne(): ImageUpdateOne {
  return { query: undefined, update: undefined };
}

export const ImageUpdateOne: MessageFns<ImageUpdateOne> = {
  encode(message: ImageUpdateOne, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.query !== undefined) {
      ImageQuery.encode(message.query, writer.uint32(10).fork()).join();
    }
    if (message.update !== undefined) {
      ImageUpdate.encode(message.update, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImageUpdateOne {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImageUpdateOne();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.query = ImageQuery.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.update = ImageUpdate.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ImageUpdateOne {
    return {
      query: isSet(object.query) ? ImageQuery.fromJSON(object.query) : undefined,
      update: isSet(object.update) ? ImageUpdate.fromJSON(object.update) : undefined,
    };
  },

  toJSON(message: ImageUpdateOne): unknown {
    const obj: any = {};
    if (message.query !== undefined) {
      obj.query = ImageQuery.toJSON(message.query);
    }
    if (message.update !== undefined) {
      obj.update = ImageUpdate.toJSON(message.update);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ImageUpdateOne>, I>>(base?: I): ImageUpdateOne {
    return ImageUpdateOne.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ImageUpdateOne>, I>>(object: I): ImageUpdateOne {
    const message = createBaseImageUpdateOne();
    message.query =
      object.query !== undefined && object.query !== null
        ? ImageQuery.fromPartial(object.query)
        : undefined;
    message.update =
      object.update !== undefined && object.update !== null
        ? ImageUpdate.fromPartial(object.update)
        : undefined;
    return message;
  },
};

function createBaseImageUpdateById(): ImageUpdateById {
  return { id: '', update: undefined };
}

export const ImageUpdateById: MessageFns<ImageUpdateById> = {
  encode(message: ImageUpdateById, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== '') {
      writer.uint32(10).string(message.id);
    }
    if (message.update !== undefined) {
      ImageUpdate.encode(message.update, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImageUpdateById {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImageUpdateById();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.update = ImageUpdate.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ImageUpdateById {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : '',
      update: isSet(object.update) ? ImageUpdate.fromJSON(object.update) : undefined,
    };
  },

  toJSON(message: ImageUpdateById): unknown {
    const obj: any = {};
    if (message.id !== '') {
      obj.id = message.id;
    }
    if (message.update !== undefined) {
      obj.update = ImageUpdate.toJSON(message.update);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ImageUpdateById>, I>>(base?: I): ImageUpdateById {
    return ImageUpdateById.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ImageUpdateById>, I>>(object: I): ImageUpdateById {
    const message = createBaseImageUpdateById();
    message.id = object.id ?? '';
    message.update =
      object.update !== undefined && object.update !== null
        ? ImageUpdate.fromPartial(object.update)
        : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
