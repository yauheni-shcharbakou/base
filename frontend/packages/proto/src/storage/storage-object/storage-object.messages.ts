// @ts-nocheck
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               v7.34.0
// source: storage/storage-object/storage-object.messages.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from '@bufbuild/protobuf/wire';
import {
  StorageObjectType,
  storageObjectTypeFromJSON,
  storageObjectTypeToJSON,
  storageObjectTypeToNumber,
} from './storage-object';
import { StorageObjectPopulated } from './storage-object.populates';

export interface StorageObjectQuery {
  id?: string;
  ids: string[];
  type?: StorageObjectType;
  userId?: string;
  isPublic?: boolean;
  parent?: string;
  file?: string;
  name?: string;
}

export interface StorageObjectQueryWeb {
  id?: string;
  ids: string[];
  type?: StorageObjectType;
  isPublic?: boolean;
  parent?: string;
  name?: string;
}

export interface StorageObjectList {
  items: StorageObjectPopulated[];
  total: number;
}

export interface StorageObjectArray {
  storageObjects: StorageObjectPopulated[];
}

export interface StorageObjectGet {
  query?: StorageObjectQuery;
}

export interface StorageObjectGetFolders {
  excludeChildrenOf?: string;
  userId: string;
}

export interface StorageObjectGetFoldersWeb {
  excludeChildrenOf?: string;
}

export interface StorageObjectCreate {
  name: string;
  isPublic: boolean;
  parent?: string;
  type: StorageObjectType;
  userId: string;
  file?: string;
  image?: string;
  video?: string;
}

export interface StorageObjectCreateWeb {
  name: string;
  isPublic: boolean;
  parent?: string;
  type: StorageObjectType;
  file?: string;
  image?: string;
  video?: string;
}

export interface StorageObjectUpdateSet {
  name?: string;
  isPublic?: boolean;
  parent?: string;
}

export interface StorageObjectUpdate {
  set?: StorageObjectUpdateSet;
}

export interface StorageObjectUpdateOne {
  query: StorageObjectQuery;
  update: StorageObjectUpdate;
}

export interface StorageObjectUpdateById {
  id: string;
  update: StorageObjectUpdate;
}

function createBaseStorageObjectQuery(): StorageObjectQuery {
  return {
    id: undefined,
    ids: [],
    type: undefined,
    userId: undefined,
    isPublic: undefined,
    parent: undefined,
    file: undefined,
    name: undefined,
  };
}

export const StorageObjectQuery: MessageFns<StorageObjectQuery> = {
  encode(message: StorageObjectQuery, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      writer.uint32(10).string(message.id);
    }
    for (const v of message.ids) {
      writer.uint32(18).string(v!);
    }
    if (message.type !== undefined) {
      writer.uint32(24).int32(storageObjectTypeToNumber(message.type));
    }
    if (message.userId !== undefined) {
      writer.uint32(34).string(message.userId);
    }
    if (message.isPublic !== undefined) {
      writer.uint32(40).bool(message.isPublic);
    }
    if (message.parent !== undefined) {
      writer.uint32(50).string(message.parent);
    }
    if (message.file !== undefined) {
      writer.uint32(58).string(message.file);
    }
    if (message.name !== undefined) {
      writer.uint32(66).string(message.name);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StorageObjectQuery {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStorageObjectQuery();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.ids.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.type = storageObjectTypeFromJSON(reader.int32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.isPublic = reader.bool();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.parent = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.file = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.name = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StorageObjectQuery {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : undefined,
      ids: globalThis.Array.isArray(object?.ids)
        ? object.ids.map((e: any) => globalThis.String(e))
        : [],
      type: isSet(object.type) ? storageObjectTypeFromJSON(object.type) : undefined,
      userId: isSet(object.userId) ? globalThis.String(object.userId) : undefined,
      isPublic: isSet(object.isPublic) ? globalThis.Boolean(object.isPublic) : undefined,
      parent: isSet(object.parent) ? globalThis.String(object.parent) : undefined,
      file: isSet(object.file) ? globalThis.String(object.file) : undefined,
      name: isSet(object.name) ? globalThis.String(object.name) : undefined,
    };
  },

  toJSON(message: StorageObjectQuery): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = message.id;
    }
    if (message.ids?.length) {
      obj.ids = message.ids;
    }
    if (message.type !== undefined) {
      obj.type = storageObjectTypeToJSON(message.type);
    }
    if (message.userId !== undefined) {
      obj.userId = message.userId;
    }
    if (message.isPublic !== undefined) {
      obj.isPublic = message.isPublic;
    }
    if (message.parent !== undefined) {
      obj.parent = message.parent;
    }
    if (message.file !== undefined) {
      obj.file = message.file;
    }
    if (message.name !== undefined) {
      obj.name = message.name;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StorageObjectQuery>, I>>(base?: I): StorageObjectQuery {
    return StorageObjectQuery.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StorageObjectQuery>, I>>(object: I): StorageObjectQuery {
    const message = createBaseStorageObjectQuery();
    message.id = object.id ?? undefined;
    message.ids = object.ids?.map((e) => e) || [];
    message.type = object.type ?? undefined;
    message.userId = object.userId ?? undefined;
    message.isPublic = object.isPublic ?? undefined;
    message.parent = object.parent ?? undefined;
    message.file = object.file ?? undefined;
    message.name = object.name ?? undefined;
    return message;
  },
};

function createBaseStorageObjectQueryWeb(): StorageObjectQueryWeb {
  return {
    id: undefined,
    ids: [],
    type: undefined,
    isPublic: undefined,
    parent: undefined,
    name: undefined,
  };
}

export const StorageObjectQueryWeb: MessageFns<StorageObjectQueryWeb> = {
  encode(message: StorageObjectQueryWeb, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      writer.uint32(10).string(message.id);
    }
    for (const v of message.ids) {
      writer.uint32(18).string(v!);
    }
    if (message.type !== undefined) {
      writer.uint32(24).int32(storageObjectTypeToNumber(message.type));
    }
    if (message.isPublic !== undefined) {
      writer.uint32(32).bool(message.isPublic);
    }
    if (message.parent !== undefined) {
      writer.uint32(42).string(message.parent);
    }
    if (message.name !== undefined) {
      writer.uint32(50).string(message.name);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StorageObjectQueryWeb {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStorageObjectQueryWeb();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.ids.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.type = storageObjectTypeFromJSON(reader.int32());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.isPublic = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.parent = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.name = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StorageObjectQueryWeb {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : undefined,
      ids: globalThis.Array.isArray(object?.ids)
        ? object.ids.map((e: any) => globalThis.String(e))
        : [],
      type: isSet(object.type) ? storageObjectTypeFromJSON(object.type) : undefined,
      isPublic: isSet(object.isPublic) ? globalThis.Boolean(object.isPublic) : undefined,
      parent: isSet(object.parent) ? globalThis.String(object.parent) : undefined,
      name: isSet(object.name) ? globalThis.String(object.name) : undefined,
    };
  },

  toJSON(message: StorageObjectQueryWeb): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = message.id;
    }
    if (message.ids?.length) {
      obj.ids = message.ids;
    }
    if (message.type !== undefined) {
      obj.type = storageObjectTypeToJSON(message.type);
    }
    if (message.isPublic !== undefined) {
      obj.isPublic = message.isPublic;
    }
    if (message.parent !== undefined) {
      obj.parent = message.parent;
    }
    if (message.name !== undefined) {
      obj.name = message.name;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StorageObjectQueryWeb>, I>>(base?: I): StorageObjectQueryWeb {
    return StorageObjectQueryWeb.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StorageObjectQueryWeb>, I>>(
    object: I,
  ): StorageObjectQueryWeb {
    const message = createBaseStorageObjectQueryWeb();
    message.id = object.id ?? undefined;
    message.ids = object.ids?.map((e) => e) || [];
    message.type = object.type ?? undefined;
    message.isPublic = object.isPublic ?? undefined;
    message.parent = object.parent ?? undefined;
    message.name = object.name ?? undefined;
    return message;
  },
};

function createBaseStorageObjectList(): StorageObjectList {
  return { items: [], total: 0 };
}

export const StorageObjectList: MessageFns<StorageObjectList> = {
  encode(message: StorageObjectList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.items) {
      StorageObjectPopulated.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.total !== 0) {
      writer.uint32(16).int32(message.total);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StorageObjectList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStorageObjectList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.items.push(StorageObjectPopulated.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.total = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StorageObjectList {
    return {
      items: globalThis.Array.isArray(object?.items)
        ? object.items.map((e: any) => StorageObjectPopulated.fromJSON(e))
        : [],
      total: isSet(object.total) ? globalThis.Number(object.total) : 0,
    };
  },

  toJSON(message: StorageObjectList): unknown {
    const obj: any = {};
    if (message.items?.length) {
      obj.items = message.items.map((e) => StorageObjectPopulated.toJSON(e));
    }
    if (message.total !== 0) {
      obj.total = Math.round(message.total);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StorageObjectList>, I>>(base?: I): StorageObjectList {
    return StorageObjectList.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StorageObjectList>, I>>(object: I): StorageObjectList {
    const message = createBaseStorageObjectList();
    message.items = object.items?.map((e) => StorageObjectPopulated.fromPartial(e)) || [];
    message.total = object.total ?? 0;
    return message;
  },
};

function createBaseStorageObjectArray(): StorageObjectArray {
  return { storageObjects: [] };
}

export const StorageObjectArray: MessageFns<StorageObjectArray> = {
  encode(message: StorageObjectArray, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.storageObjects) {
      StorageObjectPopulated.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StorageObjectArray {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStorageObjectArray();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.storageObjects.push(StorageObjectPopulated.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StorageObjectArray {
    return {
      storageObjects: globalThis.Array.isArray(object?.storageObjects)
        ? object.storageObjects.map((e: any) => StorageObjectPopulated.fromJSON(e))
        : [],
    };
  },

  toJSON(message: StorageObjectArray): unknown {
    const obj: any = {};
    if (message.storageObjects?.length) {
      obj.storageObjects = message.storageObjects.map((e) => StorageObjectPopulated.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StorageObjectArray>, I>>(base?: I): StorageObjectArray {
    return StorageObjectArray.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StorageObjectArray>, I>>(object: I): StorageObjectArray {
    const message = createBaseStorageObjectArray();
    message.storageObjects =
      object.storageObjects?.map((e) => StorageObjectPopulated.fromPartial(e)) || [];
    return message;
  },
};

function createBaseStorageObjectGet(): StorageObjectGet {
  return { query: undefined };
}

export const StorageObjectGet: MessageFns<StorageObjectGet> = {
  encode(message: StorageObjectGet, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.query !== undefined) {
      StorageObjectQuery.encode(message.query, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StorageObjectGet {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStorageObjectGet();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.query = StorageObjectQuery.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StorageObjectGet {
    return { query: isSet(object.query) ? StorageObjectQuery.fromJSON(object.query) : undefined };
  },

  toJSON(message: StorageObjectGet): unknown {
    const obj: any = {};
    if (message.query !== undefined) {
      obj.query = StorageObjectQuery.toJSON(message.query);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StorageObjectGet>, I>>(base?: I): StorageObjectGet {
    return StorageObjectGet.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StorageObjectGet>, I>>(object: I): StorageObjectGet {
    const message = createBaseStorageObjectGet();
    message.query =
      object.query !== undefined && object.query !== null
        ? StorageObjectQuery.fromPartial(object.query)
        : undefined;
    return message;
  },
};

function createBaseStorageObjectGetFolders(): StorageObjectGetFolders {
  return { excludeChildrenOf: undefined, userId: '' };
}

export const StorageObjectGetFolders: MessageFns<StorageObjectGetFolders> = {
  encode(
    message: StorageObjectGetFolders,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.excludeChildrenOf !== undefined) {
      writer.uint32(10).string(message.excludeChildrenOf);
    }
    if (message.userId !== '') {
      writer.uint32(18).string(message.userId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StorageObjectGetFolders {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStorageObjectGetFolders();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.excludeChildrenOf = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StorageObjectGetFolders {
    return {
      excludeChildrenOf: isSet(object.excludeChildrenOf)
        ? globalThis.String(object.excludeChildrenOf)
        : undefined,
      userId: isSet(object.userId) ? globalThis.String(object.userId) : '',
    };
  },

  toJSON(message: StorageObjectGetFolders): unknown {
    const obj: any = {};
    if (message.excludeChildrenOf !== undefined) {
      obj.excludeChildrenOf = message.excludeChildrenOf;
    }
    if (message.userId !== '') {
      obj.userId = message.userId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StorageObjectGetFolders>, I>>(
    base?: I,
  ): StorageObjectGetFolders {
    return StorageObjectGetFolders.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StorageObjectGetFolders>, I>>(
    object: I,
  ): StorageObjectGetFolders {
    const message = createBaseStorageObjectGetFolders();
    message.excludeChildrenOf = object.excludeChildrenOf ?? undefined;
    message.userId = object.userId ?? '';
    return message;
  },
};

function createBaseStorageObjectGetFoldersWeb(): StorageObjectGetFoldersWeb {
  return { excludeChildrenOf: undefined };
}

export const StorageObjectGetFoldersWeb: MessageFns<StorageObjectGetFoldersWeb> = {
  encode(
    message: StorageObjectGetFoldersWeb,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.excludeChildrenOf !== undefined) {
      writer.uint32(10).string(message.excludeChildrenOf);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StorageObjectGetFoldersWeb {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStorageObjectGetFoldersWeb();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.excludeChildrenOf = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StorageObjectGetFoldersWeb {
    return {
      excludeChildrenOf: isSet(object.excludeChildrenOf)
        ? globalThis.String(object.excludeChildrenOf)
        : undefined,
    };
  },

  toJSON(message: StorageObjectGetFoldersWeb): unknown {
    const obj: any = {};
    if (message.excludeChildrenOf !== undefined) {
      obj.excludeChildrenOf = message.excludeChildrenOf;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StorageObjectGetFoldersWeb>, I>>(
    base?: I,
  ): StorageObjectGetFoldersWeb {
    return StorageObjectGetFoldersWeb.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StorageObjectGetFoldersWeb>, I>>(
    object: I,
  ): StorageObjectGetFoldersWeb {
    const message = createBaseStorageObjectGetFoldersWeb();
    message.excludeChildrenOf = object.excludeChildrenOf ?? undefined;
    return message;
  },
};

function createBaseStorageObjectCreate(): StorageObjectCreate {
  return {
    name: '',
    isPublic: false,
    parent: undefined,
    type: StorageObjectType.FOLDER,
    userId: '',
    file: undefined,
    image: undefined,
    video: undefined,
  };
}

export const StorageObjectCreate: MessageFns<StorageObjectCreate> = {
  encode(message: StorageObjectCreate, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== '') {
      writer.uint32(10).string(message.name);
    }
    if (message.isPublic !== false) {
      writer.uint32(16).bool(message.isPublic);
    }
    if (message.parent !== undefined) {
      writer.uint32(26).string(message.parent);
    }
    if (message.type !== StorageObjectType.FOLDER) {
      writer.uint32(32).int32(storageObjectTypeToNumber(message.type));
    }
    if (message.userId !== '') {
      writer.uint32(42).string(message.userId);
    }
    if (message.file !== undefined) {
      writer.uint32(50).string(message.file);
    }
    if (message.image !== undefined) {
      writer.uint32(58).string(message.image);
    }
    if (message.video !== undefined) {
      writer.uint32(66).string(message.video);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StorageObjectCreate {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStorageObjectCreate();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.isPublic = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.parent = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.type = storageObjectTypeFromJSON(reader.int32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.file = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.image = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.video = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StorageObjectCreate {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : '',
      isPublic: isSet(object.isPublic) ? globalThis.Boolean(object.isPublic) : false,
      parent: isSet(object.parent) ? globalThis.String(object.parent) : undefined,
      type: isSet(object.type) ? storageObjectTypeFromJSON(object.type) : StorageObjectType.FOLDER,
      userId: isSet(object.userId) ? globalThis.String(object.userId) : '',
      file: isSet(object.file) ? globalThis.String(object.file) : undefined,
      image: isSet(object.image) ? globalThis.String(object.image) : undefined,
      video: isSet(object.video) ? globalThis.String(object.video) : undefined,
    };
  },

  toJSON(message: StorageObjectCreate): unknown {
    const obj: any = {};
    if (message.name !== '') {
      obj.name = message.name;
    }
    if (message.isPublic !== false) {
      obj.isPublic = message.isPublic;
    }
    if (message.parent !== undefined) {
      obj.parent = message.parent;
    }
    if (message.type !== StorageObjectType.FOLDER) {
      obj.type = storageObjectTypeToJSON(message.type);
    }
    if (message.userId !== '') {
      obj.userId = message.userId;
    }
    if (message.file !== undefined) {
      obj.file = message.file;
    }
    if (message.image !== undefined) {
      obj.image = message.image;
    }
    if (message.video !== undefined) {
      obj.video = message.video;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StorageObjectCreate>, I>>(base?: I): StorageObjectCreate {
    return StorageObjectCreate.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StorageObjectCreate>, I>>(
    object: I,
  ): StorageObjectCreate {
    const message = createBaseStorageObjectCreate();
    message.name = object.name ?? '';
    message.isPublic = object.isPublic ?? false;
    message.parent = object.parent ?? undefined;
    message.type = object.type ?? StorageObjectType.FOLDER;
    message.userId = object.userId ?? '';
    message.file = object.file ?? undefined;
    message.image = object.image ?? undefined;
    message.video = object.video ?? undefined;
    return message;
  },
};

function createBaseStorageObjectCreateWeb(): StorageObjectCreateWeb {
  return {
    name: '',
    isPublic: false,
    parent: undefined,
    type: StorageObjectType.FOLDER,
    file: undefined,
    image: undefined,
    video: undefined,
  };
}

export const StorageObjectCreateWeb: MessageFns<StorageObjectCreateWeb> = {
  encode(message: StorageObjectCreateWeb, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== '') {
      writer.uint32(10).string(message.name);
    }
    if (message.isPublic !== false) {
      writer.uint32(16).bool(message.isPublic);
    }
    if (message.parent !== undefined) {
      writer.uint32(26).string(message.parent);
    }
    if (message.type !== StorageObjectType.FOLDER) {
      writer.uint32(32).int32(storageObjectTypeToNumber(message.type));
    }
    if (message.file !== undefined) {
      writer.uint32(42).string(message.file);
    }
    if (message.image !== undefined) {
      writer.uint32(50).string(message.image);
    }
    if (message.video !== undefined) {
      writer.uint32(58).string(message.video);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StorageObjectCreateWeb {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStorageObjectCreateWeb();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.isPublic = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.parent = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.type = storageObjectTypeFromJSON(reader.int32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.file = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.image = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.video = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StorageObjectCreateWeb {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : '',
      isPublic: isSet(object.isPublic) ? globalThis.Boolean(object.isPublic) : false,
      parent: isSet(object.parent) ? globalThis.String(object.parent) : undefined,
      type: isSet(object.type) ? storageObjectTypeFromJSON(object.type) : StorageObjectType.FOLDER,
      file: isSet(object.file) ? globalThis.String(object.file) : undefined,
      image: isSet(object.image) ? globalThis.String(object.image) : undefined,
      video: isSet(object.video) ? globalThis.String(object.video) : undefined,
    };
  },

  toJSON(message: StorageObjectCreateWeb): unknown {
    const obj: any = {};
    if (message.name !== '') {
      obj.name = message.name;
    }
    if (message.isPublic !== false) {
      obj.isPublic = message.isPublic;
    }
    if (message.parent !== undefined) {
      obj.parent = message.parent;
    }
    if (message.type !== StorageObjectType.FOLDER) {
      obj.type = storageObjectTypeToJSON(message.type);
    }
    if (message.file !== undefined) {
      obj.file = message.file;
    }
    if (message.image !== undefined) {
      obj.image = message.image;
    }
    if (message.video !== undefined) {
      obj.video = message.video;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StorageObjectCreateWeb>, I>>(
    base?: I,
  ): StorageObjectCreateWeb {
    return StorageObjectCreateWeb.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StorageObjectCreateWeb>, I>>(
    object: I,
  ): StorageObjectCreateWeb {
    const message = createBaseStorageObjectCreateWeb();
    message.name = object.name ?? '';
    message.isPublic = object.isPublic ?? false;
    message.parent = object.parent ?? undefined;
    message.type = object.type ?? StorageObjectType.FOLDER;
    message.file = object.file ?? undefined;
    message.image = object.image ?? undefined;
    message.video = object.video ?? undefined;
    return message;
  },
};

function createBaseStorageObjectUpdateSet(): StorageObjectUpdateSet {
  return { name: undefined, isPublic: undefined, parent: undefined };
}

export const StorageObjectUpdateSet: MessageFns<StorageObjectUpdateSet> = {
  encode(message: StorageObjectUpdateSet, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== undefined) {
      writer.uint32(10).string(message.name);
    }
    if (message.isPublic !== undefined) {
      writer.uint32(16).bool(message.isPublic);
    }
    if (message.parent !== undefined) {
      writer.uint32(26).string(message.parent);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StorageObjectUpdateSet {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStorageObjectUpdateSet();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.isPublic = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.parent = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StorageObjectUpdateSet {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : undefined,
      isPublic: isSet(object.isPublic) ? globalThis.Boolean(object.isPublic) : undefined,
      parent: isSet(object.parent) ? globalThis.String(object.parent) : undefined,
    };
  },

  toJSON(message: StorageObjectUpdateSet): unknown {
    const obj: any = {};
    if (message.name !== undefined) {
      obj.name = message.name;
    }
    if (message.isPublic !== undefined) {
      obj.isPublic = message.isPublic;
    }
    if (message.parent !== undefined) {
      obj.parent = message.parent;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StorageObjectUpdateSet>, I>>(
    base?: I,
  ): StorageObjectUpdateSet {
    return StorageObjectUpdateSet.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StorageObjectUpdateSet>, I>>(
    object: I,
  ): StorageObjectUpdateSet {
    const message = createBaseStorageObjectUpdateSet();
    message.name = object.name ?? undefined;
    message.isPublic = object.isPublic ?? undefined;
    message.parent = object.parent ?? undefined;
    return message;
  },
};

function createBaseStorageObjectUpdate(): StorageObjectUpdate {
  return { set: undefined };
}

export const StorageObjectUpdate: MessageFns<StorageObjectUpdate> = {
  encode(message: StorageObjectUpdate, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.set !== undefined) {
      StorageObjectUpdateSet.encode(message.set, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StorageObjectUpdate {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStorageObjectUpdate();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.set = StorageObjectUpdateSet.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StorageObjectUpdate {
    return { set: isSet(object.set) ? StorageObjectUpdateSet.fromJSON(object.set) : undefined };
  },

  toJSON(message: StorageObjectUpdate): unknown {
    const obj: any = {};
    if (message.set !== undefined) {
      obj.set = StorageObjectUpdateSet.toJSON(message.set);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StorageObjectUpdate>, I>>(base?: I): StorageObjectUpdate {
    return StorageObjectUpdate.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StorageObjectUpdate>, I>>(
    object: I,
  ): StorageObjectUpdate {
    const message = createBaseStorageObjectUpdate();
    message.set =
      object.set !== undefined && object.set !== null
        ? StorageObjectUpdateSet.fromPartial(object.set)
        : undefined;
    return message;
  },
};

function createBaseStorageObjectUpdateOne(): StorageObjectUpdateOne {
  return { query: undefined, update: undefined };
}

export const StorageObjectUpdateOne: MessageFns<StorageObjectUpdateOne> = {
  encode(message: StorageObjectUpdateOne, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.query !== undefined) {
      StorageObjectQuery.encode(message.query, writer.uint32(10).fork()).join();
    }
    if (message.update !== undefined) {
      StorageObjectUpdate.encode(message.update, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StorageObjectUpdateOne {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStorageObjectUpdateOne();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.query = StorageObjectQuery.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.update = StorageObjectUpdate.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StorageObjectUpdateOne {
    return {
      query: isSet(object.query) ? StorageObjectQuery.fromJSON(object.query) : undefined,
      update: isSet(object.update) ? StorageObjectUpdate.fromJSON(object.update) : undefined,
    };
  },

  toJSON(message: StorageObjectUpdateOne): unknown {
    const obj: any = {};
    if (message.query !== undefined) {
      obj.query = StorageObjectQuery.toJSON(message.query);
    }
    if (message.update !== undefined) {
      obj.update = StorageObjectUpdate.toJSON(message.update);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StorageObjectUpdateOne>, I>>(
    base?: I,
  ): StorageObjectUpdateOne {
    return StorageObjectUpdateOne.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StorageObjectUpdateOne>, I>>(
    object: I,
  ): StorageObjectUpdateOne {
    const message = createBaseStorageObjectUpdateOne();
    message.query =
      object.query !== undefined && object.query !== null
        ? StorageObjectQuery.fromPartial(object.query)
        : undefined;
    message.update =
      object.update !== undefined && object.update !== null
        ? StorageObjectUpdate.fromPartial(object.update)
        : undefined;
    return message;
  },
};

function createBaseStorageObjectUpdateById(): StorageObjectUpdateById {
  return { id: '', update: undefined };
}

export const StorageObjectUpdateById: MessageFns<StorageObjectUpdateById> = {
  encode(
    message: StorageObjectUpdateById,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.id !== '') {
      writer.uint32(10).string(message.id);
    }
    if (message.update !== undefined) {
      StorageObjectUpdate.encode(message.update, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StorageObjectUpdateById {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStorageObjectUpdateById();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.update = StorageObjectUpdate.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StorageObjectUpdateById {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : '',
      update: isSet(object.update) ? StorageObjectUpdate.fromJSON(object.update) : undefined,
    };
  },

  toJSON(message: StorageObjectUpdateById): unknown {
    const obj: any = {};
    if (message.id !== '') {
      obj.id = message.id;
    }
    if (message.update !== undefined) {
      obj.update = StorageObjectUpdate.toJSON(message.update);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StorageObjectUpdateById>, I>>(
    base?: I,
  ): StorageObjectUpdateById {
    return StorageObjectUpdateById.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StorageObjectUpdateById>, I>>(
    object: I,
  ): StorageObjectUpdateById {
    const message = createBaseStorageObjectUpdateById();
    message.id = object.id ?? '';
    message.update =
      object.update !== undefined && object.update !== null
        ? StorageObjectUpdate.fromPartial(object.update)
        : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
