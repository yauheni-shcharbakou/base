// @ts-nocheck
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.5
//   protoc               v7.34.0
// source: cv/messages/contact.messages.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { GrpcContact } from "../models/contact";
import "server-only";

export interface GrpcContactQuery {
  id?: string;
  ids: string[];
  name?: string;
  isPublic?: boolean;
}

export interface GrpcContactRequest {
  query?: GrpcContactQuery;
}

export interface GrpcContactList {
  items: GrpcContact[];
}

function createBaseGrpcContactQuery(): GrpcContactQuery {
  return { id: undefined, ids: [], name: undefined, isPublic: undefined };
}

export const GrpcContactQuery: MessageFns<GrpcContactQuery> = {
  encode(message: GrpcContactQuery, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      writer.uint32(10).string(message.id);
    }
    for (const v of message.ids) {
      writer.uint32(18).string(v!);
    }
    if (message.name !== undefined) {
      writer.uint32(26).string(message.name);
    }
    if (message.isPublic !== undefined) {
      writer.uint32(32).bool(message.isPublic);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GrpcContactQuery {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGrpcContactQuery();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.ids.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.isPublic = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GrpcContactQuery {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : undefined,
      ids: globalThis.Array.isArray(object?.ids) ? object.ids.map((e: any) => globalThis.String(e)) : [],
      name: isSet(object.name) ? globalThis.String(object.name) : undefined,
      isPublic: isSet(object.isPublic) ? globalThis.Boolean(object.isPublic) : undefined,
    };
  },

  toJSON(message: GrpcContactQuery): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = message.id;
    }
    if (message.ids?.length) {
      obj.ids = message.ids;
    }
    if (message.name !== undefined) {
      obj.name = message.name;
    }
    if (message.isPublic !== undefined) {
      obj.isPublic = message.isPublic;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GrpcContactQuery>, I>>(base?: I): GrpcContactQuery {
    return GrpcContactQuery.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GrpcContactQuery>, I>>(object: I): GrpcContactQuery {
    const message = createBaseGrpcContactQuery();
    message.id = object.id ?? undefined;
    message.ids = object.ids?.map((e) => e) || [];
    message.name = object.name ?? undefined;
    message.isPublic = object.isPublic ?? undefined;
    return message;
  },
};

function createBaseGrpcContactRequest(): GrpcContactRequest {
  return { query: undefined };
}

export const GrpcContactRequest: MessageFns<GrpcContactRequest> = {
  encode(message: GrpcContactRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.query !== undefined) {
      GrpcContactQuery.encode(message.query, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GrpcContactRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGrpcContactRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.query = GrpcContactQuery.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GrpcContactRequest {
    return { query: isSet(object.query) ? GrpcContactQuery.fromJSON(object.query) : undefined };
  },

  toJSON(message: GrpcContactRequest): unknown {
    const obj: any = {};
    if (message.query !== undefined) {
      obj.query = GrpcContactQuery.toJSON(message.query);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GrpcContactRequest>, I>>(base?: I): GrpcContactRequest {
    return GrpcContactRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GrpcContactRequest>, I>>(object: I): GrpcContactRequest {
    const message = createBaseGrpcContactRequest();
    message.query = (object.query !== undefined && object.query !== null)
      ? GrpcContactQuery.fromPartial(object.query)
      : undefined;
    return message;
  },
};

function createBaseGrpcContactList(): GrpcContactList {
  return { items: [] };
}

export const GrpcContactList: MessageFns<GrpcContactList> = {
  encode(message: GrpcContactList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.items) {
      GrpcContact.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GrpcContactList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGrpcContactList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.items.push(GrpcContact.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GrpcContactList {
    return {
      items: globalThis.Array.isArray(object?.items) ? object.items.map((e: any) => GrpcContact.fromJSON(e)) : [],
    };
  },

  toJSON(message: GrpcContactList): unknown {
    const obj: any = {};
    if (message.items?.length) {
      obj.items = message.items.map((e) => GrpcContact.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GrpcContactList>, I>>(base?: I): GrpcContactList {
    return GrpcContactList.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GrpcContactList>, I>>(object: I): GrpcContactList {
    const message = createBaseGrpcContactList();
    message.items = object.items?.map((e) => GrpcContact.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
