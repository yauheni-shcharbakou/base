// @ts-nocheck
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               v7.34.0
// source: cv/models/cv.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from '@bufbuild/protobuf/wire';
import { GrpcTimestamp } from '../../google/protobuf/timestamp';
import 'server-only';

export interface GrpcCv {
  id: string;
  createdAt: Date;
  updatedAt?: Date;
  job: string;
  contacts: string[];
  certificates: string[];
  languages: string[];
  workExperience: string[];
  education: string[];
  personalProjects: string[];
  skills: string[];
  alias: string;
  isPrimary: boolean;
  fullName: string;
  summary: string[];
}

function createBaseGrpcCv(): GrpcCv {
  return {
    id: '',
    createdAt: undefined,
    updatedAt: undefined,
    job: '',
    contacts: [],
    certificates: [],
    languages: [],
    workExperience: [],
    education: [],
    personalProjects: [],
    skills: [],
    alias: '',
    isPrimary: false,
    fullName: '',
    summary: [],
  };
}

export const GrpcCv: MessageFns<GrpcCv> = {
  encode(message: GrpcCv, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== '') {
      writer.uint32(10).string(message.id);
    }
    if (message.createdAt !== undefined) {
      GrpcTimestamp.encode(toTimestamp(message.createdAt), writer.uint32(18).fork()).join();
    }
    if (message.updatedAt !== undefined) {
      GrpcTimestamp.encode(toTimestamp(message.updatedAt), writer.uint32(26).fork()).join();
    }
    if (message.job !== '') {
      writer.uint32(34).string(message.job);
    }
    for (const v of message.contacts) {
      writer.uint32(42).string(v!);
    }
    for (const v of message.certificates) {
      writer.uint32(50).string(v!);
    }
    for (const v of message.languages) {
      writer.uint32(58).string(v!);
    }
    for (const v of message.workExperience) {
      writer.uint32(66).string(v!);
    }
    for (const v of message.education) {
      writer.uint32(74).string(v!);
    }
    for (const v of message.personalProjects) {
      writer.uint32(82).string(v!);
    }
    for (const v of message.skills) {
      writer.uint32(90).string(v!);
    }
    if (message.alias !== '') {
      writer.uint32(98).string(message.alias);
    }
    if (message.isPrimary !== false) {
      writer.uint32(104).bool(message.isPrimary);
    }
    if (message.fullName !== '') {
      writer.uint32(114).string(message.fullName);
    }
    for (const v of message.summary) {
      writer.uint32(122).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GrpcCv {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGrpcCv();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.createdAt = fromTimestamp(GrpcTimestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.updatedAt = fromTimestamp(GrpcTimestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.job = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.contacts.push(reader.string());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.certificates.push(reader.string());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.languages.push(reader.string());
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.workExperience.push(reader.string());
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.education.push(reader.string());
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.personalProjects.push(reader.string());
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.skills.push(reader.string());
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.alias = reader.string();
          continue;
        }
        case 13: {
          if (tag !== 104) {
            break;
          }

          message.isPrimary = reader.bool();
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.fullName = reader.string();
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }

          message.summary.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GrpcCv {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : '',
      createdAt: isSet(object.createdAt) ? fromJsonTimestamp(object.createdAt) : undefined,
      updatedAt: isSet(object.updatedAt) ? fromJsonTimestamp(object.updatedAt) : undefined,
      job: isSet(object.job) ? globalThis.String(object.job) : '',
      contacts: globalThis.Array.isArray(object?.contacts)
        ? object.contacts.map((e: any) => globalThis.String(e))
        : [],
      certificates: globalThis.Array.isArray(object?.certificates)
        ? object.certificates.map((e: any) => globalThis.String(e))
        : [],
      languages: globalThis.Array.isArray(object?.languages)
        ? object.languages.map((e: any) => globalThis.String(e))
        : [],
      workExperience: globalThis.Array.isArray(object?.workExperience)
        ? object.workExperience.map((e: any) => globalThis.String(e))
        : [],
      education: globalThis.Array.isArray(object?.education)
        ? object.education.map((e: any) => globalThis.String(e))
        : [],
      personalProjects: globalThis.Array.isArray(object?.personalProjects)
        ? object.personalProjects.map((e: any) => globalThis.String(e))
        : [],
      skills: globalThis.Array.isArray(object?.skills)
        ? object.skills.map((e: any) => globalThis.String(e))
        : [],
      alias: isSet(object.alias) ? globalThis.String(object.alias) : '',
      isPrimary: isSet(object.isPrimary) ? globalThis.Boolean(object.isPrimary) : false,
      fullName: isSet(object.fullName) ? globalThis.String(object.fullName) : '',
      summary: globalThis.Array.isArray(object?.summary)
        ? object.summary.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: GrpcCv): unknown {
    const obj: any = {};
    if (message.id !== '') {
      obj.id = message.id;
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.updatedAt !== undefined) {
      obj.updatedAt = message.updatedAt.toISOString();
    }
    if (message.job !== '') {
      obj.job = message.job;
    }
    if (message.contacts?.length) {
      obj.contacts = message.contacts;
    }
    if (message.certificates?.length) {
      obj.certificates = message.certificates;
    }
    if (message.languages?.length) {
      obj.languages = message.languages;
    }
    if (message.workExperience?.length) {
      obj.workExperience = message.workExperience;
    }
    if (message.education?.length) {
      obj.education = message.education;
    }
    if (message.personalProjects?.length) {
      obj.personalProjects = message.personalProjects;
    }
    if (message.skills?.length) {
      obj.skills = message.skills;
    }
    if (message.alias !== '') {
      obj.alias = message.alias;
    }
    if (message.isPrimary !== false) {
      obj.isPrimary = message.isPrimary;
    }
    if (message.fullName !== '') {
      obj.fullName = message.fullName;
    }
    if (message.summary?.length) {
      obj.summary = message.summary;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GrpcCv>, I>>(base?: I): GrpcCv {
    return GrpcCv.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GrpcCv>, I>>(object: I): GrpcCv {
    const message = createBaseGrpcCv();
    message.id = object.id ?? '';
    message.createdAt = object.createdAt ?? undefined;
    message.updatedAt = object.updatedAt ?? undefined;
    message.job = object.job ?? '';
    message.contacts = object.contacts?.map((e) => e) || [];
    message.certificates = object.certificates?.map((e) => e) || [];
    message.languages = object.languages?.map((e) => e) || [];
    message.workExperience = object.workExperience?.map((e) => e) || [];
    message.education = object.education?.map((e) => e) || [];
    message.personalProjects = object.personalProjects?.map((e) => e) || [];
    message.skills = object.skills?.map((e) => e) || [];
    message.alias = object.alias ?? '';
    message.isPrimary = object.isPrimary ?? false;
    message.fullName = object.fullName ?? '';
    message.summary = object.summary?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): GrpcTimestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: GrpcTimestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === 'string') {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(GrpcTimestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
