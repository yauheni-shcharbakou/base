// @ts-nocheck
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.5
//   protoc               v7.34.0
// source: cv/models/project.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
    GrpcEmployeeRole,
    grpcEmployeeRoleFromJSON,
    grpcEmployeeRoleToJSON,
    grpcEmployeeRoleToNumber,
} from "../../common/enums";
import { GrpcTimestamp } from "../../google/protobuf/timestamp";
import "server-only";

export interface GrpcProject {
  id: string;
  createdAt: Date;
  updatedAt?: Date;
  name?: string;
  link?: string;
  repo?: string;
  domain: string;
  role: GrpcEmployeeRole;
  isPersonal: boolean;
  responsibilities: string[];
  stack: string[];
}

function createBaseGrpcProject(): GrpcProject {
  return {
    id: "",
    createdAt: undefined,
    updatedAt: undefined,
    name: undefined,
    link: undefined,
    repo: undefined,
    domain: "",
    role: GrpcEmployeeRole.BACKEND_DEVELOPER,
    isPersonal: false,
    responsibilities: [],
    stack: [],
  };
}

export const GrpcProject: MessageFns<GrpcProject> = {
  encode(message: GrpcProject, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.createdAt !== undefined) {
      GrpcTimestamp.encode(toTimestamp(message.createdAt), writer.uint32(18).fork()).join();
    }
    if (message.updatedAt !== undefined) {
      GrpcTimestamp.encode(toTimestamp(message.updatedAt), writer.uint32(26).fork()).join();
    }
    if (message.name !== undefined) {
      writer.uint32(34).string(message.name);
    }
    if (message.link !== undefined) {
      writer.uint32(42).string(message.link);
    }
    if (message.repo !== undefined) {
      writer.uint32(50).string(message.repo);
    }
    if (message.domain !== "") {
      writer.uint32(58).string(message.domain);
    }
    if (message.role !== GrpcEmployeeRole.BACKEND_DEVELOPER) {
      writer.uint32(64).int32(grpcEmployeeRoleToNumber(message.role));
    }
    if (message.isPersonal !== false) {
      writer.uint32(72).bool(message.isPersonal);
    }
    for (const v of message.responsibilities) {
      writer.uint32(82).string(v!);
    }
    for (const v of message.stack) {
      writer.uint32(90).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GrpcProject {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGrpcProject();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.createdAt = fromTimestamp(GrpcTimestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.updatedAt = fromTimestamp(GrpcTimestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.link = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.repo = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.domain = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.role = grpcEmployeeRoleFromJSON(reader.int32());
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.isPersonal = reader.bool();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.responsibilities.push(reader.string());
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.stack.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GrpcProject {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      createdAt: isSet(object.createdAt) ? fromJsonTimestamp(object.createdAt) : undefined,
      updatedAt: isSet(object.updatedAt) ? fromJsonTimestamp(object.updatedAt) : undefined,
      name: isSet(object.name) ? globalThis.String(object.name) : undefined,
      link: isSet(object.link) ? globalThis.String(object.link) : undefined,
      repo: isSet(object.repo) ? globalThis.String(object.repo) : undefined,
      domain: isSet(object.domain) ? globalThis.String(object.domain) : "",
      role: isSet(object.role) ? grpcEmployeeRoleFromJSON(object.role) : GrpcEmployeeRole.BACKEND_DEVELOPER,
      isPersonal: isSet(object.isPersonal) ? globalThis.Boolean(object.isPersonal) : false,
      responsibilities: globalThis.Array.isArray(object?.responsibilities)
        ? object.responsibilities.map((e: any) => globalThis.String(e))
        : [],
      stack: globalThis.Array.isArray(object?.stack) ? object.stack.map((e: any) => globalThis.String(e)) : [],
    };
  },

  toJSON(message: GrpcProject): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.updatedAt !== undefined) {
      obj.updatedAt = message.updatedAt.toISOString();
    }
    if (message.name !== undefined) {
      obj.name = message.name;
    }
    if (message.link !== undefined) {
      obj.link = message.link;
    }
    if (message.repo !== undefined) {
      obj.repo = message.repo;
    }
    if (message.domain !== "") {
      obj.domain = message.domain;
    }
    if (message.role !== GrpcEmployeeRole.BACKEND_DEVELOPER) {
      obj.role = grpcEmployeeRoleToJSON(message.role);
    }
    if (message.isPersonal !== false) {
      obj.isPersonal = message.isPersonal;
    }
    if (message.responsibilities?.length) {
      obj.responsibilities = message.responsibilities;
    }
    if (message.stack?.length) {
      obj.stack = message.stack;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GrpcProject>, I>>(base?: I): GrpcProject {
    return GrpcProject.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GrpcProject>, I>>(object: I): GrpcProject {
    const message = createBaseGrpcProject();
    message.id = object.id ?? "";
    message.createdAt = object.createdAt ?? undefined;
    message.updatedAt = object.updatedAt ?? undefined;
    message.name = object.name ?? undefined;
    message.link = object.link ?? undefined;
    message.repo = object.repo ?? undefined;
    message.domain = object.domain ?? "";
    message.role = object.role ?? GrpcEmployeeRole.BACKEND_DEVELOPER;
    message.isPersonal = object.isPersonal ?? false;
    message.responsibilities = object.responsibilities?.map((e) => e) || [];
    message.stack = object.stack?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): GrpcTimestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: GrpcTimestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(GrpcTimestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
