// @ts-nocheck
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.5
//   protoc               v7.34.0
// source: cv/models/language.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { GrpcTimestamp } from "../../google/protobuf/timestamp";
import "server-only";

export enum GrpcLanguageLevel {
  A1 = "A1",
  A2 = "A2",
  B1 = "B1",
  B2 = "B2",
  C1 = "C1",
  C2 = "C2",
}

export function grpcLanguageLevelFromJSON(object: any): GrpcLanguageLevel {
  switch (object) {
    case 0:
    case "A1":
      return GrpcLanguageLevel.A1;
    case 1:
    case "A2":
      return GrpcLanguageLevel.A2;
    case 2:
    case "B1":
      return GrpcLanguageLevel.B1;
    case 3:
    case "B2":
      return GrpcLanguageLevel.B2;
    case 4:
    case "C1":
      return GrpcLanguageLevel.C1;
    case 5:
    case "C2":
      return GrpcLanguageLevel.C2;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum GrpcLanguageLevel");
  }
}

export function grpcLanguageLevelToJSON(object: GrpcLanguageLevel): string {
  switch (object) {
    case GrpcLanguageLevel.A1:
      return "A1";
    case GrpcLanguageLevel.A2:
      return "A2";
    case GrpcLanguageLevel.B1:
      return "B1";
    case GrpcLanguageLevel.B2:
      return "B2";
    case GrpcLanguageLevel.C1:
      return "C1";
    case GrpcLanguageLevel.C2:
      return "C2";
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum GrpcLanguageLevel");
  }
}

export function grpcLanguageLevelToNumber(object: GrpcLanguageLevel): number {
  switch (object) {
    case GrpcLanguageLevel.A1:
      return 0;
    case GrpcLanguageLevel.A2:
      return 1;
    case GrpcLanguageLevel.B1:
      return 2;
    case GrpcLanguageLevel.B2:
      return 3;
    case GrpcLanguageLevel.C1:
      return 4;
    case GrpcLanguageLevel.C2:
      return 5;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum GrpcLanguageLevel");
  }
}

export enum GrpcLanguageLevelDescription {
  BEGINNER = "BEGINNER",
  ELEMENTARY = "ELEMENTARY",
  INTERMEDIATE = "INTERMEDIATE",
  UPPER_INTERMEDIATE = "UPPER_INTERMEDIATE",
  ADVANCED = "ADVANCED",
  FLUENT = "FLUENT",
}

export function grpcLanguageLevelDescriptionFromJSON(object: any): GrpcLanguageLevelDescription {
  switch (object) {
    case 0:
    case "BEGINNER":
      return GrpcLanguageLevelDescription.BEGINNER;
    case 1:
    case "ELEMENTARY":
      return GrpcLanguageLevelDescription.ELEMENTARY;
    case 2:
    case "INTERMEDIATE":
      return GrpcLanguageLevelDescription.INTERMEDIATE;
    case 3:
    case "UPPER_INTERMEDIATE":
      return GrpcLanguageLevelDescription.UPPER_INTERMEDIATE;
    case 4:
    case "ADVANCED":
      return GrpcLanguageLevelDescription.ADVANCED;
    case 5:
    case "FLUENT":
      return GrpcLanguageLevelDescription.FLUENT;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum GrpcLanguageLevelDescription");
  }
}

export function grpcLanguageLevelDescriptionToJSON(object: GrpcLanguageLevelDescription): string {
  switch (object) {
    case GrpcLanguageLevelDescription.BEGINNER:
      return "BEGINNER";
    case GrpcLanguageLevelDescription.ELEMENTARY:
      return "ELEMENTARY";
    case GrpcLanguageLevelDescription.INTERMEDIATE:
      return "INTERMEDIATE";
    case GrpcLanguageLevelDescription.UPPER_INTERMEDIATE:
      return "UPPER_INTERMEDIATE";
    case GrpcLanguageLevelDescription.ADVANCED:
      return "ADVANCED";
    case GrpcLanguageLevelDescription.FLUENT:
      return "FLUENT";
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum GrpcLanguageLevelDescription");
  }
}

export function grpcLanguageLevelDescriptionToNumber(object: GrpcLanguageLevelDescription): number {
  switch (object) {
    case GrpcLanguageLevelDescription.BEGINNER:
      return 0;
    case GrpcLanguageLevelDescription.ELEMENTARY:
      return 1;
    case GrpcLanguageLevelDescription.INTERMEDIATE:
      return 2;
    case GrpcLanguageLevelDescription.UPPER_INTERMEDIATE:
      return 3;
    case GrpcLanguageLevelDescription.ADVANCED:
      return 4;
    case GrpcLanguageLevelDescription.FLUENT:
      return 5;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum GrpcLanguageLevelDescription");
  }
}

export interface GrpcLanguage {
  id: string;
  createdAt: Date;
  updatedAt?: Date;
  name: string;
  level: GrpcLanguageLevel;
  description: GrpcLanguageLevelDescription;
}

function createBaseGrpcLanguage(): GrpcLanguage {
  return {
    id: "",
    createdAt: undefined,
    updatedAt: undefined,
    name: "",
    level: GrpcLanguageLevel.A1,
    description: GrpcLanguageLevelDescription.BEGINNER,
  };
}

export const GrpcLanguage: MessageFns<GrpcLanguage> = {
  encode(message: GrpcLanguage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.createdAt !== undefined) {
      GrpcTimestamp.encode(toTimestamp(message.createdAt), writer.uint32(18).fork()).join();
    }
    if (message.updatedAt !== undefined) {
      GrpcTimestamp.encode(toTimestamp(message.updatedAt), writer.uint32(26).fork()).join();
    }
    if (message.name !== "") {
      writer.uint32(34).string(message.name);
    }
    if (message.level !== GrpcLanguageLevel.A1) {
      writer.uint32(40).int32(grpcLanguageLevelToNumber(message.level));
    }
    if (message.description !== GrpcLanguageLevelDescription.BEGINNER) {
      writer.uint32(48).int32(grpcLanguageLevelDescriptionToNumber(message.description));
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GrpcLanguage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGrpcLanguage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.createdAt = fromTimestamp(GrpcTimestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.updatedAt = fromTimestamp(GrpcTimestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.level = grpcLanguageLevelFromJSON(reader.int32());
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.description = grpcLanguageLevelDescriptionFromJSON(reader.int32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GrpcLanguage {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      createdAt: isSet(object.createdAt) ? fromJsonTimestamp(object.createdAt) : undefined,
      updatedAt: isSet(object.updatedAt) ? fromJsonTimestamp(object.updatedAt) : undefined,
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      level: isSet(object.level) ? grpcLanguageLevelFromJSON(object.level) : GrpcLanguageLevel.A1,
      description: isSet(object.description)
        ? grpcLanguageLevelDescriptionFromJSON(object.description)
        : GrpcLanguageLevelDescription.BEGINNER,
    };
  },

  toJSON(message: GrpcLanguage): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.updatedAt !== undefined) {
      obj.updatedAt = message.updatedAt.toISOString();
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.level !== GrpcLanguageLevel.A1) {
      obj.level = grpcLanguageLevelToJSON(message.level);
    }
    if (message.description !== GrpcLanguageLevelDescription.BEGINNER) {
      obj.description = grpcLanguageLevelDescriptionToJSON(message.description);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GrpcLanguage>, I>>(base?: I): GrpcLanguage {
    return GrpcLanguage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GrpcLanguage>, I>>(object: I): GrpcLanguage {
    const message = createBaseGrpcLanguage();
    message.id = object.id ?? "";
    message.createdAt = object.createdAt ?? undefined;
    message.updatedAt = object.updatedAt ?? undefined;
    message.name = object.name ?? "";
    message.level = object.level ?? GrpcLanguageLevel.A1;
    message.description = object.description ?? GrpcLanguageLevelDescription.BEGINNER;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): GrpcTimestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: GrpcTimestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(GrpcTimestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
