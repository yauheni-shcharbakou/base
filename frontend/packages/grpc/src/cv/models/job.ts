// @ts-nocheck
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.5
//   protoc               v7.34.0
// source: cv/models/job.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { GrpcTimestamp } from "../../google/protobuf/timestamp";
import "server-only";

export enum GrpcJobType {
  BACKEND = "BACKEND",
  FRONTEND = "FRONTEND",
  FULL_STACK = "FULL_STACK",
}

export function grpcJobTypeFromJSON(object: any): GrpcJobType {
  switch (object) {
    case 0:
    case "BACKEND":
      return GrpcJobType.BACKEND;
    case 1:
    case "FRONTEND":
      return GrpcJobType.FRONTEND;
    case 2:
    case "FULL_STACK":
      return GrpcJobType.FULL_STACK;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum GrpcJobType");
  }
}

export function grpcJobTypeToJSON(object: GrpcJobType): string {
  switch (object) {
    case GrpcJobType.BACKEND:
      return "BACKEND";
    case GrpcJobType.FRONTEND:
      return "FRONTEND";
    case GrpcJobType.FULL_STACK:
      return "FULL_STACK";
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum GrpcJobType");
  }
}

export function grpcJobTypeToNumber(object: GrpcJobType): number {
  switch (object) {
    case GrpcJobType.BACKEND:
      return 0;
    case GrpcJobType.FRONTEND:
      return 1;
    case GrpcJobType.FULL_STACK:
      return 2;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum GrpcJobType");
  }
}

export interface GrpcJob {
  id: string;
  createdAt: Date;
  updatedAt?: Date;
  title: string;
  isPublic: boolean;
  type: GrpcJobType;
}

function createBaseGrpcJob(): GrpcJob {
  return { id: "", createdAt: undefined, updatedAt: undefined, title: "", isPublic: false, type: GrpcJobType.BACKEND };
}

export const GrpcJob: MessageFns<GrpcJob> = {
  encode(message: GrpcJob, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.createdAt !== undefined) {
      GrpcTimestamp.encode(toTimestamp(message.createdAt), writer.uint32(18).fork()).join();
    }
    if (message.updatedAt !== undefined) {
      GrpcTimestamp.encode(toTimestamp(message.updatedAt), writer.uint32(26).fork()).join();
    }
    if (message.title !== "") {
      writer.uint32(34).string(message.title);
    }
    if (message.isPublic !== false) {
      writer.uint32(40).bool(message.isPublic);
    }
    if (message.type !== GrpcJobType.BACKEND) {
      writer.uint32(48).int32(grpcJobTypeToNumber(message.type));
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GrpcJob {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGrpcJob();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.createdAt = fromTimestamp(GrpcTimestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.updatedAt = fromTimestamp(GrpcTimestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.isPublic = reader.bool();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.type = grpcJobTypeFromJSON(reader.int32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GrpcJob {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      createdAt: isSet(object.createdAt) ? fromJsonTimestamp(object.createdAt) : undefined,
      updatedAt: isSet(object.updatedAt) ? fromJsonTimestamp(object.updatedAt) : undefined,
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      isPublic: isSet(object.isPublic) ? globalThis.Boolean(object.isPublic) : false,
      type: isSet(object.type) ? grpcJobTypeFromJSON(object.type) : GrpcJobType.BACKEND,
    };
  },

  toJSON(message: GrpcJob): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.updatedAt !== undefined) {
      obj.updatedAt = message.updatedAt.toISOString();
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.isPublic !== false) {
      obj.isPublic = message.isPublic;
    }
    if (message.type !== GrpcJobType.BACKEND) {
      obj.type = grpcJobTypeToJSON(message.type);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GrpcJob>, I>>(base?: I): GrpcJob {
    return GrpcJob.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GrpcJob>, I>>(object: I): GrpcJob {
    const message = createBaseGrpcJob();
    message.id = object.id ?? "";
    message.createdAt = object.createdAt ?? undefined;
    message.updatedAt = object.updatedAt ?? undefined;
    message.title = object.title ?? "";
    message.isPublic = object.isPublic ?? false;
    message.type = object.type ?? GrpcJobType.BACKEND;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): GrpcTimestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: GrpcTimestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(GrpcTimestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
