// @ts-nocheck
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               v7.34.0
// source: auth/messages/temp-code.messages.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from '@bufbuild/protobuf/wire';
import { GrpcTempCode } from '../models/temp-code';
import 'server-only';

export interface GrpcTempCodeGetListResponse {
  items: GrpcTempCode[];
  total: number;
}

export interface GrpcTempCodeCreate {
  user: string;
}

function createBaseGrpcTempCodeGetListResponse(): GrpcTempCodeGetListResponse {
  return { items: [], total: 0 };
}

export const GrpcTempCodeGetListResponse: MessageFns<GrpcTempCodeGetListResponse> = {
  encode(
    message: GrpcTempCodeGetListResponse,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    for (const v of message.items) {
      GrpcTempCode.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.total !== 0) {
      writer.uint32(16).int32(message.total);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GrpcTempCodeGetListResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGrpcTempCodeGetListResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.items.push(GrpcTempCode.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.total = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GrpcTempCodeGetListResponse {
    return {
      items: globalThis.Array.isArray(object?.items)
        ? object.items.map((e: any) => GrpcTempCode.fromJSON(e))
        : [],
      total: isSet(object.total) ? globalThis.Number(object.total) : 0,
    };
  },

  toJSON(message: GrpcTempCodeGetListResponse): unknown {
    const obj: any = {};
    if (message.items?.length) {
      obj.items = message.items.map((e) => GrpcTempCode.toJSON(e));
    }
    if (message.total !== 0) {
      obj.total = Math.round(message.total);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GrpcTempCodeGetListResponse>, I>>(
    base?: I,
  ): GrpcTempCodeGetListResponse {
    return GrpcTempCodeGetListResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GrpcTempCodeGetListResponse>, I>>(
    object: I,
  ): GrpcTempCodeGetListResponse {
    const message = createBaseGrpcTempCodeGetListResponse();
    message.items = object.items?.map((e) => GrpcTempCode.fromPartial(e)) || [];
    message.total = object.total ?? 0;
    return message;
  },
};

function createBaseGrpcTempCodeCreate(): GrpcTempCodeCreate {
  return { user: '' };
}

export const GrpcTempCodeCreate: MessageFns<GrpcTempCodeCreate> = {
  encode(message: GrpcTempCodeCreate, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.user !== '') {
      writer.uint32(10).string(message.user);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GrpcTempCodeCreate {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGrpcTempCodeCreate();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.user = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GrpcTempCodeCreate {
    return { user: isSet(object.user) ? globalThis.String(object.user) : '' };
  },

  toJSON(message: GrpcTempCodeCreate): unknown {
    const obj: any = {};
    if (message.user !== '') {
      obj.user = message.user;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GrpcTempCodeCreate>, I>>(base?: I): GrpcTempCodeCreate {
    return GrpcTempCodeCreate.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GrpcTempCodeCreate>, I>>(object: I): GrpcTempCodeCreate {
    const message = createBaseGrpcTempCodeCreate();
    message.user = object.user ?? '';
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
