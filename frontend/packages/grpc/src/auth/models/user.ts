// @ts-nocheck
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.5
//   protoc               v7.34.0
// source: auth/models/user.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { GrpcTimestamp } from "../../google/protobuf/timestamp";
import "server-only";

export enum GrpcUserRole {
  ADMIN = "ADMIN",
  USER = "USER",
}

export function grpcUserRoleFromJSON(object: any): GrpcUserRole {
  switch (object) {
    case 0:
    case "ADMIN":
      return GrpcUserRole.ADMIN;
    case 1:
    case "USER":
      return GrpcUserRole.USER;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum GrpcUserRole");
  }
}

export function grpcUserRoleToJSON(object: GrpcUserRole): string {
  switch (object) {
    case GrpcUserRole.ADMIN:
      return "ADMIN";
    case GrpcUserRole.USER:
      return "USER";
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum GrpcUserRole");
  }
}

export function grpcUserRoleToNumber(object: GrpcUserRole): number {
  switch (object) {
    case GrpcUserRole.ADMIN:
      return 0;
    case GrpcUserRole.USER:
      return 1;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum GrpcUserRole");
  }
}

export interface GrpcUser {
  id: string;
  createdAt: Date;
  updatedAt?: Date;
  email: string;
  role: GrpcUserRole;
}

function createBaseGrpcUser(): GrpcUser {
  return { id: "", createdAt: undefined, updatedAt: undefined, email: "", role: GrpcUserRole.ADMIN };
}

export const GrpcUser: MessageFns<GrpcUser> = {
  encode(message: GrpcUser, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.createdAt !== undefined) {
      GrpcTimestamp.encode(toTimestamp(message.createdAt), writer.uint32(18).fork()).join();
    }
    if (message.updatedAt !== undefined) {
      GrpcTimestamp.encode(toTimestamp(message.updatedAt), writer.uint32(26).fork()).join();
    }
    if (message.email !== "") {
      writer.uint32(34).string(message.email);
    }
    if (message.role !== GrpcUserRole.ADMIN) {
      writer.uint32(40).int32(grpcUserRoleToNumber(message.role));
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GrpcUser {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGrpcUser();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.createdAt = fromTimestamp(GrpcTimestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.updatedAt = fromTimestamp(GrpcTimestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.email = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.role = grpcUserRoleFromJSON(reader.int32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GrpcUser {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      createdAt: isSet(object.createdAt) ? fromJsonTimestamp(object.createdAt) : undefined,
      updatedAt: isSet(object.updatedAt) ? fromJsonTimestamp(object.updatedAt) : undefined,
      email: isSet(object.email) ? globalThis.String(object.email) : "",
      role: isSet(object.role) ? grpcUserRoleFromJSON(object.role) : GrpcUserRole.ADMIN,
    };
  },

  toJSON(message: GrpcUser): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.updatedAt !== undefined) {
      obj.updatedAt = message.updatedAt.toISOString();
    }
    if (message.email !== "") {
      obj.email = message.email;
    }
    if (message.role !== GrpcUserRole.ADMIN) {
      obj.role = grpcUserRoleToJSON(message.role);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GrpcUser>, I>>(base?: I): GrpcUser {
    return GrpcUser.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GrpcUser>, I>>(object: I): GrpcUser {
    const message = createBaseGrpcUser();
    message.id = object.id ?? "";
    message.createdAt = object.createdAt ?? undefined;
    message.updatedAt = object.updatedAt ?? undefined;
    message.email = object.email ?? "";
    message.role = object.role ?? GrpcUserRole.ADMIN;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): GrpcTimestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: GrpcTimestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(GrpcTimestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
