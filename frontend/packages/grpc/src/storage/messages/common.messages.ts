// @ts-nocheck
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               v7.34.0
// source: storage/messages/common.messages.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from '@bufbuild/protobuf/wire';
import 'server-only';

export interface GrpcDownloadData {
  url: string;
  fileName: string;
}

export interface GrpcUrlMap {
  items: Map<string, string>;
}

export interface GrpcUrlMap_ItemsEntry {
  key: string;
  value: string;
}

export interface GrpcDownloadMap {
  items: Map<string, GrpcDownloadData>;
}

export interface GrpcDownloadMap_ItemsEntry {
  key: string;
  value: GrpcDownloadData;
}

export interface GrpcUploadRequest {
  id?: string;
  chunk?: Uint8Array<ArrayBufferLike>;
}

function createBaseGrpcDownloadData(): GrpcDownloadData {
  return { url: '', fileName: '' };
}

export const GrpcDownloadData: MessageFns<GrpcDownloadData> = {
  encode(message: GrpcDownloadData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.url !== '') {
      writer.uint32(10).string(message.url);
    }
    if (message.fileName !== '') {
      writer.uint32(18).string(message.fileName);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GrpcDownloadData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGrpcDownloadData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.url = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.fileName = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GrpcDownloadData {
    return {
      url: isSet(object.url) ? globalThis.String(object.url) : '',
      fileName: isSet(object.fileName) ? globalThis.String(object.fileName) : '',
    };
  },

  toJSON(message: GrpcDownloadData): unknown {
    const obj: any = {};
    if (message.url !== '') {
      obj.url = message.url;
    }
    if (message.fileName !== '') {
      obj.fileName = message.fileName;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GrpcDownloadData>, I>>(base?: I): GrpcDownloadData {
    return GrpcDownloadData.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GrpcDownloadData>, I>>(object: I): GrpcDownloadData {
    const message = createBaseGrpcDownloadData();
    message.url = object.url ?? '';
    message.fileName = object.fileName ?? '';
    return message;
  },
};

function createBaseGrpcUrlMap(): GrpcUrlMap {
  return { items: new Map() };
}

export const GrpcUrlMap: MessageFns<GrpcUrlMap> = {
  encode(message: GrpcUrlMap, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    message.items.forEach((value, key) => {
      GrpcUrlMap_ItemsEntry.encode({ key: key as any, value }, writer.uint32(10).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GrpcUrlMap {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGrpcUrlMap();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          const entry1 = GrpcUrlMap_ItemsEntry.decode(reader, reader.uint32());
          if (entry1.value !== undefined) {
            message.items.set(entry1.key, entry1.value);
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GrpcUrlMap {
    return {
      items: isObject(object.items)
        ? (globalThis.Object.entries(object.items) as [string, any][]).reduce(
            (acc: Map<string, string>, [key, value]: [string, any]) => {
              acc.set(key, globalThis.String(value));
              return acc;
            },
            new Map(),
          )
        : new Map(),
    };
  },

  toJSON(message: GrpcUrlMap): unknown {
    const obj: any = {};
    if (message.items?.size) {
      obj.items = {};
      message.items.forEach((v, k) => {
        obj.items[k] = v;
      });
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GrpcUrlMap>, I>>(base?: I): GrpcUrlMap {
    return GrpcUrlMap.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GrpcUrlMap>, I>>(object: I): GrpcUrlMap {
    const message = createBaseGrpcUrlMap();
    message.items = (() => {
      const m = new Map();
      ((object.items as Map<string, string>) ?? new Map()).forEach((value, key) => {
        if (value !== undefined) {
          m.set(key, globalThis.String(value));
        }
      });
      return m;
    })();
    return message;
  },
};

function createBaseGrpcUrlMap_ItemsEntry(): GrpcUrlMap_ItemsEntry {
  return { key: '', value: '' };
}

export const GrpcUrlMap_ItemsEntry: MessageFns<GrpcUrlMap_ItemsEntry> = {
  encode(message: GrpcUrlMap_ItemsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== '') {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== '') {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GrpcUrlMap_ItemsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGrpcUrlMap_ItemsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GrpcUrlMap_ItemsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : '',
      value: isSet(object.value) ? globalThis.String(object.value) : '',
    };
  },

  toJSON(message: GrpcUrlMap_ItemsEntry): unknown {
    const obj: any = {};
    if (message.key !== '') {
      obj.key = message.key;
    }
    if (message.value !== '') {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GrpcUrlMap_ItemsEntry>, I>>(base?: I): GrpcUrlMap_ItemsEntry {
    return GrpcUrlMap_ItemsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GrpcUrlMap_ItemsEntry>, I>>(
    object: I,
  ): GrpcUrlMap_ItemsEntry {
    const message = createBaseGrpcUrlMap_ItemsEntry();
    message.key = object.key ?? '';
    message.value = object.value ?? '';
    return message;
  },
};

function createBaseGrpcDownloadMap(): GrpcDownloadMap {
  return { items: new Map() };
}

export const GrpcDownloadMap: MessageFns<GrpcDownloadMap> = {
  encode(message: GrpcDownloadMap, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    message.items.forEach((value, key) => {
      GrpcDownloadMap_ItemsEntry.encode(
        { key: key as any, value },
        writer.uint32(10).fork(),
      ).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GrpcDownloadMap {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGrpcDownloadMap();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          const entry1 = GrpcDownloadMap_ItemsEntry.decode(reader, reader.uint32());
          if (entry1.value !== undefined) {
            message.items.set(entry1.key, entry1.value);
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GrpcDownloadMap {
    return {
      items: isObject(object.items)
        ? (globalThis.Object.entries(object.items) as [string, any][]).reduce(
            (acc: Map<string, GrpcDownloadData>, [key, value]: [string, any]) => {
              acc.set(key, GrpcDownloadData.fromJSON(value));
              return acc;
            },
            new Map(),
          )
        : new Map(),
    };
  },

  toJSON(message: GrpcDownloadMap): unknown {
    const obj: any = {};
    if (message.items?.size) {
      obj.items = {};
      message.items.forEach((v, k) => {
        obj.items[k] = GrpcDownloadData.toJSON(v);
      });
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GrpcDownloadMap>, I>>(base?: I): GrpcDownloadMap {
    return GrpcDownloadMap.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GrpcDownloadMap>, I>>(object: I): GrpcDownloadMap {
    const message = createBaseGrpcDownloadMap();
    message.items = (() => {
      const m = new Map();
      ((object.items as Map<string, GrpcDownloadData>) ?? new Map()).forEach((value, key) => {
        if (value !== undefined) {
          m.set(key, GrpcDownloadData.fromPartial(value));
        }
      });
      return m;
    })();
    return message;
  },
};

function createBaseGrpcDownloadMap_ItemsEntry(): GrpcDownloadMap_ItemsEntry {
  return { key: '', value: undefined };
}

export const GrpcDownloadMap_ItemsEntry: MessageFns<GrpcDownloadMap_ItemsEntry> = {
  encode(
    message: GrpcDownloadMap_ItemsEntry,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.key !== '') {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== undefined) {
      GrpcDownloadData.encode(message.value, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GrpcDownloadMap_ItemsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGrpcDownloadMap_ItemsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = GrpcDownloadData.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GrpcDownloadMap_ItemsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : '',
      value: isSet(object.value) ? GrpcDownloadData.fromJSON(object.value) : undefined,
    };
  },

  toJSON(message: GrpcDownloadMap_ItemsEntry): unknown {
    const obj: any = {};
    if (message.key !== '') {
      obj.key = message.key;
    }
    if (message.value !== undefined) {
      obj.value = GrpcDownloadData.toJSON(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GrpcDownloadMap_ItemsEntry>, I>>(
    base?: I,
  ): GrpcDownloadMap_ItemsEntry {
    return GrpcDownloadMap_ItemsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GrpcDownloadMap_ItemsEntry>, I>>(
    object: I,
  ): GrpcDownloadMap_ItemsEntry {
    const message = createBaseGrpcDownloadMap_ItemsEntry();
    message.key = object.key ?? '';
    message.value =
      object.value !== undefined && object.value !== null
        ? GrpcDownloadData.fromPartial(object.value)
        : undefined;
    return message;
  },
};

function createBaseGrpcUploadRequest(): GrpcUploadRequest {
  return { id: undefined, chunk: undefined };
}

export const GrpcUploadRequest: MessageFns<GrpcUploadRequest> = {
  encode(message: GrpcUploadRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      writer.uint32(10).string(message.id);
    }
    if (message.chunk !== undefined) {
      writer.uint32(18).bytes(message.chunk);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GrpcUploadRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGrpcUploadRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.chunk = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GrpcUploadRequest {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : undefined,
      chunk: isSet(object.chunk) ? bytesFromBase64(object.chunk) : undefined,
    };
  },

  toJSON(message: GrpcUploadRequest): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = message.id;
    }
    if (message.chunk !== undefined) {
      obj.chunk = base64FromBytes(message.chunk);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GrpcUploadRequest>, I>>(base?: I): GrpcUploadRequest {
    return GrpcUploadRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GrpcUploadRequest>, I>>(object: I): GrpcUploadRequest {
    const message = createBaseGrpcUploadRequest();
    message.id = object.id ?? undefined;
    message.chunk = object.chunk ?? undefined;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, 'base64'));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString('base64');
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(''));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isObject(value: any): boolean {
  return typeof value === 'object' && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
