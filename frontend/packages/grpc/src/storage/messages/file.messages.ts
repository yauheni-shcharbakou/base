// @ts-nocheck
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.5
//   protoc               v7.34.0
// source: storage/messages/file.messages.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { GrpcTimestamp } from "../../google/protobuf/timestamp";
import {
    GrpcFile,
    GrpcFileUploadStatus,
    grpcFileUploadStatusFromJSON,
    grpcFileUploadStatusToJSON,
    grpcFileUploadStatusToNumber,
} from "../models/file";
import { GrpcStorageObjectManyMetadata, GrpcStorageObjectMetadata } from "./storage-object.messages";
import "server-only";

export interface GrpcFileQuery {
  id?: string;
  ids: string[];
  mimeType?: string;
  mimeTypes: string[];
  userId?: string;
  userIds: string[];
  extension?: string;
  uploadStatus?: GrpcFileUploadStatus;
  uploadStatuses: GrpcFileUploadStatus[];
  createdAfter?: Date;
}

export interface GrpcFileRequest {
  query: GrpcFileQuery;
}

export interface GrpcFileGetListResponse {
  items: GrpcFile[];
  total: number;
}

export interface GrpcFileCreate {
  originalName: string;
  size: number;
  mimeType: string;
}

export interface GrpcFileCreateRequest {
  file: GrpcFileCreate;
  storage?: GrpcStorageObjectMetadata;
}

export interface GrpcFileCreateManyItem {
  file: GrpcFileCreate;
  uploadId: string;
}

export interface GrpcFileCreateManyRequest {
  storage?: GrpcStorageObjectManyMetadata;
  items: GrpcFileCreateManyItem[];
}

export interface GrpcFileCreateManyResponse {
  items: GrpcFile[];
}

export interface GrpcFileUploadResponse {
  entity?: GrpcFile;
  canSendChunks?: boolean;
  ack?: boolean;
}

function createBaseGrpcFileQuery(): GrpcFileQuery {
  return {
    id: undefined,
    ids: [],
    mimeType: undefined,
    mimeTypes: [],
    userId: undefined,
    userIds: [],
    extension: undefined,
    uploadStatus: undefined,
    uploadStatuses: [],
    createdAfter: undefined,
  };
}

export const GrpcFileQuery: MessageFns<GrpcFileQuery> = {
  encode(message: GrpcFileQuery, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      writer.uint32(10).string(message.id);
    }
    for (const v of message.ids) {
      writer.uint32(18).string(v!);
    }
    if (message.mimeType !== undefined) {
      writer.uint32(26).string(message.mimeType);
    }
    for (const v of message.mimeTypes) {
      writer.uint32(34).string(v!);
    }
    if (message.userId !== undefined) {
      writer.uint32(42).string(message.userId);
    }
    for (const v of message.userIds) {
      writer.uint32(50).string(v!);
    }
    if (message.extension !== undefined) {
      writer.uint32(58).string(message.extension);
    }
    if (message.uploadStatus !== undefined) {
      writer.uint32(64).int32(grpcFileUploadStatusToNumber(message.uploadStatus));
    }
    writer.uint32(74).fork();
    for (const v of message.uploadStatuses) {
      writer.int32(grpcFileUploadStatusToNumber(v));
    }
    writer.join();
    if (message.createdAfter !== undefined) {
      GrpcTimestamp.encode(toTimestamp(message.createdAfter), writer.uint32(82).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GrpcFileQuery {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGrpcFileQuery();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.ids.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.mimeType = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.mimeTypes.push(reader.string());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.userIds.push(reader.string());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.extension = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.uploadStatus = grpcFileUploadStatusFromJSON(reader.int32());
          continue;
        }
        case 9: {
          if (tag === 72) {
            message.uploadStatuses.push(grpcFileUploadStatusFromJSON(reader.int32()));

            continue;
          }

          if (tag === 74) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.uploadStatuses.push(grpcFileUploadStatusFromJSON(reader.int32()));
            }

            continue;
          }

          break;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.createdAfter = fromTimestamp(GrpcTimestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GrpcFileQuery {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : undefined,
      ids: globalThis.Array.isArray(object?.ids) ? object.ids.map((e: any) => globalThis.String(e)) : [],
      mimeType: isSet(object.mimeType) ? globalThis.String(object.mimeType) : undefined,
      mimeTypes: globalThis.Array.isArray(object?.mimeTypes)
        ? object.mimeTypes.map((e: any) => globalThis.String(e))
        : [],
      userId: isSet(object.userId) ? globalThis.String(object.userId) : undefined,
      userIds: globalThis.Array.isArray(object?.userIds) ? object.userIds.map((e: any) => globalThis.String(e)) : [],
      extension: isSet(object.extension) ? globalThis.String(object.extension) : undefined,
      uploadStatus: isSet(object.uploadStatus) ? grpcFileUploadStatusFromJSON(object.uploadStatus) : undefined,
      uploadStatuses: globalThis.Array.isArray(object?.uploadStatuses)
        ? object.uploadStatuses.map((e: any) => grpcFileUploadStatusFromJSON(e))
        : [],
      createdAfter: isSet(object.createdAfter) ? fromJsonTimestamp(object.createdAfter) : undefined,
    };
  },

  toJSON(message: GrpcFileQuery): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = message.id;
    }
    if (message.ids?.length) {
      obj.ids = message.ids;
    }
    if (message.mimeType !== undefined) {
      obj.mimeType = message.mimeType;
    }
    if (message.mimeTypes?.length) {
      obj.mimeTypes = message.mimeTypes;
    }
    if (message.userId !== undefined) {
      obj.userId = message.userId;
    }
    if (message.userIds?.length) {
      obj.userIds = message.userIds;
    }
    if (message.extension !== undefined) {
      obj.extension = message.extension;
    }
    if (message.uploadStatus !== undefined) {
      obj.uploadStatus = grpcFileUploadStatusToJSON(message.uploadStatus);
    }
    if (message.uploadStatuses?.length) {
      obj.uploadStatuses = message.uploadStatuses.map((e) => grpcFileUploadStatusToJSON(e));
    }
    if (message.createdAfter !== undefined) {
      obj.createdAfter = message.createdAfter.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GrpcFileQuery>, I>>(base?: I): GrpcFileQuery {
    return GrpcFileQuery.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GrpcFileQuery>, I>>(object: I): GrpcFileQuery {
    const message = createBaseGrpcFileQuery();
    message.id = object.id ?? undefined;
    message.ids = object.ids?.map((e) => e) || [];
    message.mimeType = object.mimeType ?? undefined;
    message.mimeTypes = object.mimeTypes?.map((e) => e) || [];
    message.userId = object.userId ?? undefined;
    message.userIds = object.userIds?.map((e) => e) || [];
    message.extension = object.extension ?? undefined;
    message.uploadStatus = object.uploadStatus ?? undefined;
    message.uploadStatuses = object.uploadStatuses?.map((e) => e) || [];
    message.createdAfter = object.createdAfter ?? undefined;
    return message;
  },
};

function createBaseGrpcFileRequest(): GrpcFileRequest {
  return { query: undefined };
}

export const GrpcFileRequest: MessageFns<GrpcFileRequest> = {
  encode(message: GrpcFileRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.query !== undefined) {
      GrpcFileQuery.encode(message.query, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GrpcFileRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGrpcFileRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.query = GrpcFileQuery.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GrpcFileRequest {
    return { query: isSet(object.query) ? GrpcFileQuery.fromJSON(object.query) : undefined };
  },

  toJSON(message: GrpcFileRequest): unknown {
    const obj: any = {};
    if (message.query !== undefined) {
      obj.query = GrpcFileQuery.toJSON(message.query);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GrpcFileRequest>, I>>(base?: I): GrpcFileRequest {
    return GrpcFileRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GrpcFileRequest>, I>>(object: I): GrpcFileRequest {
    const message = createBaseGrpcFileRequest();
    message.query = (object.query !== undefined && object.query !== null)
      ? GrpcFileQuery.fromPartial(object.query)
      : undefined;
    return message;
  },
};

function createBaseGrpcFileGetListResponse(): GrpcFileGetListResponse {
  return { items: [], total: 0 };
}

export const GrpcFileGetListResponse: MessageFns<GrpcFileGetListResponse> = {
  encode(message: GrpcFileGetListResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.items) {
      GrpcFile.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.total !== 0) {
      writer.uint32(16).int32(message.total);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GrpcFileGetListResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGrpcFileGetListResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.items.push(GrpcFile.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.total = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GrpcFileGetListResponse {
    return {
      items: globalThis.Array.isArray(object?.items) ? object.items.map((e: any) => GrpcFile.fromJSON(e)) : [],
      total: isSet(object.total) ? globalThis.Number(object.total) : 0,
    };
  },

  toJSON(message: GrpcFileGetListResponse): unknown {
    const obj: any = {};
    if (message.items?.length) {
      obj.items = message.items.map((e) => GrpcFile.toJSON(e));
    }
    if (message.total !== 0) {
      obj.total = Math.round(message.total);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GrpcFileGetListResponse>, I>>(base?: I): GrpcFileGetListResponse {
    return GrpcFileGetListResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GrpcFileGetListResponse>, I>>(object: I): GrpcFileGetListResponse {
    const message = createBaseGrpcFileGetListResponse();
    message.items = object.items?.map((e) => GrpcFile.fromPartial(e)) || [];
    message.total = object.total ?? 0;
    return message;
  },
};

function createBaseGrpcFileCreate(): GrpcFileCreate {
  return { originalName: "", size: 0, mimeType: "" };
}

export const GrpcFileCreate: MessageFns<GrpcFileCreate> = {
  encode(message: GrpcFileCreate, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.originalName !== "") {
      writer.uint32(10).string(message.originalName);
    }
    if (message.size !== 0) {
      writer.uint32(16).int32(message.size);
    }
    if (message.mimeType !== "") {
      writer.uint32(26).string(message.mimeType);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GrpcFileCreate {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGrpcFileCreate();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.originalName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.size = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.mimeType = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GrpcFileCreate {
    return {
      originalName: isSet(object.originalName) ? globalThis.String(object.originalName) : "",
      size: isSet(object.size) ? globalThis.Number(object.size) : 0,
      mimeType: isSet(object.mimeType) ? globalThis.String(object.mimeType) : "",
    };
  },

  toJSON(message: GrpcFileCreate): unknown {
    const obj: any = {};
    if (message.originalName !== "") {
      obj.originalName = message.originalName;
    }
    if (message.size !== 0) {
      obj.size = Math.round(message.size);
    }
    if (message.mimeType !== "") {
      obj.mimeType = message.mimeType;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GrpcFileCreate>, I>>(base?: I): GrpcFileCreate {
    return GrpcFileCreate.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GrpcFileCreate>, I>>(object: I): GrpcFileCreate {
    const message = createBaseGrpcFileCreate();
    message.originalName = object.originalName ?? "";
    message.size = object.size ?? 0;
    message.mimeType = object.mimeType ?? "";
    return message;
  },
};

function createBaseGrpcFileCreateRequest(): GrpcFileCreateRequest {
  return { file: undefined, storage: undefined };
}

export const GrpcFileCreateRequest: MessageFns<GrpcFileCreateRequest> = {
  encode(message: GrpcFileCreateRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.file !== undefined) {
      GrpcFileCreate.encode(message.file, writer.uint32(10).fork()).join();
    }
    if (message.storage !== undefined) {
      GrpcStorageObjectMetadata.encode(message.storage, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GrpcFileCreateRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGrpcFileCreateRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.file = GrpcFileCreate.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.storage = GrpcStorageObjectMetadata.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GrpcFileCreateRequest {
    return {
      file: isSet(object.file) ? GrpcFileCreate.fromJSON(object.file) : undefined,
      storage: isSet(object.storage) ? GrpcStorageObjectMetadata.fromJSON(object.storage) : undefined,
    };
  },

  toJSON(message: GrpcFileCreateRequest): unknown {
    const obj: any = {};
    if (message.file !== undefined) {
      obj.file = GrpcFileCreate.toJSON(message.file);
    }
    if (message.storage !== undefined) {
      obj.storage = GrpcStorageObjectMetadata.toJSON(message.storage);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GrpcFileCreateRequest>, I>>(base?: I): GrpcFileCreateRequest {
    return GrpcFileCreateRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GrpcFileCreateRequest>, I>>(object: I): GrpcFileCreateRequest {
    const message = createBaseGrpcFileCreateRequest();
    message.file = (object.file !== undefined && object.file !== null)
      ? GrpcFileCreate.fromPartial(object.file)
      : undefined;
    message.storage = (object.storage !== undefined && object.storage !== null)
      ? GrpcStorageObjectMetadata.fromPartial(object.storage)
      : undefined;
    return message;
  },
};

function createBaseGrpcFileCreateManyItem(): GrpcFileCreateManyItem {
  return { file: undefined, uploadId: "" };
}

export const GrpcFileCreateManyItem: MessageFns<GrpcFileCreateManyItem> = {
  encode(message: GrpcFileCreateManyItem, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.file !== undefined) {
      GrpcFileCreate.encode(message.file, writer.uint32(10).fork()).join();
    }
    if (message.uploadId !== "") {
      writer.uint32(18).string(message.uploadId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GrpcFileCreateManyItem {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGrpcFileCreateManyItem();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.file = GrpcFileCreate.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.uploadId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GrpcFileCreateManyItem {
    return {
      file: isSet(object.file) ? GrpcFileCreate.fromJSON(object.file) : undefined,
      uploadId: isSet(object.uploadId) ? globalThis.String(object.uploadId) : "",
    };
  },

  toJSON(message: GrpcFileCreateManyItem): unknown {
    const obj: any = {};
    if (message.file !== undefined) {
      obj.file = GrpcFileCreate.toJSON(message.file);
    }
    if (message.uploadId !== "") {
      obj.uploadId = message.uploadId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GrpcFileCreateManyItem>, I>>(base?: I): GrpcFileCreateManyItem {
    return GrpcFileCreateManyItem.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GrpcFileCreateManyItem>, I>>(object: I): GrpcFileCreateManyItem {
    const message = createBaseGrpcFileCreateManyItem();
    message.file = (object.file !== undefined && object.file !== null)
      ? GrpcFileCreate.fromPartial(object.file)
      : undefined;
    message.uploadId = object.uploadId ?? "";
    return message;
  },
};

function createBaseGrpcFileCreateManyRequest(): GrpcFileCreateManyRequest {
  return { storage: undefined, items: [] };
}

export const GrpcFileCreateManyRequest: MessageFns<GrpcFileCreateManyRequest> = {
  encode(message: GrpcFileCreateManyRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.storage !== undefined) {
      GrpcStorageObjectManyMetadata.encode(message.storage, writer.uint32(10).fork()).join();
    }
    for (const v of message.items) {
      GrpcFileCreateManyItem.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GrpcFileCreateManyRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGrpcFileCreateManyRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.storage = GrpcStorageObjectManyMetadata.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.items.push(GrpcFileCreateManyItem.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GrpcFileCreateManyRequest {
    return {
      storage: isSet(object.storage) ? GrpcStorageObjectManyMetadata.fromJSON(object.storage) : undefined,
      items: globalThis.Array.isArray(object?.items)
        ? object.items.map((e: any) => GrpcFileCreateManyItem.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GrpcFileCreateManyRequest): unknown {
    const obj: any = {};
    if (message.storage !== undefined) {
      obj.storage = GrpcStorageObjectManyMetadata.toJSON(message.storage);
    }
    if (message.items?.length) {
      obj.items = message.items.map((e) => GrpcFileCreateManyItem.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GrpcFileCreateManyRequest>, I>>(base?: I): GrpcFileCreateManyRequest {
    return GrpcFileCreateManyRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GrpcFileCreateManyRequest>, I>>(object: I): GrpcFileCreateManyRequest {
    const message = createBaseGrpcFileCreateManyRequest();
    message.storage = (object.storage !== undefined && object.storage !== null)
      ? GrpcStorageObjectManyMetadata.fromPartial(object.storage)
      : undefined;
    message.items = object.items?.map((e) => GrpcFileCreateManyItem.fromPartial(e)) || [];
    return message;
  },
};

function createBaseGrpcFileCreateManyResponse(): GrpcFileCreateManyResponse {
  return { items: [] };
}

export const GrpcFileCreateManyResponse: MessageFns<GrpcFileCreateManyResponse> = {
  encode(message: GrpcFileCreateManyResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.items) {
      GrpcFile.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GrpcFileCreateManyResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGrpcFileCreateManyResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.items.push(GrpcFile.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GrpcFileCreateManyResponse {
    return { items: globalThis.Array.isArray(object?.items) ? object.items.map((e: any) => GrpcFile.fromJSON(e)) : [] };
  },

  toJSON(message: GrpcFileCreateManyResponse): unknown {
    const obj: any = {};
    if (message.items?.length) {
      obj.items = message.items.map((e) => GrpcFile.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GrpcFileCreateManyResponse>, I>>(base?: I): GrpcFileCreateManyResponse {
    return GrpcFileCreateManyResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GrpcFileCreateManyResponse>, I>>(object: I): GrpcFileCreateManyResponse {
    const message = createBaseGrpcFileCreateManyResponse();
    message.items = object.items?.map((e) => GrpcFile.fromPartial(e)) || [];
    return message;
  },
};

function createBaseGrpcFileUploadResponse(): GrpcFileUploadResponse {
  return { entity: undefined, canSendChunks: undefined, ack: undefined };
}

export const GrpcFileUploadResponse: MessageFns<GrpcFileUploadResponse> = {
  encode(message: GrpcFileUploadResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.entity !== undefined) {
      GrpcFile.encode(message.entity, writer.uint32(10).fork()).join();
    }
    if (message.canSendChunks !== undefined) {
      writer.uint32(16).bool(message.canSendChunks);
    }
    if (message.ack !== undefined) {
      writer.uint32(24).bool(message.ack);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GrpcFileUploadResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGrpcFileUploadResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.entity = GrpcFile.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.canSendChunks = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.ack = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GrpcFileUploadResponse {
    return {
      entity: isSet(object.entity) ? GrpcFile.fromJSON(object.entity) : undefined,
      canSendChunks: isSet(object.canSendChunks) ? globalThis.Boolean(object.canSendChunks) : undefined,
      ack: isSet(object.ack) ? globalThis.Boolean(object.ack) : undefined,
    };
  },

  toJSON(message: GrpcFileUploadResponse): unknown {
    const obj: any = {};
    if (message.entity !== undefined) {
      obj.entity = GrpcFile.toJSON(message.entity);
    }
    if (message.canSendChunks !== undefined) {
      obj.canSendChunks = message.canSendChunks;
    }
    if (message.ack !== undefined) {
      obj.ack = message.ack;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GrpcFileUploadResponse>, I>>(base?: I): GrpcFileUploadResponse {
    return GrpcFileUploadResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GrpcFileUploadResponse>, I>>(object: I): GrpcFileUploadResponse {
    const message = createBaseGrpcFileUploadResponse();
    message.entity = (object.entity !== undefined && object.entity !== null)
      ? GrpcFile.fromPartial(object.entity)
      : undefined;
    message.canSendChunks = object.canSendChunks ?? undefined;
    message.ack = object.ack ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): GrpcTimestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: GrpcTimestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(GrpcTimestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
