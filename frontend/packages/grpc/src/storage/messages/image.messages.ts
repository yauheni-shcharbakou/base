// @ts-nocheck
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.5
//   protoc               v7.34.0
// source: storage/messages/image.messages.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { GrpcTimestamp } from "../../google/protobuf/timestamp";
import { GrpcFile } from "../models/file";
import { GrpcImage } from "../models/image";
import { GrpcFileCreate } from "./file.messages";
import { GrpcStorageObjectManyMetadata, GrpcStorageObjectMetadata } from "./storage-object.messages";
import "server-only";

export interface GrpcImagePopulated {
  id: string;
  createdAt: Date;
  updatedAt?: Date;
  width: number;
  height: number;
  alt: string;
  userId: string;
  fileId: string;
  file: GrpcFile;
  uploadId: string;
}

export interface GrpcImageQuery {
  id?: string;
  ids: string[];
  file?: string;
  userId?: string;
}

export interface GrpcImageGetListResponse {
  items: GrpcImagePopulated[];
  total: number;
}

export interface GrpcImageCreate {
  width: number;
  height: number;
  alt: string;
}

export interface GrpcImageCreateRequest {
  image: GrpcImageCreate;
  file: GrpcFileCreate;
  storage?: GrpcStorageObjectMetadata;
}

export interface GrpcImageCreateManyItem {
  image: GrpcImageCreate;
  file: GrpcFileCreate;
  uploadId: string;
}

export interface GrpcImageCreateManyRequest {
  storage?: GrpcStorageObjectManyMetadata;
  items: GrpcImageCreateManyItem[];
}

export interface GrpcImageCreateManyResponse {
  items: GrpcImage[];
}

export interface GrpcImageUpdateSet {
  alt?: string;
}

export interface GrpcImageUpdate {
  set?: GrpcImageUpdateSet;
}

export interface GrpcImageUpdateRequest {
  query: GrpcImageQuery;
  update: GrpcImageUpdate;
}

export interface GrpcImageUpdateByIdRequest {
  id: string;
  update: GrpcImageUpdate;
}

function createBaseGrpcImagePopulated(): GrpcImagePopulated {
  return {
    id: "",
    createdAt: undefined,
    updatedAt: undefined,
    width: 0,
    height: 0,
    alt: "",
    userId: "",
    fileId: "",
    file: undefined,
    uploadId: "",
  };
}

export const GrpcImagePopulated: MessageFns<GrpcImagePopulated> = {
  encode(message: GrpcImagePopulated, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.createdAt !== undefined) {
      GrpcTimestamp.encode(toTimestamp(message.createdAt), writer.uint32(18).fork()).join();
    }
    if (message.updatedAt !== undefined) {
      GrpcTimestamp.encode(toTimestamp(message.updatedAt), writer.uint32(26).fork()).join();
    }
    if (message.width !== 0) {
      writer.uint32(32).int32(message.width);
    }
    if (message.height !== 0) {
      writer.uint32(40).int32(message.height);
    }
    if (message.alt !== "") {
      writer.uint32(50).string(message.alt);
    }
    if (message.userId !== "") {
      writer.uint32(58).string(message.userId);
    }
    if (message.fileId !== "") {
      writer.uint32(66).string(message.fileId);
    }
    if (message.file !== undefined) {
      GrpcFile.encode(message.file, writer.uint32(74).fork()).join();
    }
    if (message.uploadId !== "") {
      writer.uint32(82).string(message.uploadId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GrpcImagePopulated {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGrpcImagePopulated();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.createdAt = fromTimestamp(GrpcTimestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.updatedAt = fromTimestamp(GrpcTimestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.width = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.height = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.alt = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.fileId = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.file = GrpcFile.decode(reader, reader.uint32());
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.uploadId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GrpcImagePopulated {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      createdAt: isSet(object.createdAt) ? fromJsonTimestamp(object.createdAt) : undefined,
      updatedAt: isSet(object.updatedAt) ? fromJsonTimestamp(object.updatedAt) : undefined,
      width: isSet(object.width) ? globalThis.Number(object.width) : 0,
      height: isSet(object.height) ? globalThis.Number(object.height) : 0,
      alt: isSet(object.alt) ? globalThis.String(object.alt) : "",
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "",
      fileId: isSet(object.fileId) ? globalThis.String(object.fileId) : "",
      file: isSet(object.file) ? GrpcFile.fromJSON(object.file) : undefined,
      uploadId: isSet(object.uploadId) ? globalThis.String(object.uploadId) : "",
    };
  },

  toJSON(message: GrpcImagePopulated): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.updatedAt !== undefined) {
      obj.updatedAt = message.updatedAt.toISOString();
    }
    if (message.width !== 0) {
      obj.width = Math.round(message.width);
    }
    if (message.height !== 0) {
      obj.height = Math.round(message.height);
    }
    if (message.alt !== "") {
      obj.alt = message.alt;
    }
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.fileId !== "") {
      obj.fileId = message.fileId;
    }
    if (message.file !== undefined) {
      obj.file = GrpcFile.toJSON(message.file);
    }
    if (message.uploadId !== "") {
      obj.uploadId = message.uploadId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GrpcImagePopulated>, I>>(base?: I): GrpcImagePopulated {
    return GrpcImagePopulated.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GrpcImagePopulated>, I>>(object: I): GrpcImagePopulated {
    const message = createBaseGrpcImagePopulated();
    message.id = object.id ?? "";
    message.createdAt = object.createdAt ?? undefined;
    message.updatedAt = object.updatedAt ?? undefined;
    message.width = object.width ?? 0;
    message.height = object.height ?? 0;
    message.alt = object.alt ?? "";
    message.userId = object.userId ?? "";
    message.fileId = object.fileId ?? "";
    message.file = (object.file !== undefined && object.file !== null) ? GrpcFile.fromPartial(object.file) : undefined;
    message.uploadId = object.uploadId ?? "";
    return message;
  },
};

function createBaseGrpcImageQuery(): GrpcImageQuery {
  return { id: undefined, ids: [], file: undefined, userId: undefined };
}

export const GrpcImageQuery: MessageFns<GrpcImageQuery> = {
  encode(message: GrpcImageQuery, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      writer.uint32(10).string(message.id);
    }
    for (const v of message.ids) {
      writer.uint32(18).string(v!);
    }
    if (message.file !== undefined) {
      writer.uint32(26).string(message.file);
    }
    if (message.userId !== undefined) {
      writer.uint32(34).string(message.userId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GrpcImageQuery {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGrpcImageQuery();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.ids.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.file = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GrpcImageQuery {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : undefined,
      ids: globalThis.Array.isArray(object?.ids) ? object.ids.map((e: any) => globalThis.String(e)) : [],
      file: isSet(object.file) ? globalThis.String(object.file) : undefined,
      userId: isSet(object.userId) ? globalThis.String(object.userId) : undefined,
    };
  },

  toJSON(message: GrpcImageQuery): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = message.id;
    }
    if (message.ids?.length) {
      obj.ids = message.ids;
    }
    if (message.file !== undefined) {
      obj.file = message.file;
    }
    if (message.userId !== undefined) {
      obj.userId = message.userId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GrpcImageQuery>, I>>(base?: I): GrpcImageQuery {
    return GrpcImageQuery.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GrpcImageQuery>, I>>(object: I): GrpcImageQuery {
    const message = createBaseGrpcImageQuery();
    message.id = object.id ?? undefined;
    message.ids = object.ids?.map((e) => e) || [];
    message.file = object.file ?? undefined;
    message.userId = object.userId ?? undefined;
    return message;
  },
};

function createBaseGrpcImageGetListResponse(): GrpcImageGetListResponse {
  return { items: [], total: 0 };
}

export const GrpcImageGetListResponse: MessageFns<GrpcImageGetListResponse> = {
  encode(message: GrpcImageGetListResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.items) {
      GrpcImagePopulated.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.total !== 0) {
      writer.uint32(16).int32(message.total);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GrpcImageGetListResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGrpcImageGetListResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.items.push(GrpcImagePopulated.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.total = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GrpcImageGetListResponse {
    return {
      items: globalThis.Array.isArray(object?.items)
        ? object.items.map((e: any) => GrpcImagePopulated.fromJSON(e))
        : [],
      total: isSet(object.total) ? globalThis.Number(object.total) : 0,
    };
  },

  toJSON(message: GrpcImageGetListResponse): unknown {
    const obj: any = {};
    if (message.items?.length) {
      obj.items = message.items.map((e) => GrpcImagePopulated.toJSON(e));
    }
    if (message.total !== 0) {
      obj.total = Math.round(message.total);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GrpcImageGetListResponse>, I>>(base?: I): GrpcImageGetListResponse {
    return GrpcImageGetListResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GrpcImageGetListResponse>, I>>(object: I): GrpcImageGetListResponse {
    const message = createBaseGrpcImageGetListResponse();
    message.items = object.items?.map((e) => GrpcImagePopulated.fromPartial(e)) || [];
    message.total = object.total ?? 0;
    return message;
  },
};

function createBaseGrpcImageCreate(): GrpcImageCreate {
  return { width: 0, height: 0, alt: "" };
}

export const GrpcImageCreate: MessageFns<GrpcImageCreate> = {
  encode(message: GrpcImageCreate, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.width !== 0) {
      writer.uint32(8).int32(message.width);
    }
    if (message.height !== 0) {
      writer.uint32(16).int32(message.height);
    }
    if (message.alt !== "") {
      writer.uint32(26).string(message.alt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GrpcImageCreate {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGrpcImageCreate();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.width = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.height = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.alt = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GrpcImageCreate {
    return {
      width: isSet(object.width) ? globalThis.Number(object.width) : 0,
      height: isSet(object.height) ? globalThis.Number(object.height) : 0,
      alt: isSet(object.alt) ? globalThis.String(object.alt) : "",
    };
  },

  toJSON(message: GrpcImageCreate): unknown {
    const obj: any = {};
    if (message.width !== 0) {
      obj.width = Math.round(message.width);
    }
    if (message.height !== 0) {
      obj.height = Math.round(message.height);
    }
    if (message.alt !== "") {
      obj.alt = message.alt;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GrpcImageCreate>, I>>(base?: I): GrpcImageCreate {
    return GrpcImageCreate.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GrpcImageCreate>, I>>(object: I): GrpcImageCreate {
    const message = createBaseGrpcImageCreate();
    message.width = object.width ?? 0;
    message.height = object.height ?? 0;
    message.alt = object.alt ?? "";
    return message;
  },
};

function createBaseGrpcImageCreateRequest(): GrpcImageCreateRequest {
  return { image: undefined, file: undefined, storage: undefined };
}

export const GrpcImageCreateRequest: MessageFns<GrpcImageCreateRequest> = {
  encode(message: GrpcImageCreateRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.image !== undefined) {
      GrpcImageCreate.encode(message.image, writer.uint32(10).fork()).join();
    }
    if (message.file !== undefined) {
      GrpcFileCreate.encode(message.file, writer.uint32(18).fork()).join();
    }
    if (message.storage !== undefined) {
      GrpcStorageObjectMetadata.encode(message.storage, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GrpcImageCreateRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGrpcImageCreateRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.image = GrpcImageCreate.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.file = GrpcFileCreate.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.storage = GrpcStorageObjectMetadata.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GrpcImageCreateRequest {
    return {
      image: isSet(object.image) ? GrpcImageCreate.fromJSON(object.image) : undefined,
      file: isSet(object.file) ? GrpcFileCreate.fromJSON(object.file) : undefined,
      storage: isSet(object.storage) ? GrpcStorageObjectMetadata.fromJSON(object.storage) : undefined,
    };
  },

  toJSON(message: GrpcImageCreateRequest): unknown {
    const obj: any = {};
    if (message.image !== undefined) {
      obj.image = GrpcImageCreate.toJSON(message.image);
    }
    if (message.file !== undefined) {
      obj.file = GrpcFileCreate.toJSON(message.file);
    }
    if (message.storage !== undefined) {
      obj.storage = GrpcStorageObjectMetadata.toJSON(message.storage);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GrpcImageCreateRequest>, I>>(base?: I): GrpcImageCreateRequest {
    return GrpcImageCreateRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GrpcImageCreateRequest>, I>>(object: I): GrpcImageCreateRequest {
    const message = createBaseGrpcImageCreateRequest();
    message.image = (object.image !== undefined && object.image !== null)
      ? GrpcImageCreate.fromPartial(object.image)
      : undefined;
    message.file = (object.file !== undefined && object.file !== null)
      ? GrpcFileCreate.fromPartial(object.file)
      : undefined;
    message.storage = (object.storage !== undefined && object.storage !== null)
      ? GrpcStorageObjectMetadata.fromPartial(object.storage)
      : undefined;
    return message;
  },
};

function createBaseGrpcImageCreateManyItem(): GrpcImageCreateManyItem {
  return { image: undefined, file: undefined, uploadId: "" };
}

export const GrpcImageCreateManyItem: MessageFns<GrpcImageCreateManyItem> = {
  encode(message: GrpcImageCreateManyItem, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.image !== undefined) {
      GrpcImageCreate.encode(message.image, writer.uint32(10).fork()).join();
    }
    if (message.file !== undefined) {
      GrpcFileCreate.encode(message.file, writer.uint32(18).fork()).join();
    }
    if (message.uploadId !== "") {
      writer.uint32(26).string(message.uploadId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GrpcImageCreateManyItem {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGrpcImageCreateManyItem();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.image = GrpcImageCreate.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.file = GrpcFileCreate.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.uploadId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GrpcImageCreateManyItem {
    return {
      image: isSet(object.image) ? GrpcImageCreate.fromJSON(object.image) : undefined,
      file: isSet(object.file) ? GrpcFileCreate.fromJSON(object.file) : undefined,
      uploadId: isSet(object.uploadId) ? globalThis.String(object.uploadId) : "",
    };
  },

  toJSON(message: GrpcImageCreateManyItem): unknown {
    const obj: any = {};
    if (message.image !== undefined) {
      obj.image = GrpcImageCreate.toJSON(message.image);
    }
    if (message.file !== undefined) {
      obj.file = GrpcFileCreate.toJSON(message.file);
    }
    if (message.uploadId !== "") {
      obj.uploadId = message.uploadId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GrpcImageCreateManyItem>, I>>(base?: I): GrpcImageCreateManyItem {
    return GrpcImageCreateManyItem.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GrpcImageCreateManyItem>, I>>(object: I): GrpcImageCreateManyItem {
    const message = createBaseGrpcImageCreateManyItem();
    message.image = (object.image !== undefined && object.image !== null)
      ? GrpcImageCreate.fromPartial(object.image)
      : undefined;
    message.file = (object.file !== undefined && object.file !== null)
      ? GrpcFileCreate.fromPartial(object.file)
      : undefined;
    message.uploadId = object.uploadId ?? "";
    return message;
  },
};

function createBaseGrpcImageCreateManyRequest(): GrpcImageCreateManyRequest {
  return { storage: undefined, items: [] };
}

export const GrpcImageCreateManyRequest: MessageFns<GrpcImageCreateManyRequest> = {
  encode(message: GrpcImageCreateManyRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.storage !== undefined) {
      GrpcStorageObjectManyMetadata.encode(message.storage, writer.uint32(10).fork()).join();
    }
    for (const v of message.items) {
      GrpcImageCreateManyItem.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GrpcImageCreateManyRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGrpcImageCreateManyRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.storage = GrpcStorageObjectManyMetadata.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.items.push(GrpcImageCreateManyItem.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GrpcImageCreateManyRequest {
    return {
      storage: isSet(object.storage) ? GrpcStorageObjectManyMetadata.fromJSON(object.storage) : undefined,
      items: globalThis.Array.isArray(object?.items)
        ? object.items.map((e: any) => GrpcImageCreateManyItem.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GrpcImageCreateManyRequest): unknown {
    const obj: any = {};
    if (message.storage !== undefined) {
      obj.storage = GrpcStorageObjectManyMetadata.toJSON(message.storage);
    }
    if (message.items?.length) {
      obj.items = message.items.map((e) => GrpcImageCreateManyItem.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GrpcImageCreateManyRequest>, I>>(base?: I): GrpcImageCreateManyRequest {
    return GrpcImageCreateManyRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GrpcImageCreateManyRequest>, I>>(object: I): GrpcImageCreateManyRequest {
    const message = createBaseGrpcImageCreateManyRequest();
    message.storage = (object.storage !== undefined && object.storage !== null)
      ? GrpcStorageObjectManyMetadata.fromPartial(object.storage)
      : undefined;
    message.items = object.items?.map((e) => GrpcImageCreateManyItem.fromPartial(e)) || [];
    return message;
  },
};

function createBaseGrpcImageCreateManyResponse(): GrpcImageCreateManyResponse {
  return { items: [] };
}

export const GrpcImageCreateManyResponse: MessageFns<GrpcImageCreateManyResponse> = {
  encode(message: GrpcImageCreateManyResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.items) {
      GrpcImage.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GrpcImageCreateManyResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGrpcImageCreateManyResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.items.push(GrpcImage.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GrpcImageCreateManyResponse {
    return {
      items: globalThis.Array.isArray(object?.items) ? object.items.map((e: any) => GrpcImage.fromJSON(e)) : [],
    };
  },

  toJSON(message: GrpcImageCreateManyResponse): unknown {
    const obj: any = {};
    if (message.items?.length) {
      obj.items = message.items.map((e) => GrpcImage.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GrpcImageCreateManyResponse>, I>>(base?: I): GrpcImageCreateManyResponse {
    return GrpcImageCreateManyResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GrpcImageCreateManyResponse>, I>>(object: I): GrpcImageCreateManyResponse {
    const message = createBaseGrpcImageCreateManyResponse();
    message.items = object.items?.map((e) => GrpcImage.fromPartial(e)) || [];
    return message;
  },
};

function createBaseGrpcImageUpdateSet(): GrpcImageUpdateSet {
  return { alt: undefined };
}

export const GrpcImageUpdateSet: MessageFns<GrpcImageUpdateSet> = {
  encode(message: GrpcImageUpdateSet, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.alt !== undefined) {
      writer.uint32(10).string(message.alt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GrpcImageUpdateSet {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGrpcImageUpdateSet();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.alt = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GrpcImageUpdateSet {
    return { alt: isSet(object.alt) ? globalThis.String(object.alt) : undefined };
  },

  toJSON(message: GrpcImageUpdateSet): unknown {
    const obj: any = {};
    if (message.alt !== undefined) {
      obj.alt = message.alt;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GrpcImageUpdateSet>, I>>(base?: I): GrpcImageUpdateSet {
    return GrpcImageUpdateSet.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GrpcImageUpdateSet>, I>>(object: I): GrpcImageUpdateSet {
    const message = createBaseGrpcImageUpdateSet();
    message.alt = object.alt ?? undefined;
    return message;
  },
};

function createBaseGrpcImageUpdate(): GrpcImageUpdate {
  return { set: undefined };
}

export const GrpcImageUpdate: MessageFns<GrpcImageUpdate> = {
  encode(message: GrpcImageUpdate, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.set !== undefined) {
      GrpcImageUpdateSet.encode(message.set, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GrpcImageUpdate {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGrpcImageUpdate();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.set = GrpcImageUpdateSet.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GrpcImageUpdate {
    return { set: isSet(object.set) ? GrpcImageUpdateSet.fromJSON(object.set) : undefined };
  },

  toJSON(message: GrpcImageUpdate): unknown {
    const obj: any = {};
    if (message.set !== undefined) {
      obj.set = GrpcImageUpdateSet.toJSON(message.set);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GrpcImageUpdate>, I>>(base?: I): GrpcImageUpdate {
    return GrpcImageUpdate.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GrpcImageUpdate>, I>>(object: I): GrpcImageUpdate {
    const message = createBaseGrpcImageUpdate();
    message.set = (object.set !== undefined && object.set !== null)
      ? GrpcImageUpdateSet.fromPartial(object.set)
      : undefined;
    return message;
  },
};

function createBaseGrpcImageUpdateRequest(): GrpcImageUpdateRequest {
  return { query: undefined, update: undefined };
}

export const GrpcImageUpdateRequest: MessageFns<GrpcImageUpdateRequest> = {
  encode(message: GrpcImageUpdateRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.query !== undefined) {
      GrpcImageQuery.encode(message.query, writer.uint32(10).fork()).join();
    }
    if (message.update !== undefined) {
      GrpcImageUpdate.encode(message.update, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GrpcImageUpdateRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGrpcImageUpdateRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.query = GrpcImageQuery.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.update = GrpcImageUpdate.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GrpcImageUpdateRequest {
    return {
      query: isSet(object.query) ? GrpcImageQuery.fromJSON(object.query) : undefined,
      update: isSet(object.update) ? GrpcImageUpdate.fromJSON(object.update) : undefined,
    };
  },

  toJSON(message: GrpcImageUpdateRequest): unknown {
    const obj: any = {};
    if (message.query !== undefined) {
      obj.query = GrpcImageQuery.toJSON(message.query);
    }
    if (message.update !== undefined) {
      obj.update = GrpcImageUpdate.toJSON(message.update);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GrpcImageUpdateRequest>, I>>(base?: I): GrpcImageUpdateRequest {
    return GrpcImageUpdateRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GrpcImageUpdateRequest>, I>>(object: I): GrpcImageUpdateRequest {
    const message = createBaseGrpcImageUpdateRequest();
    message.query = (object.query !== undefined && object.query !== null)
      ? GrpcImageQuery.fromPartial(object.query)
      : undefined;
    message.update = (object.update !== undefined && object.update !== null)
      ? GrpcImageUpdate.fromPartial(object.update)
      : undefined;
    return message;
  },
};

function createBaseGrpcImageUpdateByIdRequest(): GrpcImageUpdateByIdRequest {
  return { id: "", update: undefined };
}

export const GrpcImageUpdateByIdRequest: MessageFns<GrpcImageUpdateByIdRequest> = {
  encode(message: GrpcImageUpdateByIdRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.update !== undefined) {
      GrpcImageUpdate.encode(message.update, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GrpcImageUpdateByIdRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGrpcImageUpdateByIdRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.update = GrpcImageUpdate.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GrpcImageUpdateByIdRequest {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      update: isSet(object.update) ? GrpcImageUpdate.fromJSON(object.update) : undefined,
    };
  },

  toJSON(message: GrpcImageUpdateByIdRequest): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.update !== undefined) {
      obj.update = GrpcImageUpdate.toJSON(message.update);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GrpcImageUpdateByIdRequest>, I>>(base?: I): GrpcImageUpdateByIdRequest {
    return GrpcImageUpdateByIdRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GrpcImageUpdateByIdRequest>, I>>(object: I): GrpcImageUpdateByIdRequest {
    const message = createBaseGrpcImageUpdateByIdRequest();
    message.id = object.id ?? "";
    message.update = (object.update !== undefined && object.update !== null)
      ? GrpcImageUpdate.fromPartial(object.update)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): GrpcTimestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: GrpcTimestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(GrpcTimestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
