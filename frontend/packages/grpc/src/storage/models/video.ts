// @ts-nocheck
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.5
//   protoc               v7.34.0
// source: storage/models/video.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { GrpcTimestamp } from "../../google/protobuf/timestamp";
import "server-only";

export interface GrpcVideo {
  id: string;
  createdAt: Date;
  updatedAt?: Date;
  title: string;
  duration: number;
  views: number;
  description?: string;
  providerId: string;
  userId: string;
  fileId: string;
  uploadId: string;
}

function createBaseGrpcVideo(): GrpcVideo {
  return {
    id: "",
    createdAt: undefined,
    updatedAt: undefined,
    title: "",
    duration: 0,
    views: 0,
    description: undefined,
    providerId: "",
    userId: "",
    fileId: "",
    uploadId: "",
  };
}

export const GrpcVideo: MessageFns<GrpcVideo> = {
  encode(message: GrpcVideo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.createdAt !== undefined) {
      GrpcTimestamp.encode(toTimestamp(message.createdAt), writer.uint32(18).fork()).join();
    }
    if (message.updatedAt !== undefined) {
      GrpcTimestamp.encode(toTimestamp(message.updatedAt), writer.uint32(26).fork()).join();
    }
    if (message.title !== "") {
      writer.uint32(34).string(message.title);
    }
    if (message.duration !== 0) {
      writer.uint32(40).int32(message.duration);
    }
    if (message.views !== 0) {
      writer.uint32(48).int32(message.views);
    }
    if (message.description !== undefined) {
      writer.uint32(58).string(message.description);
    }
    if (message.providerId !== "") {
      writer.uint32(66).string(message.providerId);
    }
    if (message.userId !== "") {
      writer.uint32(74).string(message.userId);
    }
    if (message.fileId !== "") {
      writer.uint32(82).string(message.fileId);
    }
    if (message.uploadId !== "") {
      writer.uint32(90).string(message.uploadId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GrpcVideo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGrpcVideo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.createdAt = fromTimestamp(GrpcTimestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.updatedAt = fromTimestamp(GrpcTimestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.duration = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.views = reader.int32();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.providerId = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.fileId = reader.string();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.uploadId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GrpcVideo {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      createdAt: isSet(object.createdAt) ? fromJsonTimestamp(object.createdAt) : undefined,
      updatedAt: isSet(object.updatedAt) ? fromJsonTimestamp(object.updatedAt) : undefined,
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      duration: isSet(object.duration) ? globalThis.Number(object.duration) : 0,
      views: isSet(object.views) ? globalThis.Number(object.views) : 0,
      description: isSet(object.description) ? globalThis.String(object.description) : undefined,
      providerId: isSet(object.providerId) ? globalThis.String(object.providerId) : "",
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "",
      fileId: isSet(object.fileId) ? globalThis.String(object.fileId) : "",
      uploadId: isSet(object.uploadId) ? globalThis.String(object.uploadId) : "",
    };
  },

  toJSON(message: GrpcVideo): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.updatedAt !== undefined) {
      obj.updatedAt = message.updatedAt.toISOString();
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.duration !== 0) {
      obj.duration = Math.round(message.duration);
    }
    if (message.views !== 0) {
      obj.views = Math.round(message.views);
    }
    if (message.description !== undefined) {
      obj.description = message.description;
    }
    if (message.providerId !== "") {
      obj.providerId = message.providerId;
    }
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.fileId !== "") {
      obj.fileId = message.fileId;
    }
    if (message.uploadId !== "") {
      obj.uploadId = message.uploadId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GrpcVideo>, I>>(base?: I): GrpcVideo {
    return GrpcVideo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GrpcVideo>, I>>(object: I): GrpcVideo {
    const message = createBaseGrpcVideo();
    message.id = object.id ?? "";
    message.createdAt = object.createdAt ?? undefined;
    message.updatedAt = object.updatedAt ?? undefined;
    message.title = object.title ?? "";
    message.duration = object.duration ?? 0;
    message.views = object.views ?? 0;
    message.description = object.description ?? undefined;
    message.providerId = object.providerId ?? "";
    message.userId = object.userId ?? "";
    message.fileId = object.fileId ?? "";
    message.uploadId = object.uploadId ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): GrpcTimestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: GrpcTimestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(GrpcTimestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
