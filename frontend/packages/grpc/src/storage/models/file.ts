// @ts-nocheck
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               v7.34.0
// source: storage/models/file.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from '@bufbuild/protobuf/wire';
import { GrpcTimestamp } from '../../google/protobuf/timestamp';
import 'server-only';

export enum GrpcFileUploadStatus {
  PENDING = 'PENDING',
  FAILED = 'FAILED',
  READY = 'READY',
}

export function grpcFileUploadStatusFromJSON(object: any): GrpcFileUploadStatus {
  switch (object) {
    case 0:
    case 'PENDING':
      return GrpcFileUploadStatus.PENDING;
    case 1:
    case 'FAILED':
      return GrpcFileUploadStatus.FAILED;
    case 2:
    case 'READY':
      return GrpcFileUploadStatus.READY;
    default:
      throw new globalThis.Error(
        'Unrecognized enum value ' + object + ' for enum GrpcFileUploadStatus',
      );
  }
}

export function grpcFileUploadStatusToJSON(object: GrpcFileUploadStatus): string {
  switch (object) {
    case GrpcFileUploadStatus.PENDING:
      return 'PENDING';
    case GrpcFileUploadStatus.FAILED:
      return 'FAILED';
    case GrpcFileUploadStatus.READY:
      return 'READY';
    default:
      throw new globalThis.Error(
        'Unrecognized enum value ' + object + ' for enum GrpcFileUploadStatus',
      );
  }
}

export function grpcFileUploadStatusToNumber(object: GrpcFileUploadStatus): number {
  switch (object) {
    case GrpcFileUploadStatus.PENDING:
      return 0;
    case GrpcFileUploadStatus.FAILED:
      return 1;
    case GrpcFileUploadStatus.READY:
      return 2;
    default:
      throw new globalThis.Error(
        'Unrecognized enum value ' + object + ' for enum GrpcFileUploadStatus',
      );
  }
}

export interface GrpcFile {
  id: string;
  createdAt: Date;
  updatedAt?: Date;
  originalName: string;
  size: number;
  mimeType: string;
  extension: string;
  uploadStatus: GrpcFileUploadStatus;
  providerId?: string;
  userId: string;
  uploadId: string;
}

function createBaseGrpcFile(): GrpcFile {
  return {
    id: '',
    createdAt: undefined,
    updatedAt: undefined,
    originalName: '',
    size: 0,
    mimeType: '',
    extension: '',
    uploadStatus: GrpcFileUploadStatus.PENDING,
    providerId: undefined,
    userId: '',
    uploadId: '',
  };
}

export const GrpcFile: MessageFns<GrpcFile> = {
  encode(message: GrpcFile, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== '') {
      writer.uint32(10).string(message.id);
    }
    if (message.createdAt !== undefined) {
      GrpcTimestamp.encode(toTimestamp(message.createdAt), writer.uint32(18).fork()).join();
    }
    if (message.updatedAt !== undefined) {
      GrpcTimestamp.encode(toTimestamp(message.updatedAt), writer.uint32(26).fork()).join();
    }
    if (message.originalName !== '') {
      writer.uint32(34).string(message.originalName);
    }
    if (message.size !== 0) {
      writer.uint32(40).int32(message.size);
    }
    if (message.mimeType !== '') {
      writer.uint32(50).string(message.mimeType);
    }
    if (message.extension !== '') {
      writer.uint32(58).string(message.extension);
    }
    if (message.uploadStatus !== GrpcFileUploadStatus.PENDING) {
      writer.uint32(64).int32(grpcFileUploadStatusToNumber(message.uploadStatus));
    }
    if (message.providerId !== undefined) {
      writer.uint32(74).string(message.providerId);
    }
    if (message.userId !== '') {
      writer.uint32(82).string(message.userId);
    }
    if (message.uploadId !== '') {
      writer.uint32(90).string(message.uploadId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GrpcFile {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGrpcFile();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.createdAt = fromTimestamp(GrpcTimestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.updatedAt = fromTimestamp(GrpcTimestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.originalName = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.size = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.mimeType = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.extension = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.uploadStatus = grpcFileUploadStatusFromJSON(reader.int32());
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.providerId = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.uploadId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GrpcFile {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : '',
      createdAt: isSet(object.createdAt) ? fromJsonTimestamp(object.createdAt) : undefined,
      updatedAt: isSet(object.updatedAt) ? fromJsonTimestamp(object.updatedAt) : undefined,
      originalName: isSet(object.originalName) ? globalThis.String(object.originalName) : '',
      size: isSet(object.size) ? globalThis.Number(object.size) : 0,
      mimeType: isSet(object.mimeType) ? globalThis.String(object.mimeType) : '',
      extension: isSet(object.extension) ? globalThis.String(object.extension) : '',
      uploadStatus: isSet(object.uploadStatus)
        ? grpcFileUploadStatusFromJSON(object.uploadStatus)
        : GrpcFileUploadStatus.PENDING,
      providerId: isSet(object.providerId) ? globalThis.String(object.providerId) : undefined,
      userId: isSet(object.userId) ? globalThis.String(object.userId) : '',
      uploadId: isSet(object.uploadId) ? globalThis.String(object.uploadId) : '',
    };
  },

  toJSON(message: GrpcFile): unknown {
    const obj: any = {};
    if (message.id !== '') {
      obj.id = message.id;
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.updatedAt !== undefined) {
      obj.updatedAt = message.updatedAt.toISOString();
    }
    if (message.originalName !== '') {
      obj.originalName = message.originalName;
    }
    if (message.size !== 0) {
      obj.size = Math.round(message.size);
    }
    if (message.mimeType !== '') {
      obj.mimeType = message.mimeType;
    }
    if (message.extension !== '') {
      obj.extension = message.extension;
    }
    if (message.uploadStatus !== GrpcFileUploadStatus.PENDING) {
      obj.uploadStatus = grpcFileUploadStatusToJSON(message.uploadStatus);
    }
    if (message.providerId !== undefined) {
      obj.providerId = message.providerId;
    }
    if (message.userId !== '') {
      obj.userId = message.userId;
    }
    if (message.uploadId !== '') {
      obj.uploadId = message.uploadId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GrpcFile>, I>>(base?: I): GrpcFile {
    return GrpcFile.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GrpcFile>, I>>(object: I): GrpcFile {
    const message = createBaseGrpcFile();
    message.id = object.id ?? '';
    message.createdAt = object.createdAt ?? undefined;
    message.updatedAt = object.updatedAt ?? undefined;
    message.originalName = object.originalName ?? '';
    message.size = object.size ?? 0;
    message.mimeType = object.mimeType ?? '';
    message.extension = object.extension ?? '';
    message.uploadStatus = object.uploadStatus ?? GrpcFileUploadStatus.PENDING;
    message.providerId = object.providerId ?? undefined;
    message.userId = object.userId ?? '';
    message.uploadId = object.uploadId ?? '';
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): GrpcTimestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: GrpcTimestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === 'string') {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(GrpcTimestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
