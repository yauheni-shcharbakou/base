// @ts-nocheck
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               v7.34.0
// source: storage/models/storage-object.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from '@bufbuild/protobuf/wire';
import { GrpcTimestamp } from '../../google/protobuf/timestamp';
import 'server-only';

export enum GrpcStorageObjectType {
  FOLDER = 'FOLDER',
  FILE = 'FILE',
  IMAGE = 'IMAGE',
  VIDEO = 'VIDEO',
}

export function grpcStorageObjectTypeFromJSON(object: any): GrpcStorageObjectType {
  switch (object) {
    case 0:
    case 'FOLDER':
      return GrpcStorageObjectType.FOLDER;
    case 1:
    case 'FILE':
      return GrpcStorageObjectType.FILE;
    case 2:
    case 'IMAGE':
      return GrpcStorageObjectType.IMAGE;
    case 3:
    case 'VIDEO':
      return GrpcStorageObjectType.VIDEO;
    default:
      throw new globalThis.Error(
        'Unrecognized enum value ' + object + ' for enum GrpcStorageObjectType',
      );
  }
}

export function grpcStorageObjectTypeToJSON(object: GrpcStorageObjectType): string {
  switch (object) {
    case GrpcStorageObjectType.FOLDER:
      return 'FOLDER';
    case GrpcStorageObjectType.FILE:
      return 'FILE';
    case GrpcStorageObjectType.IMAGE:
      return 'IMAGE';
    case GrpcStorageObjectType.VIDEO:
      return 'VIDEO';
    default:
      throw new globalThis.Error(
        'Unrecognized enum value ' + object + ' for enum GrpcStorageObjectType',
      );
  }
}

export function grpcStorageObjectTypeToNumber(object: GrpcStorageObjectType): number {
  switch (object) {
    case GrpcStorageObjectType.FOLDER:
      return 0;
    case GrpcStorageObjectType.FILE:
      return 1;
    case GrpcStorageObjectType.IMAGE:
      return 2;
    case GrpcStorageObjectType.VIDEO:
      return 3;
    default:
      throw new globalThis.Error(
        'Unrecognized enum value ' + object + ' for enum GrpcStorageObjectType',
      );
  }
}

export interface GrpcStorageObject {
  id: string;
  createdAt: Date;
  updatedAt?: Date;
  name: string;
  isPublic: boolean;
  isFolder: boolean;
  type: GrpcStorageObjectType;
  userId: string;
  fileId?: string;
  parentId?: string;
  folderPath?: string;
  imageId?: string;
  videoId?: string;
}

function createBaseGrpcStorageObject(): GrpcStorageObject {
  return {
    id: '',
    createdAt: undefined,
    updatedAt: undefined,
    name: '',
    isPublic: false,
    isFolder: false,
    type: GrpcStorageObjectType.FOLDER,
    userId: '',
    fileId: undefined,
    parentId: undefined,
    folderPath: undefined,
    imageId: undefined,
    videoId: undefined,
  };
}

export const GrpcStorageObject: MessageFns<GrpcStorageObject> = {
  encode(message: GrpcStorageObject, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== '') {
      writer.uint32(10).string(message.id);
    }
    if (message.createdAt !== undefined) {
      GrpcTimestamp.encode(toTimestamp(message.createdAt), writer.uint32(18).fork()).join();
    }
    if (message.updatedAt !== undefined) {
      GrpcTimestamp.encode(toTimestamp(message.updatedAt), writer.uint32(26).fork()).join();
    }
    if (message.name !== '') {
      writer.uint32(34).string(message.name);
    }
    if (message.isPublic !== false) {
      writer.uint32(40).bool(message.isPublic);
    }
    if (message.isFolder !== false) {
      writer.uint32(48).bool(message.isFolder);
    }
    if (message.type !== GrpcStorageObjectType.FOLDER) {
      writer.uint32(56).int32(grpcStorageObjectTypeToNumber(message.type));
    }
    if (message.userId !== '') {
      writer.uint32(66).string(message.userId);
    }
    if (message.fileId !== undefined) {
      writer.uint32(74).string(message.fileId);
    }
    if (message.parentId !== undefined) {
      writer.uint32(82).string(message.parentId);
    }
    if (message.folderPath !== undefined) {
      writer.uint32(90).string(message.folderPath);
    }
    if (message.imageId !== undefined) {
      writer.uint32(98).string(message.imageId);
    }
    if (message.videoId !== undefined) {
      writer.uint32(106).string(message.videoId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GrpcStorageObject {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGrpcStorageObject();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.createdAt = fromTimestamp(GrpcTimestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.updatedAt = fromTimestamp(GrpcTimestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.isPublic = reader.bool();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.isFolder = reader.bool();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.type = grpcStorageObjectTypeFromJSON(reader.int32());
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.fileId = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.parentId = reader.string();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.folderPath = reader.string();
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.imageId = reader.string();
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.videoId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GrpcStorageObject {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : '',
      createdAt: isSet(object.createdAt) ? fromJsonTimestamp(object.createdAt) : undefined,
      updatedAt: isSet(object.updatedAt) ? fromJsonTimestamp(object.updatedAt) : undefined,
      name: isSet(object.name) ? globalThis.String(object.name) : '',
      isPublic: isSet(object.isPublic) ? globalThis.Boolean(object.isPublic) : false,
      isFolder: isSet(object.isFolder) ? globalThis.Boolean(object.isFolder) : false,
      type: isSet(object.type)
        ? grpcStorageObjectTypeFromJSON(object.type)
        : GrpcStorageObjectType.FOLDER,
      userId: isSet(object.userId) ? globalThis.String(object.userId) : '',
      fileId: isSet(object.fileId) ? globalThis.String(object.fileId) : undefined,
      parentId: isSet(object.parentId) ? globalThis.String(object.parentId) : undefined,
      folderPath: isSet(object.folderPath) ? globalThis.String(object.folderPath) : undefined,
      imageId: isSet(object.imageId) ? globalThis.String(object.imageId) : undefined,
      videoId: isSet(object.videoId) ? globalThis.String(object.videoId) : undefined,
    };
  },

  toJSON(message: GrpcStorageObject): unknown {
    const obj: any = {};
    if (message.id !== '') {
      obj.id = message.id;
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.updatedAt !== undefined) {
      obj.updatedAt = message.updatedAt.toISOString();
    }
    if (message.name !== '') {
      obj.name = message.name;
    }
    if (message.isPublic !== false) {
      obj.isPublic = message.isPublic;
    }
    if (message.isFolder !== false) {
      obj.isFolder = message.isFolder;
    }
    if (message.type !== GrpcStorageObjectType.FOLDER) {
      obj.type = grpcStorageObjectTypeToJSON(message.type);
    }
    if (message.userId !== '') {
      obj.userId = message.userId;
    }
    if (message.fileId !== undefined) {
      obj.fileId = message.fileId;
    }
    if (message.parentId !== undefined) {
      obj.parentId = message.parentId;
    }
    if (message.folderPath !== undefined) {
      obj.folderPath = message.folderPath;
    }
    if (message.imageId !== undefined) {
      obj.imageId = message.imageId;
    }
    if (message.videoId !== undefined) {
      obj.videoId = message.videoId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GrpcStorageObject>, I>>(base?: I): GrpcStorageObject {
    return GrpcStorageObject.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GrpcStorageObject>, I>>(object: I): GrpcStorageObject {
    const message = createBaseGrpcStorageObject();
    message.id = object.id ?? '';
    message.createdAt = object.createdAt ?? undefined;
    message.updatedAt = object.updatedAt ?? undefined;
    message.name = object.name ?? '';
    message.isPublic = object.isPublic ?? false;
    message.isFolder = object.isFolder ?? false;
    message.type = object.type ?? GrpcStorageObjectType.FOLDER;
    message.userId = object.userId ?? '';
    message.fileId = object.fileId ?? undefined;
    message.parentId = object.parentId ?? undefined;
    message.folderPath = object.folderPath ?? undefined;
    message.imageId = object.imageId ?? undefined;
    message.videoId = object.videoId ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): GrpcTimestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: GrpcTimestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === 'string') {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(GrpcTimestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
