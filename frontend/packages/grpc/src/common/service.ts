// @ts-nocheck
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.5
//   protoc               v7.34.0
// source: common/service.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { GrpcTimestamp } from "../google/protobuf/timestamp";
import { GrpcCrudConditionalFilter, GrpcCrudLogicalFilter, GrpcCrudSorter } from "./crud";
import "server-only";

export interface GrpcIdField {
  id: string;
}

export interface GrpcIdsField {
  ids: string[];
}

export interface GrpcEntityWithTimestamps {
  id: string;
  createdAt: Date;
  updatedAt?: Date;
}

export interface GrpcBaseQuery {
  id?: string;
  ids: string[];
}

export interface GrpcBaseRequest {
  query: GrpcBaseQuery;
}

export interface GrpcPagination {
  page?: number;
  limit?: number;
}

export interface GrpcGetListRequest {
  logicalFilters: GrpcCrudLogicalFilter[];
  conditionalFilters: GrpcCrudConditionalFilter[];
  sorters: GrpcCrudSorter[];
  pagination?: GrpcPagination;
}

export interface GrpcBooleanResult {
  value: boolean;
}

function createBaseGrpcIdField(): GrpcIdField {
  return { id: "" };
}

export const GrpcIdField: MessageFns<GrpcIdField> = {
  encode(message: GrpcIdField, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GrpcIdField {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGrpcIdField();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GrpcIdField {
    return { id: isSet(object.id) ? globalThis.String(object.id) : "" };
  },

  toJSON(message: GrpcIdField): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GrpcIdField>, I>>(base?: I): GrpcIdField {
    return GrpcIdField.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GrpcIdField>, I>>(object: I): GrpcIdField {
    const message = createBaseGrpcIdField();
    message.id = object.id ?? "";
    return message;
  },
};

function createBaseGrpcIdsField(): GrpcIdsField {
  return { ids: [] };
}

export const GrpcIdsField: MessageFns<GrpcIdsField> = {
  encode(message: GrpcIdsField, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.ids) {
      writer.uint32(10).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GrpcIdsField {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGrpcIdsField();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.ids.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GrpcIdsField {
    return { ids: globalThis.Array.isArray(object?.ids) ? object.ids.map((e: any) => globalThis.String(e)) : [] };
  },

  toJSON(message: GrpcIdsField): unknown {
    const obj: any = {};
    if (message.ids?.length) {
      obj.ids = message.ids;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GrpcIdsField>, I>>(base?: I): GrpcIdsField {
    return GrpcIdsField.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GrpcIdsField>, I>>(object: I): GrpcIdsField {
    const message = createBaseGrpcIdsField();
    message.ids = object.ids?.map((e) => e) || [];
    return message;
  },
};

function createBaseGrpcEntityWithTimestamps(): GrpcEntityWithTimestamps {
  return { id: "", createdAt: undefined, updatedAt: undefined };
}

export const GrpcEntityWithTimestamps: MessageFns<GrpcEntityWithTimestamps> = {
  encode(message: GrpcEntityWithTimestamps, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.createdAt !== undefined) {
      GrpcTimestamp.encode(toTimestamp(message.createdAt), writer.uint32(18).fork()).join();
    }
    if (message.updatedAt !== undefined) {
      GrpcTimestamp.encode(toTimestamp(message.updatedAt), writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GrpcEntityWithTimestamps {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGrpcEntityWithTimestamps();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.createdAt = fromTimestamp(GrpcTimestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.updatedAt = fromTimestamp(GrpcTimestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GrpcEntityWithTimestamps {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      createdAt: isSet(object.createdAt) ? fromJsonTimestamp(object.createdAt) : undefined,
      updatedAt: isSet(object.updatedAt) ? fromJsonTimestamp(object.updatedAt) : undefined,
    };
  },

  toJSON(message: GrpcEntityWithTimestamps): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.updatedAt !== undefined) {
      obj.updatedAt = message.updatedAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GrpcEntityWithTimestamps>, I>>(base?: I): GrpcEntityWithTimestamps {
    return GrpcEntityWithTimestamps.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GrpcEntityWithTimestamps>, I>>(object: I): GrpcEntityWithTimestamps {
    const message = createBaseGrpcEntityWithTimestamps();
    message.id = object.id ?? "";
    message.createdAt = object.createdAt ?? undefined;
    message.updatedAt = object.updatedAt ?? undefined;
    return message;
  },
};

function createBaseGrpcBaseQuery(): GrpcBaseQuery {
  return { id: undefined, ids: [] };
}

export const GrpcBaseQuery: MessageFns<GrpcBaseQuery> = {
  encode(message: GrpcBaseQuery, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      writer.uint32(10).string(message.id);
    }
    for (const v of message.ids) {
      writer.uint32(18).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GrpcBaseQuery {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGrpcBaseQuery();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.ids.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GrpcBaseQuery {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : undefined,
      ids: globalThis.Array.isArray(object?.ids) ? object.ids.map((e: any) => globalThis.String(e)) : [],
    };
  },

  toJSON(message: GrpcBaseQuery): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = message.id;
    }
    if (message.ids?.length) {
      obj.ids = message.ids;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GrpcBaseQuery>, I>>(base?: I): GrpcBaseQuery {
    return GrpcBaseQuery.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GrpcBaseQuery>, I>>(object: I): GrpcBaseQuery {
    const message = createBaseGrpcBaseQuery();
    message.id = object.id ?? undefined;
    message.ids = object.ids?.map((e) => e) || [];
    return message;
  },
};

function createBaseGrpcBaseRequest(): GrpcBaseRequest {
  return { query: undefined };
}

export const GrpcBaseRequest: MessageFns<GrpcBaseRequest> = {
  encode(message: GrpcBaseRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.query !== undefined) {
      GrpcBaseQuery.encode(message.query, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GrpcBaseRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGrpcBaseRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.query = GrpcBaseQuery.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GrpcBaseRequest {
    return { query: isSet(object.query) ? GrpcBaseQuery.fromJSON(object.query) : undefined };
  },

  toJSON(message: GrpcBaseRequest): unknown {
    const obj: any = {};
    if (message.query !== undefined) {
      obj.query = GrpcBaseQuery.toJSON(message.query);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GrpcBaseRequest>, I>>(base?: I): GrpcBaseRequest {
    return GrpcBaseRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GrpcBaseRequest>, I>>(object: I): GrpcBaseRequest {
    const message = createBaseGrpcBaseRequest();
    message.query = (object.query !== undefined && object.query !== null)
      ? GrpcBaseQuery.fromPartial(object.query)
      : undefined;
    return message;
  },
};

function createBaseGrpcPagination(): GrpcPagination {
  return { page: undefined, limit: undefined };
}

export const GrpcPagination: MessageFns<GrpcPagination> = {
  encode(message: GrpcPagination, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.page !== undefined) {
      writer.uint32(8).int32(message.page);
    }
    if (message.limit !== undefined) {
      writer.uint32(16).int32(message.limit);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GrpcPagination {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGrpcPagination();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.page = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.limit = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GrpcPagination {
    return {
      page: isSet(object.page) ? globalThis.Number(object.page) : undefined,
      limit: isSet(object.limit) ? globalThis.Number(object.limit) : undefined,
    };
  },

  toJSON(message: GrpcPagination): unknown {
    const obj: any = {};
    if (message.page !== undefined) {
      obj.page = Math.round(message.page);
    }
    if (message.limit !== undefined) {
      obj.limit = Math.round(message.limit);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GrpcPagination>, I>>(base?: I): GrpcPagination {
    return GrpcPagination.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GrpcPagination>, I>>(object: I): GrpcPagination {
    const message = createBaseGrpcPagination();
    message.page = object.page ?? undefined;
    message.limit = object.limit ?? undefined;
    return message;
  },
};

function createBaseGrpcGetListRequest(): GrpcGetListRequest {
  return { logicalFilters: [], conditionalFilters: [], sorters: [], pagination: undefined };
}

export const GrpcGetListRequest: MessageFns<GrpcGetListRequest> = {
  encode(message: GrpcGetListRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.logicalFilters) {
      GrpcCrudLogicalFilter.encode(v!, writer.uint32(10).fork()).join();
    }
    for (const v of message.conditionalFilters) {
      GrpcCrudConditionalFilter.encode(v!, writer.uint32(18).fork()).join();
    }
    for (const v of message.sorters) {
      GrpcCrudSorter.encode(v!, writer.uint32(26).fork()).join();
    }
    if (message.pagination !== undefined) {
      GrpcPagination.encode(message.pagination, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GrpcGetListRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGrpcGetListRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.logicalFilters.push(GrpcCrudLogicalFilter.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.conditionalFilters.push(GrpcCrudConditionalFilter.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.sorters.push(GrpcCrudSorter.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.pagination = GrpcPagination.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GrpcGetListRequest {
    return {
      logicalFilters: globalThis.Array.isArray(object?.logicalFilters)
        ? object.logicalFilters.map((e: any) => GrpcCrudLogicalFilter.fromJSON(e))
        : [],
      conditionalFilters: globalThis.Array.isArray(object?.conditionalFilters)
        ? object.conditionalFilters.map((e: any) => GrpcCrudConditionalFilter.fromJSON(e))
        : [],
      sorters: globalThis.Array.isArray(object?.sorters)
        ? object.sorters.map((e: any) => GrpcCrudSorter.fromJSON(e))
        : [],
      pagination: isSet(object.pagination) ? GrpcPagination.fromJSON(object.pagination) : undefined,
    };
  },

  toJSON(message: GrpcGetListRequest): unknown {
    const obj: any = {};
    if (message.logicalFilters?.length) {
      obj.logicalFilters = message.logicalFilters.map((e) => GrpcCrudLogicalFilter.toJSON(e));
    }
    if (message.conditionalFilters?.length) {
      obj.conditionalFilters = message.conditionalFilters.map((e) => GrpcCrudConditionalFilter.toJSON(e));
    }
    if (message.sorters?.length) {
      obj.sorters = message.sorters.map((e) => GrpcCrudSorter.toJSON(e));
    }
    if (message.pagination !== undefined) {
      obj.pagination = GrpcPagination.toJSON(message.pagination);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GrpcGetListRequest>, I>>(base?: I): GrpcGetListRequest {
    return GrpcGetListRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GrpcGetListRequest>, I>>(object: I): GrpcGetListRequest {
    const message = createBaseGrpcGetListRequest();
    message.logicalFilters = object.logicalFilters?.map((e) => GrpcCrudLogicalFilter.fromPartial(e)) || [];
    message.conditionalFilters = object.conditionalFilters?.map((e) => GrpcCrudConditionalFilter.fromPartial(e)) || [];
    message.sorters = object.sorters?.map((e) => GrpcCrudSorter.fromPartial(e)) || [];
    message.pagination = (object.pagination !== undefined && object.pagination !== null)
      ? GrpcPagination.fromPartial(object.pagination)
      : undefined;
    return message;
  },
};

function createBaseGrpcBooleanResult(): GrpcBooleanResult {
  return { value: false };
}

export const GrpcBooleanResult: MessageFns<GrpcBooleanResult> = {
  encode(message: GrpcBooleanResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.value !== false) {
      writer.uint32(8).bool(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GrpcBooleanResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGrpcBooleanResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.value = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GrpcBooleanResult {
    return { value: isSet(object.value) ? globalThis.Boolean(object.value) : false };
  },

  toJSON(message: GrpcBooleanResult): unknown {
    const obj: any = {};
    if (message.value !== false) {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GrpcBooleanResult>, I>>(base?: I): GrpcBooleanResult {
    return GrpcBooleanResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GrpcBooleanResult>, I>>(object: I): GrpcBooleanResult {
    const message = createBaseGrpcBooleanResult();
    message.value = object.value ?? false;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): GrpcTimestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: GrpcTimestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(GrpcTimestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
