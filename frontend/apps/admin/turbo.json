{
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": [
        "src/**",
        "public/**",
        ".eslintrc.json",
        "Dockerfile",
        "next.config.mjs",
        "package.json",
        "tsconfig*.json"
      ],
      "outputs": [".next/**"]
    },
    "reset:modules": {
      "inputs": ["node_modules/**"]
    }
  }
}
