{
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": ["src/**", "package.json", "ts-config*.json", "proto/**"],
      "outputs": ["dist/**"],
      "dependsOn": ["compile", "^build"]
    },
    "compile": {
      "inputs": ["package.json", "ts-config*.json", "proto/**", "compiler/**"],
      "outputs": ["src/**"]
    }
  }
}
