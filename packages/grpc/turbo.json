{
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": ["src/**/*.ts", "package.json", "ts-config*.json", "proto/**"],
      "outputs": ["dist/**"],
      "dependsOn": ["^build", "compile"]
    },
    "compile": {
      "inputs": ["package.json", "ts-config*.json", "proto/**", "compiler/**"],
      "outputs": ["src/**"]
    }
  }
}
