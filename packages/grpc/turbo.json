{
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": ["src/**/*.ts", "package.json", "ts-config*.json", "proto/**", "compiler/**"],
      "outputs": ["dist/**"]
    },
    "compile": {
      "inputs": ["package.json", "ts-config*.json", "proto/**", "compiler/**"],
      "outputs": ["src/**"]
    }
  }
}
