syntax = 'proto3';

package auth;

import "services/auth.proto";

service AuthService {
  rpc login (services.auth.AuthLogin) returns (services.auth.AuthData) {}
  rpc refreshToken (services.auth.AuthRefresh) returns (services.auth.AuthTokens) {}
}
