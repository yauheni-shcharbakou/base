{
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": ["src/**", "package.json", "ts-config*.json"],
      "outputs": ["dist/**"]
    },
    "compile": {
      "inputs": ["package.json", "ts-config*.json", "pkg/**", "compiler/**"],
      "outputs": ["src/**"]
    },
    "reset:modules": {
      "inputs": ["package.json"]
    }
  }
}
