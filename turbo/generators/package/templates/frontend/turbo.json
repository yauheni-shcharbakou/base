{
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": [
        "src/**/*.ts",
        "src/**/*.tsx",
        "package.json",
        "tsconfig*.json",
        "tsdown.config.mts"
      ],
      "outputs": ["dist/**"]
    }
  }
}
