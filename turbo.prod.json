{
  "$schema": "https://turbo.build/schema.json",
  "concurrency": "10",
  "envMode": "loose",
  "tasks": {
    "build": {
      "dependsOn": ["^build"]
    },
    "reset:modules": {
      "cache": false
    }
  }
}
